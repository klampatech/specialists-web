import {Vector3} from "@babylonjs/core";export const COMBAT={dualPistol:{fireCooldownMs:120,damage:12,tracerDurationMs:80,tracerColor:"#ffce5a"},melee:{coneRadians:Math.PI/3,rangeMeters:1.5,damage:25,swingDurationMs:220},bulletTime:{scale:.25,energyMax:100,energyDrainPerSec:33,energyRechargePerSec:20}} as const;export function isWithinMeleeCone(a:Vector3,f:Vector3,t:Vector3,r=0){const d=t.subtract(a);return d.length()<=COMBAT.melee.rangeMeters+r&&d.length()>0&&Vector3.Dot(f.normalize(),d.normalize())>=Math.cos(COMBAT.melee.coneRadians/2)};export const dualPistolShoot=()=>{};export const meleeSwing=()=>{};export const bulletTime=()=>{};
