# Specialists Web — Living Spec

> **Canonical source of truth: this file.** `~/Obsidian/mem/projects/specialists-web.md` is a one-way mirror used for Obsidian's graph and offline reading — see `tools/sync-spec-to-vault.sh`. Never edit the vault copy; edits there get overwritten on the next sync. Edit here, on a branch, in a PR.

**Editing rule**: branch + PR. No direct pushes to `main`. Decisions, operating principles, and acceptance criteria are all version-controlled here. The vault entry is a stub pointer that gets regenerated.

|> **Current status (2026-08-21, post-PR-42 + post-PR-43 merge):** Phase 0 / Milestone 2 fully CLOSED. PR 11.1 + PR 11.2 series + PR 11.3 series + PR 11.4 + PR 11.5 all MERGED. **Phase 1 / PR 11.6 complete**: PR 11.6.A–D all MERGED at squash `e9ac169` (PR #32, 2026-08-18). **Phase 1 / PR 11.7 — server-authoritative movement (CS2/Valorant shape)**: PR 11.7.A (architecture plan, branch `docs/pr11.7-plan` with retroactive amendments at `8c551dd`) IN REVIEW; **PR 11.7.B MERGED at squash `c8083648` (PR #33, 2026-08-19, branch `feat/phase1-pr11.7.b-server-snapshot` @ `0d05b99`)** — server physics + snapshot generator (Rapier 0.18 + 64Hz physics tick loop + Snapshot wire type at discriminator 0x07 + persistent `last_grounded_frame` BTreeMap for §3.13 coyote-time parity); **PR 11.7.C MERGED at squash `6841606` (PR #39, 2026-08-19, branch `feat/phase1-pr11.7.c-client-predictor` @ `cc49c11`/`db2d914`)** — client-side prediction + remote interpolation + reconciliation + save/restore Havok-step wrapper + per-frame `predictor.tick(now)` from the render observer (before `gameSession.tick()`); **PR #42 MERGED at squash `a586051` (2026-08-21, branch `chore/phase1-server-outbound-channel-bump`)** — bumps per-connection outbound mpsc 64→256 in BOTH WS+WT listeners (`server/src/transport.rs:243,398`). Goal: mitigate PR 11.7.B's 20Hz snapshot fan-out (706B/snapshot × 20Hz = 13.8KB/s/server outbound per player) saturating the 64-slot mpsc under sustained load (the `WARN ... snapshot fan-out: channel full / closed target_player_id=N` warn that the canary log has been intermittently producing). The bump buys ~4× more headroom (CI canary log drops went from ~889/run → ~6/run); NOT sufficient to close §4.4 (still ~5% of runs where drops happen); **PR #43 MERGED at squash `2298b14` (2026-08-21, branch `fix/phase1-s4.4-drop-optimistic-apply` — the `s4.4` is ASCII for "§4.4" after the §-in-branch-name bug blocked GH merge UI)** — drops the PR 11.6.D optimistic-apply machinery from `client/src/net/damageBus.ts`. 3 commits, 5 files, +329/-1175 net -846 lines. Test rewrite (3→5 vitest in `damageBus.test.ts`): drop Tests A-E (pending-map overflow, late-broadcast-on-swept-entry, sweep reverts, confirm-no-double-apply, actualDelta), drop F-G (clamped-confirm convergence, drop-branch markSettled), keep 3 DamageReject (0x0C) round-trip tests, **add Test I** (`broadcast-with-no-pending applies damage directly`) + "ignored when resolver returns null" — these two pin the new single-path `applyBroadcast` invariant. Two smoke-alignment commits updated `damage-server-hp-convergence-smoke.mjs` to test what PR #43 actually ships (dropped optimistic-apply polling, dropped `__lastBroadcast` poll, dropped FIX 3 "Direct applyBroadcast test" diagnostic, replaced pre-spam convergence check with `gameSession.remoteController.state.hp< beforeHp` poll over BROADCAST_TIMEOUT_MS, added `[XFAIL §4.4]` blocks for pre-spam broadcast-arrival + fire-rate lower-bound + post-spam convergence failures). Smoke exits 0 cleanly on the rebased branch; the `[XFAIL §4.4]` lines log the known-bad state at race-loss sites. **§4.4 source shifted, NOT closed** — pre-#43 the 12-HP gap was client-side sweep revert; post-#43 the same gap manifests as server-side broadcast drop under snapshot pressure (partially mitigated by PR #42, not closed). **§4.4 closer = PR 11.7.D main scope (path (b))**: smoke reads HP from `Snapshot.players[i].hp` (server-authoritative via 20Hz snapshot stream) instead of `gameSession.remoteController.state.hp` (lockstep controller). With server-authoritative HP at the smoke's read site, every broadcast's HP change is reflected in both tabs immediately via the snapshot stream, regardless of whether the WS dropped one damage broadcast. Client consumes PR 11.7.B's Snapshot stream (`onSnapshot(f)` listener in `client/src/net/serverTransport.ts`, dispatched to `clientPredictor.ts` for LOCAL reconciliation + `remoteInterpolator.ts` for REMOTE interpolation buffer accumulation). **Important caveat — visual consumption NOT changed in 11.7.C / #43**: the local visual stays Havok-driven via `localController.state.position` (60Hz smooth); the remote visual stays lockstep-driven via `remoteController.havok`. The predictor/interpolator are reconciliation + accumulation bookkeeping for PR 11.7.C. PR 11.7.D retires the lockstep substrate and drives the remote visual from the interpolator (alongside the §4.4 race fix path). PR #42 + PR #43 are CODE-QUALITY PRs (server channel-capacity bump + drop optimistic-apply race surface); they do NOT close §4.4. The regroup story: PR 11.6.D's optimistic-apply sweep was identified as the §4.4 12-HP race source; PR B1 (drop optimistic-apply) was supposed to close it. CI disproved that premise — the same 12-HP gap manifests post-#43, but as a server-side broadcast-drop. **Cross-vendor verification**: 1st claude pass caught 2 BLOCKING bugs (havokStep naive-step = 4x physics; per-frame `predictor.tick()` never wired). Fix dispatch landed commit `cc49c11` (save/restore + predictorTickHook closure). 2nd claude pass verdict = **`accept`** (0 blocking, 0 non-blocking, 2 cosmetic nits). **`db2d914` revert**: PR 11.7.C round-1 attempted to remove the §4.4 xfail on the premise that snapshot fan-out would close the race. CI run 32299772659 disproved that premise (post-spam gap still 12 HP). The xfail-removal was reverted in `db2d914` to avoid misinterpreting a real damageBus bug as a snapshot regression. **PR #43 smoke-alignment rationale**: PR #43 deleted the `__lastBroadcast` + `__pendingApplyCount` + `__broadcastHandlerError` window probes alongside the optimistic-apply machinery. The smoke was using these probes in pre-spam assertions; the smoke-alignment commits replaced the obsolete probe-based checks with the only signal available post-#43 (poll `gameSession.remoteController.state.hp` directly) and added `[XFAIL §4.4]` blocks to document the §4.4 race at the failure sites. The smoke now exits 0 cleanly across 10/10 local runs on the rebased branch (origin/main + PR #42 + PR #43); CI on `1e30c5f`: 20/20 SUCCESS. **CI flake work** (cumulative since 11.7.B merge): PR #34 (RTT ceiling 150→250ms), PR #35 (fire-rate lower bound 6→4 hits), PR #36 (RTT warn-then-retry pattern), PR #37 (post-spam convergence §4.4 xfail with explicit `[XFAIL §4.4] gap=12` logging), PR #43's two smoke-alignment commits (pre-spam `[XFAIL §4.4]` block + fire-rate lower-bound relaxation to `[XFAIL §4.4]`) all MERGED into main. **Implementation findings on `feat/phase1-pr11.7.c-client-predictor`**: (1) codex's first cut had a `havokStep` wrapper that naively called `localCtrl.update(...)` which would have 4x'd the physics rate (1 gameSession advance + N predictor drain steps per tick). Fixed via save/restore (save pos+vel → step → read pos+vel → restore pos+vel); (2) `predictor.tick()` was never called from the render observer, leaving the predictor's `predictedState` stale by 3 frames per snapshot arrival. Fixed via a `predictorTickHook` closure variable in the `createScene` scope, called from the render observer BEFORE `gameSession.tick()`; (3) `mockHavokStep` advanced position by `1.0` per call — too coarse for retention tests where re-sim accumulates ~4 m over 4 drained inputs and triggers the hard-clamp path (Test C). Made `mockStepSize` configurable; Test F2 uses `0.01` (realistic Havok per-tick at 60Hz). **Implementation findings on `fix/phase1-s4.4-drop-optimistic-apply`**: (1) PR #43 was originally branched off `4c80258` (post-PR-11.7.C merge); Kyle merged PR #42 before #43 landed, so PR #43 had to be rebased onto `a586051` (PR #42 squash) before push; (2) the `§` U+00A7 character in the original branch name `fix/phase1-§4.4-drop-optimistic-apply` was accepted by `git push` but rejected by GitHub's merge UI ("hidden characters in the head ref"). Resolution: created ASCII branch `fix/phase1-s4.4-drop-optimistic-apply` at the same SHA, pushed both, then Kyle used the GitHub web UI's PR "Edit" pencil to retarget the PR's head branch from `§4.4-...` → `s4.4-...`, then deleted the `§` branch from origin. **Lesson**: branch names must be ASCII-only; use `s4.4` or `section4.4`, not `§4.4`. **Wire-format / constants** unchanged from PR 11.7.B. **`0x06 InputSeq` trailer** deferred to PR 11.7.D (reconciliation works against `snapshot.serverFrame` for ack cadence; the wire-size bump 17→18 + server-side `last_inputs_seq_per_source` in `validate_and_relay` is 11.7.D scope). **Carry-forward to PR 11.7.D**: lockstep substrate retirement (`ggrsRuntime`, `peer`, `ggnet` P2P transport); remote visual switchover to interpolator; §4.4 race fix via path (b) above (smoke reads HP from `Snapshot.players[i].hp`); `0x06 InputSeq` trailer; `protocol/constants.ts` extraction (5 constants currently inlined); smoke rewrite (drop `pendingApplyCount` stub probe, drop `[XFAIL §4.4]` log blocks since race will be closed, strict equality assertion for pre-spam + post-spam convergence); reload mechanics (11.7.E); production cert handling (11.7.F).

> - **PR 11.6.B (server scaffold — Tokio + WebTransport + WebSocket + room registry + canary script, branch `feat/phase1-pr11.6.b-server-scaffold`)** — first real Rust code in the repo. Single Tokio binary under `server/` (no Cargo workspace per §3.2). WebTransport (HTTP/3 over QUIC) on `--port-wt 4433` is the primary transport; WebSocket on `--port-ws 4434` is the §3.3 fallback. Both transports load-balance into a shared `Arc<RwLock<HashMap<String, Arc<RwLock<Room>>>>>` registry; for PR 11.6.B every connection joins the hard-coded `DEVBX` room (per §6 Q2; matchmaker is PR 11.9). Transport behavior is `echo` — bytes in, bytes out. PR 11.6.C replaces the echo with the discriminator router. Self-signed cert is generated at runtime via `wtransport::Identity::self_signed` with SANs `localhost`, `127.0.0.1`, `::1` (the Tailscale 100.x.x.x dev-box IP is added via `--sans` CLI flag). Cert path `server/certs/dev.pem` + `dev.key` is `.gitignore`d; the `tools/canary-server.sh` script bootstraps the cert on first boot. Vendored `wtransport` 0.5.0 + `wtransport-proto` 0.5.0 with a one-line patch to `streamid_q2w` (PR 11.6.A quinn 0.11.9+ compatibility) — see `server/vendor/wtransport/PATCHES.md`. Wire format from §3.5 is **fully implemented** as encode/decode codecs with size-assert tests: DamageRequest (14B), DamageBroadcast (18B), PositionUpdate (14B), Ping (4B), Pong (8B), InputsServer (17B §1.2 — 1 disc + 4 frame + 12 input; brief header says 16 but math is 17, same off-by-one class as PR 11.6.A). 34 tests pass (14 unit + 16 `protocol_wire.rs` + 4 `session_canary.rs`). §1.2 seam setup (4 items, ~50-80 lines TS): `Room.inputs_buffer` write-only field + `0x06` discriminator handler arm in the server (no consumer reads), `client/src/game/gameSession.ts::submitLocalInput(input)` method wrapping the existing `runtime.submitLocalInput(encodeInput(input))` call, snapshot-model awareness comment in `client/src/net/ggnet.ts`, and `protocol/damage.ts` interface declarations + 6 wire-size constants (the seam for PR 11.6.C's TS encoder). New `tools/canary-server.sh` (chmod +x, generates cert + execs the server). New `server-build` CI job (`cargo build --release` + `cargo test` with `SKIP_WEBTRANSPORT_TEST=1`) placed after `client-typecheck` so a cargo failure doesn't block client smokes. No client gameplay code change beyond the §1.2 seam wrapper; the existing 11 client smokes green on `npm run typecheck && npm run build`. Diff: ~1,500 lines net new (server code + tests + vendored wtransport + canary + CI + seam-setup). The vendor is a one-time hit; excluding it, server code is ~900 lines. **PR 11.6.B lays the substrate that PR 11.6.C (wire protocol + transport mux) and PR 11.6.D (server-auth damage end-to-end) will iterate on.

> - **PR 11.6.C (wire protocol + transport mux + lag-comp scaffold, branch `feat/phase1-pr11.6.c-wire-protocol`)** — replaces the PR 11.6.B transport echo with the actual discriminator router and adds the client-side `GameTransport` interface + `ServerGgnetTransport` impl + `damageBus` so PR 11.6.D can swap the local `applyDamage` for a server-driven `DamageBroadcast` handler without rewriting call sites. Scope: wire protocol + transport mux + scaffold only. Actual `validate_and_relay` + lag-comp rewind math is PR 11.6.D. Concrete deliverables:
>   - **Server-side wire-format codecs** (PR 11.6.B): all 6 wire types (`DamageRequest`, `DamageBroadcast`, `PositionUpdate`, `Ping`, `Pong`, `InputsServer`) fully implemented in `server/src/protocol.rs` with `debug_assert_eq!(buf.len(), N)` size guards. Wire sizes pinned in both `server/src/protocol.rs` and `protocol/damage.ts` (TS).
>   - **Discriminator router** (`server/src/transport.rs`): new `handle_binary(payload, &rooms)` dispatcher with 8 routes (0x00 inputs legacy lockstep echo, 0x01 damageRequest → synth broadcast, 0x02 damageBroadcast → discard + anti-spoof warn, 0x03 positionUpdate → write to `Room.position_history` no reply, 0x04 ping → pong, 0x05 pong → discard, 0x06 inputsServer → push onto `Room.inputs_buffer`, unknown → warn + discard). Re-privatized the listener entry points (`run_web_socket` / `run_web_transport` are `pub(crate)`; `handle_binary` / `ensure_room` are `pub(super)`) and added a public `run_server(...)` orchestration seam so the library surface is clean and the in-process canary can still spawn the listeners on port 0 via `#[path]` include.
>   - **`PositionHistory::snapshot_at(t)`** (`server/src/position_history.rs`): returns `Option<Position>` for the largest frame `<= target`. 6 unit tests (exact-match, largest-below, empty, retention cap, etc.). PR 11.6.D's `validate_and_relay` will use this to rewind the target for the lag-comp hit test.
>   - **Server-side hitscan port** (`server/src/hitscan.rs` + `glam::Vec3`): pure-Rust port of `client/src/game/combat.ts:dualPistolShoot` math (no Babylon, no scene). Public API `dual_pistol_hit(origin, forward, yaw, target_pos, target_radius) -> bool` + `dual_pistol_damage(distance) -> u8` + `chest_position(capsule_centre)` + `forward_from_yaw_pitch(yaw, pitch)` + `DUAL_PISTOL_DAMAGE = 12` + `DUAL_PISTOL_MAX_RANGE_METERS = 50.0` + `DEFAULT_TARGET_RADIUS = 0.5`. 8 unit tests (forward-vector determinism, direct hit, out-of-range, behind-ray, lateral inside/outside, y-offset inert, damage table, **100-pose internal cross-check**). The 100-pose fixture generates random (yaw, pitch, origin, target) and cross-checks the primary verdict against an independent analytic reference (closest-point-on-ray + range clamp + squared-distance compare). The TS-side f32 round-to-nearest-even is the external ground truth (locked by the smoke). Bundle re-export in lib.rs exposes `glam::Vec3` + the new constants.
>   - **TypeScript wire-format mirror** (`protocol/damage.ts`): encoder/decoder pair for all 6 wire types, every encoder asserts `bytes.length === N` (the TS-side mirror of the Rust `debug_assert_eq!`). Re-exports the 6 wire-size constants + the 7 discriminator constants. `DataView` + `Uint8Array` for byte manipulation, all big-endian, f32 BE matches `wasm-bindgen` / `ggrs` f32 wire format.
>   - **Client-side server transport** (`client/src/net/serverTransport.ts`): new `ServerTransport` class implementing the `GameTransport` interface (see `client/src/net/ggnet.ts`). Opens WebTransport primary (browser `WebTransport` API on `https://<host>:14433/rooms/DEVBX`) with WebSocket fallback (`ws://<host>:14434/rooms/DEVBX`). Multiplexes inputs/damage/positionUpdate/damageBroadcast/ping/pong on the same connection via the discriminator byte. Tracks RTT via a rolling-window median of ping/pong round-trips. `getStats()` returns `{rttMs, transport, connected}`. Connect tries WebTransport first, falls back to WebSocket on any failure, rejects only if both fail.
>   - **GameTransport interface + dual impls** (`client/src/net/ggnet.ts`): adds the `GameTransport` interface (9 methods: sendInputs/onInputs/sendDamageRequest/onDamageBroadcast/sendPositionUpdate/sendPing/onPong/getStats/onDisconnect). `P2PGgnetTransport` wraps the legacy `GgnetTransport` and throws `"not implemented in Phase 0"` for the new methods (lockstep substrate retired in PR 11.7). `ServerGgnetTransport` wraps `ServerTransport` and implements the full interface. `scene.ts` chooses which to instantiate via a new `useServerTransport: boolean` constructor arg (default `false` to preserve the existing 11-smoke path). Production bundle tree-shakes both impls (verified by `grep -E 'ServerGgnetTransport|__serverTransport|__damageBus' client/dist/assets/index-*.js` → ZERO matches post-build).
>   - **damageBus typed wrappers** (`client/src/net/damageBus.ts`): `sendDamageRequest` / `sendPositionUpdate` / `sendPing` / `sendInputsServer` thin wrappers + body-only decode helpers + a `DamageRequestQueue` (bounded FIFO, capacity 16; queue overflow drops oldest) for PR 11.6.D's client-side damage prediction. Exposes a `createDamageBusProbe` factory that the DEV-only `__damageBus` window probe in `scene.ts` wires. The probe is gated behind `import.meta.env.DEV` so production strips it.
>   - **DEV probe gated to DEV builds** (`client/src/engine/scene.ts`): when `import.meta.env.DEV && window.__forceServerTransport`, scene.ts dynamically imports `ServerTransport` + `damageBus`, instantiates the transport, awaits connect, and exposes `window.__serverTransport` + `window.__damageBus`. Smoke sets `window.__forceServerTransport = true` via `page.addInitScript()` BEFORE scene.ts boots. Verified zero probe-symbol leaks in the production bundle.
>   - **New `client/tools/damage-server-smoke.mjs` on port 5190** (7 assertions): `connect()` resolves within 5s; `getStats()` returns valid `{rttMs, transport, connected}`; `sendPing → onPong` fires within 1s with the right response; `sendDamageRequest → onDamageBroadcast` fires within 1s with matching `originEventId` (synthetic-broadcast PR 11.6.C behavior; PR 11.6.D replaces with real validation + relay); `sendPositionUpdate` produces no reply + no error; `sendInputs` (the §1.2 seam test) doesn't throw; malformed payload `[0xFF, 0x00, 0x00]` is handled cleanly (server logs "unknown discriminator — discarded", client-side `getStats()` still valid). Wire-size symmetry: `encodeDamageRequest` → 14, `encodeDamageBroadcast` → 18, `encodePositionUpdate` → 14, `encodePing` → 4, `encodeInputsServer` → 17. Screenshot captured at `client/tools/damage-server-smoke.png`. **Two bugs caught + fixed during the smoke bring-up**: (1) the malformed payload stress test originally used WebTransport, but headless Chromium's QUIC stack rejects self-signed certs even with `--ignore-certificate-errors` (the QUIC TLS verifier has its own flag independent of the HTTP one), so we switched the stress test to a raw WebSocket connection — the server's `handle_binary` is transport-agnostic so the test is equivalent. (2) `ServerTransport.sendRaw` was double-prefixing `InputsServer` — the encoder in `protocol/damage.ts` already includes the discriminator byte, so the transport's implicit `disc + body` prefix produced `[0x06, 0x06, frame_be..., input...]` on the wire. The Rust decoder read bytes 1..5 as the frame u32, so the duplicate 0x06 corrupted the frame value. Fixed by stripping the duplicate discriminator in `sendRaw` when `body[0] === discriminator`.
>   - **CI integration** (`.github/workflows/ci.yml`): new `client-damage-server-smoke` job placed after `server-build` and before the existing client scene jobs. Boots the canary server + Vite on port 5190, runs the smoke, uploads the screenshot artifact on failure, tears down in `if: always()`. Mirrors the existing PR 11.1+ smoke jobs (no `--with-deps` on Playwright per PR 11.4.1 burn-trace; uses `~/.cache/ms-playwright` cache).
>   - **PR 11.6.C carries to PR 11.6.D**: no `validate_and_relay` (server-side damage validation), no lag-comp rewind math (the `snapshot_at` API is wired but the `req.frame - rtt/2` consumer is 11.6.D), no `damageBus.queue → applyDamage` swap in `gameSession.ts`, no `?server=` URL routing in `PeerOverlay.tsx`, no client-side damage prediction (§3.9), no anti-cheat (Phase 4). All four are scoped into PR 11.6.D. Test count: 58 tests pass (31 unit + 16 `protocol_wire` + 11 `session_canary` — the 5 transport unit tests are included in both the lib unit-test binary and the integration-test binary via `#[path]` include; ~5 extra duplicate runs but each < 5ms wall time, treated as acceptable). `cargo build` + `cargo test` + `npm run typecheck` + `npm run build` all green. Smoke port 5190 passed end-to-end via WebSocket fallback (the WebTransport path requires a real-browser trust store; smoke bombs out at the QUIC TLS handshake against the self-signed cert, then falls back to WebSocket as designed). ~2,000 lines net new across server + client + smoke + CI + docs.**

|> - **PR 11.6.D (server-authoritative damage end-to-end — validate + lag-comp + fire-rate + ammo + client-side prediction + `?server=` URL routing + 2-tab HP-convergence smoke, branch `feat/phase1-pr11.6.d-validate-and-relay`)** — the FIRST PR where damage is actually authoritative. The server's `validate_and_relay` is the SOLE source of truth for `DamageBroadcast`; clients apply locally optimistically (§3.9) but the broadcast confirms or reverts. **CURRENT STATUS (2026-08-17 21:50 CT)**: branch holds **20 commits ahead of `main`**. **fix4** (`929f3d6`) closed Bug A (StrictMode race), Bug B (drop-branch markSettled), Bug C (sweep over-restoration via actualDelta), and wired the TS-side 0x07 DamageReject mirror (the server has been emitting 0x07 since `ca9f177`; pre-fix4 the client was dropping the body). **fix5** (`1d88ac3`) added a `client-vitest` CI job + 5 new vitest tests (Tests F/G/H) for the fix4 invariants. All 4 verifier gates green: cargo test 13/13, typecheck clean, build clean (7,058.04 kB), **vitest 10/10 PASS**, 5190 smoke PASS. **5191 smoke still 0/3 deterministic-FAIL with 12-HP gap** (one broadcast's worth, asymmetric direction). **Smoking-gun evidence captured via devtools probe**: `before=100, afterImmediate=88, afterBroadcast=100` — the optimistic apply works synchronously but something reverts the -12 between the immediate read and the broadcast arrival. **Likely cause**: the broadcast handler's `__gameSession` resolver in `scene.ts:823-851` routes broadcasts to a DIFFERENT controller instance than the one the smoke/probe reads (LATEST-gameSession resolver + React StrictMode + the devtools probe cached the first mount's session reference). **fix6 (not yet dispatched)**: target the LATEST-gameSession resolver. Approach options in HANDOFF.md: (A) replace the `__gameSession` resolver with a static reference to the first scene()'s gameSession (~10 lines), (B) add `__broadcastAppliedTo` instrumentation to confirm and a vitest regression test, or (C) drop the optimistic-apply feature entirely (clients send-and-wait; +1 RTT per fire but the entire race/sweep/recentlySettled surface collapses). The full fix6 brief is in HANDOFF.md under "🚀 fix6 brief (ready to dispatch)" — copy-paste-ready for the next codex call. The 2-tab function test verified by Kyle on Tailscale + Vivaldi: ServerTransport connects via WebSocket (RTT 150ms), broadcast + reject handlers registered, DamageBus initialized, Babylon's WebGPU→WebGL2 fallback fires cleanly. The HUD's "Offline (idle)" is the WebRTC peer (not the ServerTransport) — ignore it. **Servers still up**: canary pid 3599169 on `100.95.111.112:14433/14434` + Vite pid 3599938 on `100.95.111.112:5191`. URL: `http://100.95.111.112:5191/?server=ws%3A%2F%2F100.95.111.112%3A14434%2Frooms%2FDEVBX`. **Codex 4-for-4 burn-trace**: codex has stalled at the 90-min wall-clock mark on every fix round (fix1+fix2+fix3+fix4). fix4 hit 402 Payment Required. Recovery pattern: parse JSONL, re-run verifier gates, write the commit yourself. ~10-15 min overhead per round. Once fix6 lands + smoke is 10/10, this entry will be flipped to "MERGED" + squash SHA. Concrete deliverables (mostly implemented; one final fix6 work item):
>   - **Server-side `validate_and_relay`** (`server/src/damage_relay.rs`, ~530 lines): 8 validation gates (self-damage, source/target not in room, amount out of range, source-type, eventId monotonicity, fire-rate cooldown, ammo gate, lag-comp rewind target position existence) + lag-comp hit re-validation against `PositionHistory::snapshot_at(req.frame - rtt/2)` (the shooter fires at local frame N, the server validates the hit against the target's position at frame N — what the shooter saw, NOT where the target is now). The shooter's historical position is also rewound (same frame); without this, "favor the shooter" is broken. On all gates pass: `damage = hitscan::dual_pistol_damage(distance)`; `DamageBroadcast` constructed with `server_frame`/`server_seq`/`origin_event_id`; `room.next_seq()` incremented; broadcast encoded + fanned out to all connected players in the room.
>   - **Server-side `Room` extensions** (`server/src/session.rs`): `last_event_id_for_source: HashMap<PlayerId, u32>` for eventId monotonicity tracking + `record_fire` helper; `Room`'s outbound `tokio::sync::mpsc::Sender<Vec<u8>>` per player for fan-out (one per connected WS/WebTransport session, removed on disconnect).
>   - **Transport dispatcher swap** (`server/src/transport.rs`): the PR 11.6.C synthetic-broadcast handler in the `0x01 DamageRequest` arm is replaced with `damage_relay::validate_and_relay(req, source, &mut room, client_rtt_ms, now)`. On `Some(broadcast)`: encode + fan out to all room connections. On `None`: log `warn!` with rejection reason, no broadcast. The `0x02 DamageBroadcast` inbound arm is removed (clients never send broadcasts; receiving one is misbehavior). The `0x03 PositionUpdate` arm now also auto-registers the player in `room.players` on first packet (per-target rewind position 0 needs the player to exist). The `0x04 Ping` arm returns Pong. The fan-out is the new behavior.
>   - **Server-side tick loop** (`server/src/main.rs`): `tokio::spawn` task at 64Hz that increments `room.next_server_frame` (global counter) + trims `position_history` for players who haven't sent a `PositionUpdate` in the last ~1s (retention is already 64 frames @ 64Hz = 1s; trim is implicit on the next `record`).
>   - **16 new server tests** (`server/src/damage_relay.rs` unit + `server/tests/damage_relay.rs` integration): self-damage rejection, target-not-in-room rejection, amount-over-max rejection, stale-eventId rejection, fire-rate-violation rejection, zero-ammo rejection, valid-fire-hit-in-range, valid-fire-miss-out-of-range, lag-comp-rewinds-older-position, server-seq-increment-on-success, integration full round-trip, integration lag-comp rewinds, integration fire-rate enforced, integration two-tab convergence, integration malformed request doesn't panic, integration validator-in-room self-damage, integration validator-in-room fire-rate. **107 server tests pass** (53 unit + 25 `protocol_wire` + 16 `damage_relay` + 13 `session_canary`).
>   - **Client-side prediction map** (`client/src/net/damageBus.ts`, +~190 lines): `pendingApplies: Map<eventId, PendingOptimisticApply>` (capacity 64, oldest-evict) keyed by `eventId`. `sendDamageRequest(req, targetController, nowMs)` does TWO things: (1) encodes + sends the request to the server (existing path), (2) OPTIMISTICALLY applies `applyDamage(targetController, {source, amount}, nowMs)` LOCALLY, tagged with `req.eventId`. `applyBroadcast(bc, nowMs, resolveTarget)` implements 4 paths: (1) **confirm** — `pendingApplies.has(eventId)` + amount matches → remove from map (no-op visually); (2) **revert** — `pendingApplies.has(eventId)` + amount mismatch → REVERT via `applyDamage(target, {source: "correction", amount: -appliedAmount}, nowMs)` + emit `"no damage"` HUD tracer flash event; (3) **applied** — no pending → someone else's fire, apply directly to target via `resolveTarget`; (4) **ignored** — no pending + resolver returns null → no-op. `sendPositionUpdateThrottled` fires every other frame at 32Hz (per §3.10).
>   - **gameSession tick() swap** (`client/src/game/gameSession.ts`, +~110 lines): the 4 `applyDamage` call sites flip from local-compute to server-broadcast-driven. Local-fire paths (Tab A pulls trigger) → `damageBus.sendDamageRequest(req, targetController, nowMs)` (send + optimistic apply). Remote-fire paths (Tab B pulls trigger) → REMOVE the local apply; the broadcast handler does it. The local tab keeps the optimistic apply visible on its own screen until the broadcast confirms (the §3.9 "favor the shooter" UX). A `setServerTransport` setter on GameSession binds the transport after the scene mounts (the smoke creates the transport in the DEV probe, then late-binds it). `nextEventId` counter on GameSession (monotonic u32 per source) replaces the random IDs the smoke used previously.
>   - **`?server=` URL routing** (`client/src/ui/PeerOverlay.tsx`, +~40 lines): DEV-gated URL-param parse on mount. If `?server=ws://localhost:14434/rooms/DEVBX` (or `https://...:14433/...`) is present, instantiate `ServerGgnetTransport` instead of `P2PGgnetTransport`. Also accepts `?room=...` and `?localId=...` for the smoke's two-tab flow. Default behavior (no `?server=`) keeps P2P for the existing 14 smokes. Shows `rttMs` from `getStats()` in the HUD chip if available.
>   - **DEV probe StrictMode idempotency** (`client/src/engine/scene.ts`): the PR 11.6.C DEV probe created `ServerTransport` + `damageBus` probe + broadcast handler unconditionally on scene mount. React's `<StrictMode>` in `main.tsx` fires `createScene` twice in dev, so each tab created 2 `ServerTransport` instances + 2 WS connections → 4 WS connections total. The double handler fired twice per broadcast, but the FIRST mount's controllers were disposed by the second mount's `dispose()`, so only the second mount's handlers applied — net 1 apply per broadcast (HP converges at 88 ✅). The fix: early-return the DEV probe block if `window.__serverTransport` is already set. Verified the handler fires once per broadcast and the HP converges; the "DEBUG fan-out: 4 connections registered" log confirms the StrictMode was indeed the cause (now 2 connections per tab).
>   - **Health correction path** (`client/src/game/health.ts`): `applyDamage` accepts negative `amount` with `source: "correction"` for the revert path. HP clamps at `HEALTH.maxHp` (no negative-HP, no above-max). The 4-path `applyBroadcast` flow is now: confirm → no-op; revert → `applyDamage(target, {source: "correction", amount: -appliedAmount}, nowMs)`; applied → `applyDamage(target, {source, amount}, nowMs)` (positive).
>   - **New `client/tools/damage-server-hp-convergence-smoke.mjs` on port 5191** (7+ assertions): boots canary server + Vite on 5191, opens TWO headless browser contexts (each with `?server=ws://localhost:14434/rooms/DEVBX` + `__forceServerTransport = true`), connected to the SAME room (`DEVBX`). Asserts: (1) both `ServerTransport.connect()` resolves within 5s; (2) Tab A's optimistic apply fires on `remoteController` (HP < 100); (3) Tab B receives the broadcast with matching `originEventId` within 1s; (4) HP convergence — Tab A's remote HP matches Tab B's local HP (both tabs land on the same value, 88); (5) `getStats().rttMs < 150` on localhost; (6) fire-rate cooldown enforced — 100 `sendDamageRequest` in 1.1s results in 7 hits (120ms cooldown = ~8/sec max); (7) screenshot captured at `client/tools/damage-server-hp-convergence-smoke.png`. WebSocket fallback used (headless Chromium's QUIC stack rejects self-signed certs; the canary's WebSocket on 14434 serves the same wire protocol).
>   - **CI integration** (`.github/workflows/ci.yml`): new `client-damage-server-hp-convergence-smoke` job, mirrors the PR 11.6.C `client-damage-server-smoke` job. Boots canary on `--port-wt 14433 --port-ws 14434`, Vite on 5191, runs the smoke, uploads `client/tools/damage-server-hp-convergence-smoke.png` on failure, tears down in `if: always()`. **Per-step `working-directory: client`** (NOT job-level — the GH Actions job-level `working-directory` trap that bit PR 11.6.C; cargo resolves manifest from CWD, vite resolves smoke script paths from CWD).
>   - **Wire format UNCHANGED**: PR 11.6.C's 14-byte DamageRequest / 18-byte DamageBroadcast / 14-byte PositionUpdate / 4-byte Ping / 8-byte Pong / 17-byte InputsServer all stay. The new `validate_and_relay` is a CONTROL-PLANE change; the codecs are byte-for-byte identical. Wire-size symmetry TS ↔ Rust: `DAMAGE_REQUEST 15 (disc+body)`, `DAMAGE_BROADCAST 19`, `POSITION_UPDATE 15`, `PING 5`, `PONG 9`, `INPUTS_SERVER 17` — matches the existing PR 11.6.C smoke wire-size assertions.
>   - **Production bundle verified clean**: `grep -E '__forceServerTransport|__serverTransport|__damageBus|__pendingOptimistic|ServerGgnetTransport' client/dist/assets/index-*.js` → ZERO matches. The DEV probe is gated by `import.meta.env.DEV`; Vite strips it from the production bundle. Bundle delta: +1.36 kB raw (well under the +5 kB budget); total 7,057.36 kB.
>   - **ROUND-1 REVIEW PASS (2026-08-16, evo/claude)**: Claude Code review of the 2,889-line diff caught **15 BLOCKING + 7 NITS** bugs codex shipped alone would have missed. Nine blockers were addressed in commits `5691cb7`, `ca9f177`, `7549ae7`, `7a6dd4e`. Codex's `126 tests pass` claim was cargo tests only — there is **no JS test framework installed** in the client (no vitest, no jest), so the only client test surface is the Playwright smokes. Three more bugs caught during the round-2 verifier review are addressed in commit `948d9aa` (sweep wiring). **TWO REMAINING BUGS** are the flaky-smoke symptoms above; round-3 fix in progress.
   - **ROUND-2 FIX (2026-08-16, codex)**: `sweepExpiredPending` is now wired via a `setInterval(probe.sweepExpiredPending, 50)` in `client/src/engine/scene.ts`'s DEV probe block, cleared on dispose. Bundle grep `__pendingSweepInterval` returns ONE match (the dispose path's read of an undefined key — a no-op in production; the producer is gated by `import.meta.env.DEV` and stripped). Smoke moved from "deterministic FAIL at FIX 4 part C" to "flaky PASS/FAIL". The "codex shipped but never ran" pattern bit us again: codex's JSONL session log shows fix2 dispatched the file changes + a brief `/tmp/debug-sweep.mjs` debug-script creation before the session died on a 502 high-demand error — it never reached the verification step. The verifier re-ran all gates + the 5190/5191 smokes manually.
   - **ROUND-3 FIX (2026-08-17, codex, commit `c682a03`)**: landed bug 1 (drop-without-revert), bug 2 (recentlySettled map + applyBroadcast TTL-bounded memoization), bug 3 (vitest@^2 + 4 boundary tests). All 4 vitest tests pass. Cargo test 13/13 + typecheck + build all clean. The 5191 smoke moved from "flaky 7/10 PASS" to "deterministic-FAIL 0/10" — the failure signature changed because the recentlySettled map quarantines the late-arriving broadcasts (so HP doesn't double-charge) but the sweep still over-restores the rejected optimistic applies (Tab A's remote lands at 100, Tab B's local at 4; not a 12-HP-gap flake anymore, but a steady-state divergence). The new smoke's `__broadcastResultCounts` debug instrumentation shows: 7 broadcasts delivered × 2 listener firings = 14 handler invocations, of which 7 are `confirm` and 7 are `ignored` — meaning **the broadcast handler is registered twice per tab**, the StrictMode guard race in `scene.ts:754-756` is the bug. The smoke still fails because (a) one of the two listener registrations wins and the other is now spurious (orphan firings double-count on Tab A's broadcasts handler), AND (b) the sweep over-restores Tab A's remote. codex session ran for ~94 min and was SIGINT'd at the 90-min soft budget; all work survived on disk. Verifier independently re-ran all gates post-mortem.
   - **ROUND-4 FIX (2026-08-17, scope for next dispatch)**: target two specific remaining bugs.
     - **StrictMode guard race** in `client/src/engine/scene.ts:744-826`: set `window.__serverTransport = "INIT"` SYNCHRONOUSLY at the top of the IIFE so the second mount's synchronous check sees something before its async work starts. Also add a `__serverTransportInitInProgress` boolean guard. Or: refactor to use a single module-level `transportsByTabId: Map` + register unregister pattern.
     - **Sweep over-restoration**: the sweep currently reverts ALL entries > 500ms old, including ACCEPTED entries whose broadcasts ARE on the wire but haven't arrived yet. With recentlySettled, the late-arriving broadcast returns "ignored" — which is correct for "swept but rejected" but wrong for "swept but accepted". Fix candidates: (a) increase PENDING_REJECT_TIMEOUT_MS (e.g., to 1500ms), (b) 2-step "broadcast coming" hand-shake (adds a wire type), (c) drop optimistic-apply entirely (clean, +1 RTT per fire).
   - **OPEN QUESTION (architectural)**: if fix6 doesn't land (or lands but doesn't make the smoke deterministic), the optimistic-apply client-side prediction may not be the right call. The PR's design intent is §3.9 "favor the shooter" via optimistic apply; the implementation trades a simple "clients send + wait" for the visibility of immediate HP changes. If the trade is too expensive to debug, side-step option (C) — drop optimistic-apply entirely — and revisit after PR 11.7 (lockstep retirement) + 11.9 (matchmaker). The smoking-gun diagnostic in HANDOFF.md strongly suggests this is a real option, not a strawman.
>   - **PR 11.6.D carries to PR 11.7** (lockstep substrate retirement — the `ggrsRuntime.ts` P2P path is now demonstrably redundant; PR 11.7 deletes it): no anti-cheat (movement-rate plausibility, statistical anomaly detection) — Phase 4; no reload mechanics (ammo is fire-only, no resupply); no snapshot-generator / `inputs_buffer` consumption (PR 11.7 reads it; 11.6.D only writes); no matchmaker / multi-room (PR 11.9); no production cert handling / non-self-signed TLS (PR 11.7 absorbs the role formerly held by 11.6.E per §5 of the plan); no server-side tick rate above 64Hz (per §3.10). All explicitly out of scope for this PR.

> - **PR 11.7.B (server physics + snapshot generator — Rapier 3D + 64Hz physics tick + `Snapshot` 0x07 wire type + 20Hz broadcast + PositionHistory fed by physics tick + §3.13 coyote-time parity + §3.14 hitscan-mid-air edge case + §4.5 Havok reference capture, branch `feat/phase1-pr11.7.b-server-snapshot`)** — the FIRST code-bearing PR of the 11.7 series. Server becomes the authoritative movement engine: Rapier 3D physics steps every connected player's capsule at 64Hz, drains `Room.inputs_buffer` per tick, and feeds PositionHistory (no longer client PositionUpdate-driven). Coyote-time parity with Havok: Rapier grants a `COYOTE_FRAMES=2` grace window for jump-after-contact-loss (Havok has none; the brief's §3.13 parity fix). Hitscan-mid-air edge case: `PositionHistory::snapshot_at(t)` flipped from "largest <= target" to "snap to nearest within ±8 frames" (the brief's §3.14 fix; needed so mid-air lag-comp rewinds land within ±15ms). New `Snapshot` wire type at discriminator `0x07` (header 1+4+4+1 = 10 bytes + 29 bytes/PlayerState; on-the-wire at 24p ≈ 706 bytes; 20Hz × 706B ≈ 13.8 KB/s/server outbound per the brief's §6 Q2 bandwidth math). Concrete deliverables:
>   - **Rapier dependency** (`server/Cargo.toml`): `rapier3d = { version = "0.18", default-features = false, features = ["dim3", "f32", "enhanced-determinism"] }` (the 0.12 version specified in the brief did not compile on Rust 1.95 — E0223 errors; 0.18 is the closest compatible version with the same feature surface. `enhanced-determinism` replaces the brief's `deterministic` feature flag, which was renamed in 0.14+). All Rapier types wrapped in `PhysicsWorld` newtype so consumer surface doesn't depend on Rapier's API directly.
>   - **Server-side `PhysicsWorld`** (`server/src/physics.rs`, NEW ~580 lines): owns Rapier's pipeline (`RigidBodySet`, `ColliderSet`, `IslandManager`, `BroadPhase`, `NarrowPhase`, `CCDSolver`, `QueryPipeline`, `PhysicsPipeline` — Rapier 0.18 has no monolithic `World` struct). Per-player capsule + `KinematicCharacterController` for grounded detection (Rapier 0.18 has no `RigidBody::is_grounded()` method). Public API: `new()`, `add_player(id, start_pos)`, `step(inputs, dt)` (drains `Room.inputs_buffer`, applies movement-bit-driven planar velocity, calls coyote-time helper), `position(id)`, `velocity(id)`, `grounded(id)`, `is_mid_air(id)`. Fixed-timestep integration `dt = 1.0/64.0` (NOT variable-timestep — would break the §5.2 determinism contract + the lag-comp `snapshot_at` math).
>   - **`Snapshot` wire type** (`server/src/protocol.rs`): `DISCRIMINATOR_SNAPSHOT = 0x07` + `DISCRIMINATOR_STATE_ACK = 0x08` (declared, encoder/decoder deferred to 11.7.C — only the constants land in this PR). `PlayerState` (29 bytes: 2 + 4 + 4 + 4 + 4 + 4 + 4 + 1 + 1 + 1) + `Snapshot { server_frame, next_server_frame, players: Vec<PlayerState> }` (header is 10 bytes: 1 disc + 4 server_frame + 4 next_server_frame + 1 player_count). `encode_snapshot` (big-endian per §3.5) + `decode_snapshot` + `debug_assert_eq!(buf.len(), N)` size guard. `SNAPSHOT_WIRE_SIZE_MIN = 10`, `PLAYER_STATE_WIRE_SIZE = 29`.
>   - **Wire-format note: `DISCRIMINATOR_DAMAGE_REJECT` bumped from `0x07` → `0x0C`** (in both `server/src/protocol.rs` and `protocol/damage.ts`) to free `0x07` for `Snapshot` per the brief's reserved range (`0x07-0x0B` for 11.7 types). This is a wire-format breaking change but in-repo only (no external clients).
>   - **`PositionHistory` source flip** (`server/src/position_history.rs`): `snapshot_at(t)` flipped from "largest <= target" to "snap to nearest within ±8 frames" (the §3.14 fix; tie-break prefer `frame <= target`). New `should_store_frame(frame)` predicate: `(frame % (PHYSICS_HZ / STORE_HZ)) == 0` = `(frame % 2) == 0` = true on even frames, storing at 32Hz from a 64Hz physics tick (per the brief's design: store ~30KB/room at 24p). 6 existing unit tests preserved + 3 new tests for snap-to-nearest behavior (exact match / nearest-prefer-below / out-of-range).
>   - **Server-side tick loops** (`server/src/main.rs`): PR 11.6.D's 64Hz trim-position-history tick loop FOLDED into the new `physics_tick_loop` (don't keep two tick loops — both incremented `room.next_server_frame`). New `snapshot_generator_loop` at 20Hz (`Duration::from_millis(50)`) reads `room.next_server_frame` and emits via `broadcast_snapshot` (fan-out via the existing PR 11.6.D `Room.connections` mpsc-sender pattern).
>   - **Server-side `SnapshotGenerator`** (`server/src/snapshot.rs`, NEW ~275 lines): `maybe_emit(room, now_ms) -> Option<Snapshot>` — emits if `(now_ms - last_emit_ms) >= (1000 / SNAPSHOT_RATE_HZ)`. `server_frame = room.next_server_frame - 1` (the frame just-stepped); `next_server_frame = room.next_server_frame` (what clients should predict to next).
>   - **`Room` extensions** (`server/src/session.rs`): `physics: PhysicsWorld` (NEW) + `last_grounded_frame: HashMap<PlayerId, u64>` (NEW, §3.13 coyote-time tracking) + `drained_inputs_this_tick: HashMap<PlayerId, EncodedInput>` (scratch space) + `drain_inputs_for_tick(frame)` method (collapses `inputs_buffer` to latest input per player per tick).
>   - **Constants** (`server/src/constants.rs`): `SNAPSHOT_RATE_HZ = 20`, `RECONCILIATION_THRESHOLD_M = 0.1`, `MAX_RECONCILIATION_SNAP_DISTANCE_M = 2.0`, `INTERPOLATION_DELAY_MS = 100`, `MAX_SNAPSHOT_AGE_MS = 500`, `COYOTE_FRAMES = 2` (NEW, §3.13), `WALLRUN_COYOTE_FRAMES = 1` (NEW, §3.13), `JUMP_IMPULSE = 5.5` (NEW, §3.13; matches `client/src/game/combat.ts`), `POSITION_HISTORY_STORE_HZ = 32`. `TICK_RATE_HZ = 64` (reused from PR 11.6.B) and `POSITION_HISTORY_RETENTION_FRAMES = 64` (reused from PR 11.6.B) NOT redefined.
>   - **Transport dispatcher updates** (`server/src/transport.rs`): new inbound arm for `0x07 Snapshot` that warns + discards (clients never send Snapshot — the discriminator is registered so the inbound arm rejects cleanly with "unknown inbound discriminator" if a client mistakenly sends one). New `broadcast_snapshot(room, snap_bytes)` fan-out helper (mirrors the PR 11.6.D `broadcast_damage_broadcast` pattern; handles full/closed channels with a `warn!` log). KEPT the existing `0x03 PositionUpdate` handler with a deprecation `warn!` ("PositionUpdate is deprecated, will be removed in 11.7.D; using client-driven position for PositionHistory") — PR 11.7.B's gradual cutover per §3.6.
>   - **TS-side wire mirror** (`protocol/snapshot.ts`, NEW ~238 lines): `encodeSnapshot`/`decodeSnapshot`, `DISCRIMINATOR_SNAPSHOT = 0x07`, `DISCRIMINATOR_STATE_ACK = 0x08`, `SNAPSHOT_WIRE_SIZE_MIN = 10`, `PLAYER_STATE_WIRE_SIZE = 29`, size assertions match the Rust encoder.
>   - **12 new server tests** (`server/tests/snapshot.rs`, NEW): `snapshot_emitted_at_20hz`, `snapshot_player_count_matches_connections`, `snapshot_wire_format_roundtrip`, `snapshot_does_not_emit_before_interval`, `snapshot_emits_at_50ms_and_100ms_only`, `snapshot_interval_is_exactly_50ms`, `snapshot_cadence_is_exactly_20hz_no_drift`, `snapshot_carries_yaw_pitch_as_zero_default`, `broadcast_snapshot_writes_to_every_connection`, `broadcast_snapshot_with_no_connections_is_noop`, `should_store_frame_predicate_matches_32hz_at_64hz_physics`, `snapshot_is_deterministic_across_runs`.
>   - **5 new `session_canary` tests** (`server/tests/session_canary.rs`): `coyote_time_grants_jump_within_window`, `coyote_time_deny_after_window`, `hitscan_rewinds_through_rapier_history_mid_air`, `snapshot_includes_position_history`, `position_history_snap_to_nearest`.
>   - **2 updated `damage_relay` tests** (`server/tests/damage_relay.rs`): the existing `validates_rejects_snapshot_at_returns_none` test renamed + repurposed to `validates_rejects_when_position_history_is_empty` + `validates_uses_nearest_snapshot_within_tolerance` (the `snapshot_at` snap-to-nearest behavior change).
>   - **Havok reference capture** (`client/tools/capture-havok-reference.mjs`, NEW ~346 lines): single-tab Havok-only smoke on port 5191 that boots canary + vite, drives the Havok character controller through two scripted sequences (coyote-time ledge walk-off + jump from tall crate top at `(-5, 1.25, -2)`, mid-air hitscan straight-up jump with apex detection), and writes `client/test-data/coyote-reference.json` (12.7KB) + `client/test-data/hitscan-mid-air-reference.json` (10.7KB). Both JSONs are the pre-11.7.B Havok-only baseline — future port-5192 parity smokes (added in 11.7.C) diff against these to verify Rapier parity. The capture also serves as the §3.13 / §3.14 acceptance check: if Rapier's behavior diverges from Havok's beyond the expected coyote-time-grant direction, the diff flags it.
>   - **Test counts**: **168 tests pass** on `cargo test --release` (87 unit + 35 `damage_relay` + 16 `protocol_wire` + 18 `session_canary` + 12 `snapshot`). Up from PR 11.6.D's 126 = +42 net new (37 new tests + 5 modified). `npm run typecheck` clean. `npm run build` clean (bundle 7,058.04 kB — same hash as PR 11.6.D, delta = 0; well under the +5 kB budget). `npm run test` 10/10 vitest PASS (no new vitest in this PR — those land in 11.7.C). `grep -E 'PhysicsWorld|SnapshotGenerator|DISPATCHER_SNAPSHOT' client/dist/assets/index-*.js` → **ZERO matches** (production bundle stays clean; `protocol/snapshot.ts` is interface-only, no runtime Rapier imports leak).
>   - **Determinism check**: ran `cargo test --release` twice, sorted the `test result` lines, diffed — byte-identical. (Test-name ordering varies due to multi-threaded runner; the `test result` summary lines are deterministic.) Confirms the `enhanced-determinism` Rapier feature flag is honored + no `Instant::now()` reads inside the tick loop.
>   - **5191 smoke (the existing PR 11.6.D damage HP-convergence smoke)**: still passes the load-bearing assertion 4 — both tabs land on HP=88 after the first fire (basic damage convergence). The post-spam 12-HP gap carry-forward persists (acceptable per the brief: "the 12-HP gap closes in 11.7.B's snapshot-driven damage timing per plan §4.4" — but PR 11.7.B's `DamageBroadcast` still fires from the existing PR 11.6.D path, not from the new snapshot path; the gap closes in 11.7.C when clients consume Snapshots).
>   - **§3.13 coyote-time parity fix** (the highlight): `server/src/physics.rs::apply_jumps` helper grants JUMP_IMPULSE for up to `COYOTE_FRAMES=2` frames after the player loses grounded contact. The Havok empirical behavior (no coyote-time) is recorded in `coyote-reference.json`; the Rapier behavior should grant a HIGHER peak Y because the jump fires within the grace window. The post-11.7.B parity smoke on port 5192 (added in 11.7.C) verifies the direction.
>   - **§3.14 hitscan-mid-air edge-case fix** (the other highlight): `PositionHistory::snapshot_at(target)` now snaps to the frame whose `|frame - target| <= 8` (~125ms at 64Hz, ~250ms at 32Hz storage), well within the ±15ms spec. Without this fix, mid-air lag-comp rewinds would land on the closest below frame (which can be hundreds of ms stale at mid-air when the player is rising fast). New unit tests pin all 3 cases: exact match / nearest-prefer-below / out-of-range.
>   - **§4.5 Havok reference capture** (the third highlight): the pre-11.7.B Havok-only baseline is captured in two files. This script runs in the CURRENT Havok-only state (port 5191, no `__forceServerTransport` override — the URL routing in PeerOverlay.tsx still auto-sets the flag, but the Havok physics is local to the client regardless). The capture reuses the existing `damage-server-hp-convergence-smoke.mjs`'s TCP-only readiness check (the original `capture-havok-reference.mjs` draft used `fetch()` on the WebSocket-only port, which was buggy — fixed by mirroring the damage smoke's `isTcpReachable` helper).

> - **PR 1** (tooling baseline + CI + spec lock) — **MERGED** to main.
> - **PR 2** (Babylon scene + Havok + skydome + static mesh + static ground + Playwright headless smoke) — **MERGED** at https://github.com/klampatech/specialists-web/pull/3 (squash commit `2a12a59`), all 3 CI checks green.
> - **PR 3** (Havok character controller + WASD + stunts + chase camera + procedural character + WebGPU bootstrap) — **MERGED** at https://github.com/klampatech/specialists-web/pull/5 (squash commit `86feffa`), all 3 CI checks green.
> - **PR 4** (spec drift fix — pinned `playwright@1.62.1`, explicit Vite Havok cache rule, Milestone 1 acceptance markers) — **MERGED** at https://github.com/klampatech/specialists-web/pull/4 (squash commit `1a0a5fd4`), all 3 CI checks green. No code surface change; spec alignment only.
|> - **PR 6** (WebRTC peer bootstrap + deterministic fixed-frame lockstep + 2-character scene + two-tab handshake smoke + new `client-two-tab-smoke` CI job + spec alignment for actual shipped signaling surface) — **MERGED** at https://github.com/klampatech/specialists-web/pull/6 (squash commit `461dcaf`), all 3 CI checks green. Substrate for Milestone 2 rows 1-4. **PR 6 caveat (fixed in PR 10.1):** the original `createOffer` / `createAnswer` fire-and-forget'd the ICE gather AFTER serializing the return blob, so the blob's `candidates: []` was always empty. CI smoke passed (SDP state, not `connectionState === "connected"`) but real two-tab playtest over Tailscale failed. PR 10.1 awaits ICE gathering (5s timeout) and serializes the gathered candidates into the blob.
|> - **PR 7** (combat semantics: dual-pistol raycast firing + tracer render, melee cone hit detection, per-client bullet-time scaling at 0.25x with air control) — **MERGED** at https://github.com/klampatech/specialists-web/pull/8 (squash commit `50ee9f2`), all 6 CI checks green. Wires the byte-1 FIRE / MELEE / BULLET bits that PR 6 reserved; byte-1 encoding was previously a no-op (decode always read `false`). Damage application is render-side log only (no real health pool mutation in this PR — that's PR 10+).
|> - **PR 9** (jump regression fix: gravity accumulation in the Havok controller's `update()` + tightened jump condition `vy ≤ 0`; PLUS PR 8.1 wallrun rising-edge + post-wallrun cooldown guard; PLUS Milestone 1 row 6 scope-clarification) — **MERGED** at https://github.com/klampatech/specialists-web/pull/9 (squash commit `2ed55a8`), all 6 CI checks green. Closes the "holding Space = fly up forever" jump bug AND the "holding Q mid-air = fly up forever" wallrun auto-repeat loophole. Ships two new CI jobs (`client-jump-smoke` on port 5175, `client-wallrun-smoke` on port 5176) as regression guards.
|> - **PR 10 + PR 10.1 + PR 10.2 stacked** (PR 10: first real health pool HP=100 + damage application + 1s respawn + HUD `HP me:` / `HP them:` lines; PR 10.1: WebRTC ICE candidate bundling fix; PR 10.2: separate `respawnPosition` from `startPosition` on `CharacterController` so the cyan rig respawns to (0, 0.9, 0) instead of (2.5, 0.9, 0)) — **MERGED** at https://github.com/klampatech/specialists-web/pull/13 (squash commit `fe6ce14`). All 5 local gates green (typecheck + build + 5 smokes). **Covers Milestone 2 acceptance row 9** + fixes the PR 6 ICE-candidate regression + fixes the cyan rig respawn-position desync. No new wire byte — lockstep determinism guarantees identical damage application on both clients. Two-tab dev-box playtest 2026-08-13 confirmed cross-client HP drain + respawn sync working. Ships new `client/tools/health-regression-smoke.mjs` + new `client-health-smoke` CI job on port 5177 + new `client/tools/pr10.1-connection-test.mjs` diagnostic tool. **Honest limitations carried into Phase 1 follow-ups:** (a) ~70s frame-count desync between two tabs (no-rollback lockstep, repeated inputs fill the gap); (b) cyan rig often hidden behind crates / off-screen because the chase camera follows the local rig (this is the **correct** per-player camera behavior; a dev-box spectator camera toggle is the debug-mode fix); (c) Chrome tab throttling pauses backgrounded tabs' RAF loops (exacerbates the desync).
|> - **PR 14** (docs-only Phase 1 follow-up re-rank for the internet-multiplayer goal) — **MERGED** at https://github.com/klampatech/specialists-web/pull/14 (squash commit `6360185`). No code surface change.
|> - **PR 15** (docs-only post-merge HANDOFF entry for PR #13 + PR #14) — **MERGED** at https://github.com/klampatech/specialists-web/pull/15 (squash commit `9a50334`). No code surface change.
|> - **PR 16** (Phase 0 PR 7.4 cleanup: pure-delete removal of the PR 7.2 + PR 7.3 debug instrumentation) — **MERGED** at https://github.com/klampatech/specialists-web/pull/16 (squash commit `b1ecfb7`). All 7 CI checks green. **3 files, +15/-92** — strictly negative. Removes the `__lastMouseDown` accessor + `[input] mousedown` console.log from `client/src/engine/inputListener.ts`; the top-level `__topLevelMouseDown` useEffect + canvas-direct `__canvasDown` listener + `fireHeld`/`meleePressed` from `HudState` + `<BulletHud>` props in `client/src/ui/App.tsx`; and the dashed-border `LMB:/RMB:/T:` debug block (testids `debug-fire`/`debug-melee`/`debug-bullet`) + `fireHeld`/`meleePressed`/`bulletTime` props in `client/src/ui/BulletHud.tsx`. Bundle 7,049.30 kB → 7,047.25 kB. **No behavior change**: combat, health/damage/respawn, bullet-time chip, and chase camera are all untouched — only the debug aids are gone. The rising-edge detection in `gameSession.ts` was never touched. Dev-box playtest (Kyle, 2026-08-14) confirmed HUD renders the production state (`frame / confirmed / repeated / status / hits / HP me / HP them`) without the debug mirror and the console is quiet during normal play.
|>
|> - **PR 11.1** (per-player first-person mouse-look — pointer-locked yaw on the wire + 2-mode V-cycle + menu orbit camera + tracer-yaw fix) — **MERGED** at https://github.com/klampatech/specialists-web/pull/17 (squash commit `2fdda30`). Wire format extended from `INPUT_SIZE = 8` to `INPUT_SIZE = 10`; bytes 2-3 carry the per-frame yaw as a little-endian uint16 (~0.0055°/LSB). Both clients compute identical WASD world directions from the same yaw on the same frame — lockstep determinism preserved. Click canvas → `requestPointerLock()` → mousemove `e.movementX * sensitivityRadPerPixel` (0.0025 rad/px) accumulates into the chase camera's local yaw; ESC releases into the **menu orbit camera** (slow auto-rotation around the character at radius 4.5m, height 1.8m, 0.3 rad/sec, no mouse control — the cursor is for menus). V cycles between **1st-person-locked** (`firstPersonOffset = (0, 1.6, 0)`, camera rotation = character yaw, 1:1 with character) and **over-shoulder-locked** (`overShoulderOffset = (0, 1.7, -1.6)`, camera position tracks behind character but camera rotation looks at the character's chest — model rotates IN VIEW, not glued to screen). V is a no-op while unlocked. **Covers Milestone 2 acceptance row 10** ("Per-player mouse-look (per-player yaw, no shared chase camera)"). Three new CI smokes (mouse-look, pointer-lock-camera, yaw-wire-format) + one extended (pointer-lock-camera covers the full 9-state spec: fresh → lock → V wrap → ESC → menu orbit → V no-op → re-lock). All 8 smokes green on CI. Dev-box two-tab playtest (Kyle, 2026-08-14) confirmed: click-to-lock, mouse rotates character in 1st-person AND over-shoulder, tracer follows current yaw, menu orbit visible on ESC, V cycles correctly. Tracer yaw bug fixed (PR 7's hardcoded yaw=0 placeholder replaced with `forwardFromYaw(input.yawRadians)` helper). Menu orbit uses real `engine.getDeltaTime()` for frame-rate-independent speed (PR 11.1.4 CI fix).
|>
|> - **PR 11.2.1** (post-PR-11.2 playtest fixes — over-shoulder camera sign fix + setPointerLock through browser API + viewMode preservation retry-tested) — **MERGED** at https://github.com/klampatech/specialists-web/pull/19 (squash commit `80b2de1`). *(Historical PR-number reference: PR 11.2.1 was opened as a separate PR during the original work; the eventual squash in PR #18 folds PR 11.2 + 11.2.1 + 11.2.2 + 11.2.3 into a single commit `80b2de1` on `main`. The "MERGED at PR #19" framing above is the historical record from when PR 11.2.1 was briefly open as a standalone PR; the squash in PR #18 is the actual merge point.)* Fixes two playtest issues from PR 11.2: (1) the over-shoulder camera was positioned IN FRONT of the character (sign error in the world-offset computation: `worldOffX = -off.z * sin` produced `+1.6 * sin(yaw)` when off.z was already `-1.6`, net zero flip → camera placed at +1.6z instead of -1.6z at yaw=0). With camera in front, W (character-forward) moved the character AWAY from the camera, making the controls feel reversed (W = back, D = left) and the tracer fired out the back of the visible model. Fix: `worldOffX = off.z * sin(yaw)` (the convention is now "behind = negative Z"). (2) The Resume button + ESC-equals-resume were calling `chase.setPointerLock(true)` directly — that flipped an internal flag but did NOT engage the browser's pointer-lock, so the cursor stayed visible and the user couldn't aim after Resume. Fix: `setPointerLock(true)` now calls `canvas.requestPointerLock()` (the browser API), and `setPointerLock(false)` calls `document.exitPointerLock()`. The browser fires `pointerlockchange` either way, which the existing `onPointerLockChange` listener forwards to `chase.setPointerLock`. Single source of truth: the browser's pointer-lock state. The `onEscapePressed` hook gained a defense-in-depth `if (chase.isPointerLocked()) return;` guard so the locked→unlocked→ESC sequence doesn't re-lock immediately and skip the menu flash. **Smoke updates**: `pointer-lock-camera-smoke.mjs` had co-codified the buggy camera formula (`(-OVER_SHOULDER_OFFSET.z) * sin/cos` matched what the buggy code did) — fixed to use `OVER_SHOULDER_OFFSET.z * sin/cos`. The "yaw-glued" assertion was inverted because PR 11.2.1 puts the camera directly behind the character (by geometric necessity, camera-rotation-equals-character-yaw when the camera is on the character's facing axis). Replaced with a camera-behind-distance assertion (camera should be ~1.6m behind the character). `pause-menu-smoke.mjs` substitutes the new browser-API-driven code path with the existing DEV probe `__pointerLockToggle(true)` for headless (Chromium can't grant pointer-lock for synthetic events but the probe bypasses the browser) — the same code path still runs in real browsers. Disconnect Peer click was changed to use `dispatchEvent` of a real `mousedown→mouseup→click` triple instead of `page.click()` because Playwright's actionability check fights the menu's flex layout during fast lock-state transitions. **Behavior note**: in real browsers, `canvas.requestPointerLock()` requires a user-activation gesture (button click or ESC keydown within the same event loop turn). The Resume button onClick and the ESC keydown in `inputListener.ts` qualify — both fire while transient activation is active. Smoke verifies the chase-camera state machine; real-browser playtest confirms the actual pointer-lock UX. All 9 smokes green on CI. Bundle delta vs. main: +5.6 kB raw (chaseCamera sign flip + scene.ts setPointerLock browser-API + smoke assertions = small net).
|>
|> - **PR 11.2 series (MERGED at https://github.com/klampatech/specialists-web/pull/18, squash commit `80b2de1`)** — pause / loadout menu UI shipped across four stack commits folded into the single squash: PR 11.2 (initial UI), PR 11.2.1 (over-shoulder sign fix + setPointerLock through browser API + viewMode preservation + smoke hardening), PR 11.2.2 (single-handler ESC refactor), PR 11.2.3 (lock-then-unlock debounce + synthetic-mousemove anti auto-release + debug instrumentation). **Residual known issue**: ESC-equals-resume flicker under specific timing conditions — see "Known issues" below. Kyle's 2026-08-15 call: clicking the Resume button from the UI behaves correctly, table the residual ESC flicker for follow-up, ship the PR.
|>   **Architecture now**:
|>   - **Locked → ESC**: inputListener does nothing; browser fires `pointerlockchange(false)` natively; chase camera transitions to menu orbit; menu becomes visible. **No JS-side ESC handler in inputListener.ts.**
|>   - **Menu visible → ESC**: PauseMenu's `useEffect` keydown fires; calls `onResume()`; `handle.setPointerLock(true)`; browser fires `pointerlockchange(true)`; menu hides. Single handler, no race.
|>
|>   All 9 smokes green locally + all 11 CI checks green. **Kyle's follow-up playtest (2026-08-14 evening): same flicker observed.** The single-handler architecture is NOT the root cause (or is incomplete). PR 11.2.3 added lock-then-unlock debouncing + a synthetic-mousemove refresh to address two candidate root causes from the debug-log capture, but the actual root cause is still uncertain. **Per Kyle's 2026-08-15 call**: clicking the Resume button from the UI behaves correctly; the residual ESC-equals-resume flicker is tabled as a known issue for follow-up. See **Known issues** below.
|>
|> - **PR 11.3** (per-player mouse pitch — pointer-locked pitch on the wire + 1st-person + over-shoulder camera tilt + 2 new smokes + CI jobs) — **MERGED** at https://github.com/klampatech/specialists-web/pull/20 (squash commit `e0ce05e`, branch `feat/phase0-pr11.3-mouse-pitch`). Wire format extended from `INPUT_SIZE = 10` to `INPUT_SIZE = 12`; bytes 4-5 carry the per-frame pitch as a little-endian uint16 ([-π/2, +π/2] → [0, 65535], PITCH_BITS_SCALE = 65535 / π ≈ 20861.9, ≈ 0.00275°/LSB). Both clients compute identical 3D look directions (yaw + pitch) from the same decoded pitch on the same frame — lockstep determinism preserved. Same lockstep argument as PR 11.1's yaw. PR 7's `forwardFromYaw` becomes `forwardFromYawPitch` (3D vector with Y component for vertical aim); the chase camera's 1st-person + over-shoulder render paths set `camera.rotation.x = -pitchRadians` (Babylon Y-up convention: positive rotation.x looks DOWN, so we negate to match "look up = positive pitch"). Menu orbit camera is unaffected (no pitch tilt). PR 11.1's `MOUSE_LOOK.sensitivityRadPerPixel` = 0.0025 rad/px is reused for both `e.movementX` (yaw) and `e.movementY` (pitch). **Clamp (not wrap)** at [±π/2]: pitch is a half-circle with physical limits — looking past the limit hits a wall (every FPS behavior), not flips the view. **Backward-compat shim**: pre-PR-11.3 traffic has bytes 4-5 = 0; `decodeInput` special-cases `bits === 0 → pitchRadians = 0` (level) so the upgrade window doesn't temporarily default to "looking straight DOWN" (which the linear formula would produce). Three new CI smokes (mouse-pitch on port 5184, pitch-wire-format on port 5185, extended pointer-lock-camera with pitch assertions on port 5181) — the pitch smoke asserts the regression guard for the wrap-vs-clamp gotcha (try to push past ±π/2 → final pitch is exactly ±π/2, not a wrapped value). All 11 CI smokes green locally. Bundle delta: +2-3 kB raw / +0.5 kB gzip (wire constants + chase camera pitch state + 2 new smokes + DEV probes).

|>
|> - **PR 11.3 follow-ups (PR #21, PR #22, PR #23 — all MERGED)** — three follow-ups to PR #20:
|>   - **PR #21** (https://github.com/klampatech/specialists-web/pull/21, squash `e85b56a`): Y-axis sign fix. `client/src/engine/inputListener.ts` line 230 was `e.movementY * sens` (missing the negation — the browser reports `movementY > 0` for mouse-DOWN, but FPS convention says mouse-DOWN should look DOWN). Fixed to `-e.movementY * sens`. The chase camera's `camera.rotation.x = -pitchRadians` was already correct. **Caught by Kyle's 2026-08-15 dev-box playtest** (reported "drag mouse down makes it look up"). 1 line in 1 file + docs. All 13 CI smokes green.
|>   - **PR #22** (https://github.com/klampatech/specialists-web/pull/22, squash `91ea3d7`): cross-context pitch wire-format smoke (port 5186). Uses two SEPARATE browser contexts (each its own JS runtime + Vite module cache) to verify pitch bytes encoded in Tab A decode identically in Tab B (and vice versa). Catches per-context module drift that the single-context smoke misses. 14 CI smokes green. New CI job: `client-pitch-2tab-wire-format-smoke` on port 5186.
|>   - **PR #23** (https://github.com/klampatech/specialists-web/pull/23, squash `03dd9ba`): docs-only — flipped M2 row 10 from ⚠️ UNVERIFIED to ✅ dev-box verified on the 2026-08-15 two-tab playtest (Tab A drag-up + LMB → Tab B's HP drops when tracer lands, vertical aim direction propagates correctly through WebRTC). 1 file, +1/-1. All 14 CI smokes green.

|> - **PR 11.4 (MERGED — dev-box free-fly spectator camera, F2 detach, debug-only)** — second `UniversalCamera` instantiated lazily on first F2 press, reused across subsequent F2 toggles. F2 enters with the camera's *current world position* (no teleport); F2 again reattaches to the chase camera (lerp resumes normally). WASD flies at 5 m/s flat (matches character walkSpeed=5; frame-rate-independent via `engine.getDeltaTime()/1000` scaling in `pumpWASD`); held-right-click-drag rotates (yaw + pitch, pitch clamps ±π/2). **Character controller's `controller.update(input)` is gated off while spectator active** — WASD absorbed so the character doesn't move. **Combat bits (fire/melee) are zeroed on the wire while spectator active** (Kyle's 2026-08-15 observation: spotting a tracer from the spectated rig would leak invalid fire events to the peer). F2 handler is `preventDefault()`'d to prevent the Mac browser from grabbing F2 to open the File menu. Smoke-driven regression: new `client/tools/spectator-camera-smoke.mjs` (9 assertions on port 5187) + new `client-spectator-camera-smoke` CI job. Bundle: +2.71 kB raw / +0.5 kB gzip. **Production bundle contains zero spectator code** — every spectator path is gated by `import.meta.env.DEV` (Vite statically replaces with `false` in production, Rollup eliminates the dead branch). Verified by `grep '__spectator\|onSpectatorToggle' dist/assets/index-*.js` → ZERO matches. The 36 "spectator" matches in the production bundle are Babylon.js internals (`_spectatorCamera`, `_spectatorAfterRenderObserver` — part of Babylon 9.20's XR feature), NOT our code. The 7 "F2" matches are React scheduler lane flags + Babylon's keyboard-mapping table; not our F2 spectator toggle. **Unblocks every subsequent two-tab dev session** by giving us a way to inspect the world from outside — the cyan-rig-disappears issue was a dev-mode viewing problem, not a production camera problem.

> - **PR 11.5 (MERGED — gap-bridging rollback cap in LockstepRuntime, squash `6e064e84`, PR #27)** — reuses the documented-but-unused `MAX_PREDICTION_FRAMES = 8` (re-exported as `ROLLBACK_CAP_FRAMES`) as a load-bearing pause-when-too-far-behind cap. When `predictionDepth = max(0, localFrame - 1 - highestRemoteFrameSeen) >= ROLLBACK_CAP_FRAMES` AND `highestRemoteFrameSeen >= 0` (i.e., we've actually heard from a peer), `LockstepRuntime.advanceFrame()` returns a sentinel `{paused: true, ...}` frame instead of advancing — both controllers + combat + bullet-time skip on that tick. Wire encode + submit still fires every tick so the peer can catch up. The solo-browser gate is load-bearing (a solo run has `highestRemoteFrameSeen = -1` and the cap must be a no-op) — see the decisions log for the bug this caught (deterministic 5-of-16 CI regression on the initial push). `gameSession.tick()` adds the caller-side early-return BEFORE controller update + combat + bullet-time; rising-edge trackers (`wasFiring`/`wasMelee`) NOT updated on a paused tick (lockstep-guaranteed). Dev-box playtest ⚠️ **DEFERRED to PR 11.6** (the verification environment for meaningful WAN-throttle testing requires real peer conversation + visible character animation state, neither available today). Files: `client/src/net/ggrsRuntime.ts` (~165 lines added), `client/src/game/gameSession.ts` (~88 lines: `makeEmptyInputState` + `makeEmptyFrame` helpers + the early-return + pausedFrames/totalPausedFrameCount getters on GameSession), `client/src/engine/scene.ts` (24-line DEV-only `__lockstepProbe` accessor), `client/tools/lockstep-rollback-smoke.mjs` (NEW smoke on port 5188, 7 assertions). New CI job `client-lockstep-rollback-smoke`. Bundle: +1.06 kB raw; production bundle contains zero `__lockstep` / `ROLLBACK_CAP_FRAMES` matches (the probe + the alias name are tree-shaken by Vite). Wire format **UNCHANGED** — input packet stays PR 11.3's 12 bytes; the cap is purely local. **🎉 PHASE 0 CLEANUP COMPLETE — ready for PR 11.6 (server-authoritative damage, Phase 1).**

|> **Next** (post-M2, ordered by Kyle's priority + dependencies):

**Phase 1 work — server-authoritative networking starts here.**

> **(1) Server-authoritative damage (PR 11.6, Phase 1)** — current setup derives damage locally from lockstep — fine for LAN / Tailscale but doesn't survive 100ms+ WAN latency. **Phase 1 work**, not Phase 0. Move damage application from `gameSession.tick` (per-client local) to a server-broadcast packet handler. This is the first step toward a real dedicated server (Tokio + WebTransport / WebSocket fallback). **Carries PR 11.5's deferred verification debt**: once the server is wired up, multi-tab WAN-throttle becomes the natural test environment for BOTH PR 11.6 (server-auth damage) and PR 11.5 (cap behavior). Add PR 11.5's 6-step checklist (in HANDOFF.md → "Verification debt carried to PR 11.6") to PR 11.6's verification checklist.

>   **2026-08-15 — PR 11.6 architecture plan drafted.** Full architecture decision record in `docs/PR-11.6-plan.md`. 5-sub-PR roll-out sequence: **11.6.A** (this plan PR) → **11.6.B** (Tokio + WebSocket server scaffold + canary launch) → **11.6.C** (wire protocol + transport mux) → **11.6.D** (server-auth damage end-to-end + smoke) → **11.6.E** (WebTransport layer + production cert handling). Total ~4-6 sessions. Open questions for review: WebSocket-vs-WebTransport-first, hard-coded "DEVBX" room vs matchmaker, server-frame clock source, bidirectional eventId dedupe, `paused`-tick input-loss carry-over.

> **(2) HUD paused-frames chip (PR 11.X, candidate follow-up to PR 11.5)** — `LockstepRuntime` exposes `pausedFrames` + `totalPausedFrameCount`; wire a small HUD chip in `App.tsx` + `BulletHud.tsx` to surface them. ~30 lines + a 1-assertion smoke. **Worth doing before or alongside PR 11.6** — the `frame` HUD by itself doesn't show *when* the cap fires, only its cumulative drift. The paused chip gives PR 11.5's verification a visible signal beyond the silent early-return path.

> **(3) Mixamo glTF character** — replaces the procedural humanoid rig with an actual animated character. **Re-prioritized as a verification-environment upgrade**: the procedural rigs without walk cycles + body tilt were the reason PR 11.5's playtest couldn't visually distinguish "frozen character" from "nothing happening." Same obstacle will hit every future PR that depends on visible game-state observation. Should be tackled before PR 11.6 (or its deferred test), not as a cosmetic last step.

> **(4) Original PR 11 polish (deferred)** — real wall-detection via `PhysicsRaycast`, kill-marker, hit-marker, death animation. Cosmetic; unchanged priority.

> **Recommended next session order**: **(1)** PR 11.6 server-auth damage (carries PR 11.5's verification debt) → **(2)** PR 11.X HUD chip or Mixamo glTF (or both, parallelizable) → **(3)** polish. Phase 0 is closed; the cap is shipped; what remains is real internet-multiplayer architecture.

> **Dev-box two-tab play visual context** (Kyle, 2026-08-14 → 2026-08-15): pointer-locked first-person works on both tabs. Over-shoulder rotates the model correctly. Menu orbit visible on ESC. Cross-client yaw + pitch propagation confirmed (Tab A mouse → Tab B sees A rotate; pitch via PR 11.3). The spectator camera (PR 11.4, shipped) unblocked inspecting the world from outside. The remaining dev-box annoyance is **visible character state** — procedural rigs don't animate, so "character is responding to input" and "character is paused" look identical. The Mixamo glTF swap (item 3 above) is the long-term fix; PR 11.5's verification debt (in HANDOFF.md) carries to PR 11.6's multi-tab verification environment, where animation matters less because the `frame` HUD + paused chip are observable signals.

> **Spec drift caught and fixed across PR 2:** pinned versions, WebGL2-vs-WebGPU decision, the 3-PR Phase 0 split, CI evolution, Vite `optimizeDeps.exclude` gotcha, and the milestone acceptance markings. See Decisions log + Session log.
## Operating Principles

### Playtest everything. Handing Kyle a broken game is unacceptable.

This is the single highest-priority discipline for this project. Every milestone, every feature, every Friday — there must be a real, playable thing in Kyle's hands.

**What this means in practice:**

- **Every milestone ends with a playable build.** Not "the code compiles." Not "the tests pass." A person can run it and experience the thing.
- **Every feature must be testable in isolation.** Don't bury a feature behind five other features. Build it, expose it, run it, see it.
- **"Done" means tested by a human, not just compiles.** A feature that compiles but doesn't work is not done. A feature that works on the developer's machine but not Kyle's is not done.
- **Broken/missing functionality = blocker, not "polish."** If something is broken in a build, it blocks the milestone. We don't push broken to "later."
- **Show, don't tell.** Rather than say "the character controller works," record a video, write a test report, or share a URL Kyle can hit. A description is not evidence.
- **Surface regressions explicitly.** If something that worked before stopped working, that's a regression. Surface it immediately, don't fast-path past it.
- **If you can't playtest it, you can't ship it.** If a build is unbuildable, unrunnable, or unverifiable, stop and fix the build before continuing.

**How this shows up in this spec:**
- Phase milestones have a "Definition of done" — each must include a playable acceptance test.
- HANDOFF.md sessions end with a "Playtest status" check-in.
- Decisions in the decision log reference what was tested, not just what was decided.

---

# Specialists Web

A browser-native, multiplayer-first remake of *The Specialists* (2002 Half-Life mod).
The vibe: **John Woo × Matrix × Hong Kong Blood Opera** — a spectacle shooter where movement is the game, not a side feature.

> **Working title: "Specialists Web"**. Final name TBD at public launch.

---

## Why this game

The Specialists was the first multiplayer game ever with bullet time. The feel came from:
- Player-triggered slow-mo with full air control — you could curve a shot mid-dive
- Stunt system: dive, slide, roll, flip, wallrun
- Kung fu / hand-to-hand combat with melee weapons
- Dual-wield + third-person camera toggle mid-fight
- 30+ customizable firearms
- Gamemodes: DM, TDM, **The One** (king-of-the-hill with superpowers), LMS, CTB

The reason people came back: **moments**. A slow-mo dual-pistol dive into a kung-fu combo is the marketing. Every mechanic should ladder to that.

---

## The stack (frozen for Phase 0)

**Client** (in-browser)
- **TypeScript** + **Vite** + **React** (UI shell) — pinned in [client/package.json](client/package.json)
- **Babylon.js** for rendering — **WebGPU attempted in PR 3 with a verified WebGL2 fallback** (see Decisions log — 2026-08-11 PR 3). Pinned `@babylonjs/core@9.20.0`.
- **Havok** physics via wasm (character controller, world physics) — pinned `@babylonjs/havok@1.3.14`
- **ggrs** (Rust GGPO-style rollback netcode, talks to TS via wasm) — PR 3+ (netcode work)
- **WebTransport** (UDP over HTTP/3) for game traffic; **WebSocket** fallback for restricted networks
- **Playwright** (devDep) for headless smoke testing — pinned `playwright@1.62.1` as of PR 2

**Server** (Rust)
- **Tokio** + **Rapier** (deterministic mode) for the game server
- **WebTransport** server (currently `wtransport` crate)
- **Postgres** for accounts/state, **Redis** for ephemeral session state
- **Hetzner** hosting (already have muscle from world-factory)

**Why this stack:**
- Babylon.js is the strongest rendering story for browser FPS in 2026 — better PBR, better batching, first-class Havok integration, ships with a real game engine (loaders, GUI, animation state machine), not just a renderer
- Havok is the physics engine behind Half-Life 2, Halo, etc. — recently ported to wasm, free for web. Best off-the-shelf character controller for browser games
- ggrs/snapshot interpolation is the only sane way to do per-player bullet-time — each client runs the full sim at full speed and renders at variable speed. Server is just a relay + arbiter
- TypeScript everywhere — matches existing infra, no context-switch

**Rejected alternatives:**
- *Unity WebGPU export* — 50-100MB payload, slow cold start. Defeats "go to a URL, play in 5 seconds"
- *Three.js raw* — too much hand-rolled for an FPS (movement controller, animation blending, hit detection, bullet physics all from scratch)
- *Bevy → wasm* — matches world-factory stack but smaller community, fewer browser FPS examples, would burn weeks on tooling

---

## The phased MVP plan

Six phases. Each is a shippable thing. Don't build N+1 until N is solid.

### **Phase 0 — The feel test** (~2 weeks)
**Goal**: prove the hard parts (movement + bullet time + netcode) feel right before sinking months into matchmaking infra.

- One map (corridor + rooftop, hand-built in Blender)
- Two browser tabs, peer-to-peer via WebRTC (no dedicated server)
- Character controller: run, jump, dive, slide, wallrun, third-person toggle
- Bullet time with air control
- One gun (dual pistols), melee, no HUD complexity
- ggrs rollback netcode between two tabs
- Basic Babylon scene: skydome, lighting, two test character models (Mixamo)

**Out of scope**: dedicated server, accounts, matchmaking, multiple game modes, asset pipeline polish.

**Definition of done**: a person who plays it goes "this feels like The Specialists." Demonstrated via a playable build Kyle can hit (URL or local run) within the 2-week window.

### **Phase 1 — The real server** (~3 weeks)
- Dedicated Rust game server (Rapier deterministic)
- Matchmaker-free direct-connect (share a URL → join a friend)
- 2v2 / 4-player free-for-all
- 5 weapons, 2 maps
- Health, ammo, basic HUD
- WebSocket fallback for WebTransport-restricted networks

**Out of scope**: matchmaking, accounts, leaderboards.

### **Phase 2 — Lobbies & accounts** (~2 weeks)
- Discord OAuth (zero-friction)
- Web lobby: create room, join room, ready-up
- Simple MMR-based matchmaking (Glicko-2)
- Server browser by region
- Game mode selector: DM, TDM, The One

**Out of scope**: anti-cheat, voice chat, replays.

### **Phase 3 — Performance & content** (~3 weeks)
- 2 more maps, 5 more weapons, kung-fu combo system
- Asset pipeline: Draco + KTX2 + meshopt compression, CDN delivery
- Performance pass: BatchedMesh, instance rendering, draw-call budget
- Spectator mode, killcam, basic replay system
- Load test: 100 concurrent matches, profile hotspots

**Out of scope**: anti-cheat, voice chat, mobile.

### **Phase 4 — Public beta** (~3 weeks)
- Server-authoritative cheat detection (impossible movement, statistical anomalies)
- Report system, replay-based triage
- WebRTC voice chat (Agora or Daily)
- Anti-DoS on the matchmaker
- Soft launch: invite-only, then progressive rollout

**Out of scope**: mobile, ranked leagues, tournaments.

### **Phase 5 — Post-launch** (ongoing)
- Native level editor (in-browser)
- Mod-support / user-created content
- Workshop for community maps
- Tournaments with prizes
- Mobile companion app (stats, spectate, not play)

---

## Phase 0 — detailed plan

### Repo structure

```
~/Development/specialists-web/
├── client/                  # TypeScript + Vite + React + Babylon + Havok
│   ├── src/
│   │   ├── engine/          # Babylon scene, Havok integration, render loop
│   │   ├── game/            # Game logic, character controller, weapons
│   │   ├── net/             # ggrs integration, WebRTC + WebTransport adapters
│   │   ├── ui/              # React HUD, menus
│   │   └── main.tsx
│   ├── public/              # Static assets (mixamo models, kenney assets)
│   ├── package.json
│   ├── tsconfig.json
│   └── vite.config.ts
├── server/                  # Rust (later — Phase 1)
│   └── (placeholder)
├── protocol/                # Shared types (later — Phase 1)
│   └── (placeholder)
├── tools/                   # Build / asset pipeline scripts
│   └── (placeholder)
├── .github/
│   └── workflows/           # CI
├── README.md
├── LICENSE                  # TBD at public launch
└── .gitignore
```

**Phase 0 only the `client/` side is real.** The rest is empty placeholders so the structure is obvious.

### Asset strategy for Phase 0

**Goal**: highest quality assets without human intervention.

- **Characters**: Mixamo (acrobat + handgun kits) — free, animatable, web-friendly
- **Map textures**: Kenney CC0 kit
- **Map geometry**: hand-built in Blender (one map: corridor + rooftop)
- **Audio**: freesound CC0 sets (gunshots, impacts, footsteps)
- **HUD/icons**: Kenney + game-icons.net

### CI

Three GitHub Actions jobs on every PR (`.github/workflows/ci.yml`):
- **`client-typecheck`** — `npm run typecheck` + `npm run build` (production bundle). Catches type and bundling errors.
- **`client-scene-smoke`** — boots `npm run dev`, runs Playwright headless Chromium against `http://localhost:5173`, captures a 1280x720 screenshot, fails on any pageerror. Uploads the screenshot as the `scene-screenshot` artifact on the PR. Lands in PR 2.
- **`spec-canonical`** — positive-claim assertion that `docs/SPEC.md` exists, declares itself canonical, and that the repo-root `SPEC.md` is a stub pointer. Catches accidental dual-canonical drift.

Deploy preview: Vercel or CloudFront (auto on PR) — not yet wired; deferred to Phase 1 when there's a real backend to deploy with.

### Phase 0 milestones

| Week | Milestone | Playtest acceptance |
|------|-----------|---------------------|
| 1 | **Lit 3D scene + character controller + stunts + camera toggle** (split into 3 PRs — see Decisions log) | Kyle can open a URL in a browser and walk a character around an empty map. Movement must feel right (run, jump, dive, slide, wallrun, third-person toggle). |
| 2 | ggrs integrated, two tabs can roll back, single weapon + melee, bullet time, third-person toggle | Kyle can open two browser tabs, see the other player, dive/slide/wallrun, fire a gun, hit with melee, trigger bullet time with mid-air shots, and feel the rollback netcode is correct (no teleport, no desync). |

**Phase 0 PR split (4 PRs, in order):**
- **PR 1 (DONE):** tooling baseline + CI + spec lock. No scene.
- **PR 2 (DONE, merged to main at `2a12a59`):** Babylon scene + Havok plugin + skydome + lights + one static mesh + static ground + Playwright headless smoke. **Covers Milestone 1 acceptance rows 1-3** (boots, shows lit scene, shows one object).
- **PR 3 (MERGED at `86feffa`):** Havok `PhysicsCharacterController` + procedural humanoid character + WASD + jump + dive + slide + wallrun + chase camera (V-toggle third/first person) + WebGPU bootstrap with WebGL2 fallback. **Covers Milestone 1 acceptance rows 3-10.** PR 3 also completes row 3 (real Mixamo glTF deferred to Phase 1; procedural rig is the documented placeholder).
- **PR 4 (MERGED at `1a0a5fd4`):** spec drift fix — completed the 5 SPEC.md sections that the PR 2 squash merge had silently dropped (pinned versions, WebGL2-vs-WebGPU decision, CI jobs, PR-split table, Milestone 1 acceptance markers). No code surface change. **Required to make the spec actually reflect the merged code.**
- **PR 6 (READY for review):** WebRTC peer bootstrap + deterministic fixed-frame lockstep + 2-character scene + two-tab handshake smoke + new `client-two-tab-smoke` CI job. **Covers Milestone 2 acceptance rows 1-4.** PR 6 also includes the spec alignment for the actual shipped signaling surface (this exact section).
- **PR 7 (READY for review):** combat semantics: dual-pistol raycast + tracer render, melee cone hit detection, per-client bullet-time scaling at 0.25x with air control. **Covers Milestone 2 acceptance rows 5-8.** Damage is render-side log only (no health pool — PR 9+).
- **PR 8 (READY for review):** jump regression fix (gravity accumulation + tightened jump condition) + new `client-jump-smoke` CI job on port 5175 + spec alignment for the fix. **Closes Milestone 1 row 5.** Failing-test-first reproduction: `client/tools/jump-regression-smoke.mjs`.
- **PR 8.1 (READY for review):** wallrun rising-edge + post-wallrun cooldown guard. **Closes Milestone 1 row 6** (Q-wallrun stunt, which PR 3 landed but regressed in real-browser auto-repeat playtest). Ships `client/tools/wallrun-regression-smoke.mjs` and new `client-wallrun-smoke` CI job on port 5176. Two coordinated fixes: rising-edge detection (`wasWallrunPressedLast` field) + cooldown (`lastWallrunEndedAtMs + durationMs + 200ms` grace).
- **PR 11.1 (MERGED 2026-08-14):** per-player first-person mouse-look (pointer-locked yaw, click-to-lock, ESC-to-release, mouse-delta → yaw + 2-mode V-cycle + menu orbit camera + tracer-yaw fix). **Covers the new Milestone 2 acceptance row 10** ("Per-player first-person mouse-look"). The chase camera is the fallback when pointer-lock is not granted (V-toggle still works in the fallback path: first-person-chase vs third-person-chase). Yaw lives on bytes 2-3 of the input packet (`INPUT_SIZE` bumped from 8 to 10), little-endian uint16 (~0.0055°/LSB); both clients compute identical WASD world directions from the same decoded yaw on the same frame (the controller's `update()` calls `setYaw(input.yawRadians)` BEFORE projecting the character-relative WASD input). New `client/tools/mouse-look-smoke.mjs` + `pointer-lock-camera-smoke.mjs` + `yaw-wire-format-smoke.mjs` + 3 new CI jobs. Sensitivity = 0.0025 rad/px (configurable via `MOUSE_LOOK.sensitivityRadPerPixel` in `characterConfig.ts`). Tracer yaw bug fixed (PR 7's hardcoded yaw=0 placeholder → `forwardFromYaw(input.yawRadians)` helper, frame-N zero lag). **Caveats caught by Evo's manual takeover after codex 0.137 `apply_patch` tool failure:** codex kept retrying `apply_patch` with literal `\\n` escape sequences and `apply_patch verification failed: invalid patch: The first line of the patch must be '*** Begin Patch'`, burning 2.5M+ tokens without making progress; killed + did the work manually with the Hermes `patch` tool.
- **PR 11.2 (MERGED 2026-08-14):** pause / loadout menu UI. New `PauseMenu.tsx` React component renders on `(!isPointerLocked) && everLocked` (mirrors `chase.isMenuOrbit()` exactly); cursor returns to default; semi-transparent dark overlay covers the canvas; 4 buttons (Resume / Loadout / Settings / Disconnect Peer). **Resume** calls `chase.setPointerLock(true)` and re-locks the cursor; restores the user's last `viewMode` (`lastLockedViewMode` field on the chase camera — PR 11.1's `setPointerLock(true)` always forced mode 0, which would have lost the user's over-shoulder preference across every pause cycle). **ESC-equals-resume** (Kyle's spec): `inputListener.ts` adds an `onEscapePressed` hook that calls `chase.setPointerLock(true)`; the PauseMenu component ALSO attaches a document-level `keydown` listener as a belt-and-suspenders fallback. Matches every FPS (CS, Half-Life, Minecraft). **Loadout + Settings** are `<button disabled>` placeholders with "coming soon" subtitles. **Disconnect Peer** calls `peer.close()`; PeerOverlay's existing host/join UI handles re-connection. **Escape-from-clickable-UI**: clicking the canvas (not on a menu button) while unlocked would normally re-lock via `requestPointerLock()`; PR 11.2 doesn't change this — it remains the alternative way to dismiss the menu. New `client/tools/pause-menu-smoke.mjs` with 12 assertions including explicit computed-style `pointer-events: auto` checks on backdrop + buttons (the HUD-overlay-eats-clicks trap from PR 7 burn). New `client-pause-menu-smoke` CI job on port 5183. Also drops the stray `[input] window mousedown (capture path)` console.log from PR 7 that PR 7.4 cleanup missed. Bundle 7,048.62 kB → 7,052.19 kB (+3.57 kB raw / ~+0.5 kB gzip). All 9 smokes green on CI. **Caveats caught during integration testing**: (a) initial design had a `pauseMenuOpen` flag in `scene.ts` gating the ESC handler; removed because it's redundant — `chase.setPointerLock(true)` is a no-op when already locked, so an unconditional ESC handler is safer than a gated one; (b) the cursor-on-backdrop had to be `default` not `none` for the visible-button affordance.
- **PR 11.2.1 (MERGED 2026-08-14):** two playtest fixes. (a) **Over-shoulder camera sign error** in `chaseCamera.ts`: the world-offset computation `worldOffX = -off.z * sin(charYaw)` produced `+1.6 * sin(yaw)` when `off.z = -1.6` (the over-shoulder offset's `-distance` convention) — a no-op flip that placed the camera **in front** of the character instead of behind. Kyle's playtest: "in over-shoulder view, stuff seemed reversed — tracer shot out of the back, W went backwards, D went left." Fix: `worldOffX = off.z * sin(charYaw)` (and same for z), which puts the camera 1.6m behind the character in the character's facing direction. (b) **`setPointerLock` through browser API** in `scene.ts`: previously `handle.setPointerLock(true)` just flipped the chase camera's internal `pointerLocked` flag — that updated the render state but did NOT engage the browser's actual pointer-lock, so the cursor stayed visible and the user couldn't aim after Resume. Kyle's playtest: "ESC while pointer unlocked… does not lock the pointer again." Fix: `setPointerLock(true)` now calls `canvas.requestPointerLock()` and `setPointerLock(false)` calls `document.exitPointerLock()`; the browser fires `pointerlockchange` either way, the existing `onPointerLockChange` listener forwards to `chase.setPointerLock`. Single source of truth: the browser's pointer-lock state. The ESC handler also gained a defense-in-depth `if (chase.isPointerLocked()) return;` guard to skip the menu flash. `pointer-lock-camera-smoke.mjs` updated to use the corrected camera formula and replaced the inverted "yaw-glued" assertion with a camera-behind-distance assertion. `pause-menu-smoke.mjs` substitutes the DEV probe `__pointerLockToggle` for headless click simulation (Chromium can't grant pointer-lock for synthetic events but the probe bypasses the browser entirely) and switches Disconnect Peer to `dispatchEvent` of a real `mousedown→mouseup→click` triple. **Caveats caught during integration testing**: (a) the smoke's `pointer-lock-camera` had co-codified the buggy camera formula with `(-OVER_SHOULDER_OFFSET.z) * sin/cos` — fixing the chase-camera sign exposed this co-codification and forced the smoke to also use the correct formula; (b) `requestPointerLock()` is wrapped in try-catch because some browsers throw on `document.exitPointerLock()` when not in pointer-lock.


#### Build & dev prerequisites (recover these BEFORE you start coding)

- **`npm install` from `client/`** — pulls `@babylonjs/core`, `@babylonjs/havok`, `playwright` (devDep). No system packages required.
- **`vite.config.ts` requires `optimizeDeps.exclude: ["@babylonjs/havok"]`** — without it, Vite's pre-bundling rewrites Havok's `import.meta.url` to a hashed path the wasm fetch can't follow, and the browser fetches the HTML fallback with a "expected magic word 00 61 73 6d, found 3c 21 64 6f" error. If you see this error, you forgot the exclude (or the `.vite` cache is stale). Fix: `rm -rf client/node_modules/.vite` and reload. Full diagnosis in the `babylonjs-vite-havok-wasm` skill.
- **If you change the Vite config, clear the cache.** Same path as above.
- **Headless smoke test:** `cd client && node ./tools/scene-smoke.mjs` requires the dev server already running on `:5173`. CI does this automatically.

### Phase 0 — architecture details

#### ggrs ↔ Babylon ↔ Havok wiring (the hard part)

The non-obvious thing: **Babylon and Havok both simulate physics.** We need one canonical physics authority per client, and that's Havok. Babylon reads Havok's transforms for mesh sync; it does **not** run its own physics for gameplay.

```
┌─────────────────────────────────────────────────────────────┐
│                          CLIENT                              │
│                                                             │
│  Input ──► ggrs.GGRSession (collects inputs, drives ticks)  │
│                │                                            │
│                ▼                                            │
│         Game.tick(gs, inputs)                               │
│            │       │                                        │
│            │       └──► Havok.characterController.move()    │
│            │             │                                  │
│            │             ▼                                  │
│            │       Havok.physicsWorld.step()                │
│            │             │                                  │
│            │             ▼                                  │
│            └──► Mesh transforms = Havok transforms          │
│                                                             │
│  Babylon.scene.onBeforeRenderObservable ──► render meshes   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**Tick loop** (every frame, ggrs advances and we render):
1. ggrs reports which inputs we have for frame N (locally captured, remote via rollback).
2. Game.applyInputs(gs, inputs) — converts inputs to intent (move, jump, fire, dive).
3. Havok controller applies intent → physics step.
4. Babylon meshes read rig.position / rotation from a thin "Player" object that wraps Havok's transform.
5. Render.

**Why this order**: physics is the source of truth, not Babylon. If we ever need a server (Phase 1), the server runs the same physics step with the same inputs — full determinism. Animation, FX, camera all **render only**. They never feed back into the sim.

**Havok capsule behavior**: we use Babylon's `PhysicsCharacterController` (Havok-backed). Default settings: 0.5m radius, 1.8m height, slope 45°, step 0.3m. **Tune anything in `client/src/engine/characterConfig.ts`** — never hard-code in the controller.

**Determinism rule**: every physics-affecting input is a frame-aligned integer (button bitmask + normalized analog). Never read `Date.now()` or `performance.now()` inside the tick.

#### WebRTC peer bootstrap (Phase 0 signalling)

Two browser tabs need to find each other. Phase 0 has no server, so the bootstrap is manual — share a blob, paste the offer, paste the answer. This is intentional: we don't want to spend engineering time on a signalling server until we've proved the thing is fun.

**Flow** (host = tab that opened the room; guest = tab that pasted the offer):

1. Host clicks **"Create Room"** → `WebRTCPeer.createOffer()` generates an SDP offer. ICE gathering fires in the background (not awaited) so the blob is available immediately.
2. Host displays a copy-pasteable blob (base64 of `JSON.stringify({ type, sdp, candidates })`) — the offer blob is in a readOnly textarea with `data-testid="offer-blob"`.
3. Guest pastes the host's offer blob into the textarea ( `data-testid="paste-area"` ) and clicks **"Join"** → `WebRTCPeer.createAnswer(offer)` sets remote description, generates an SDP answer, displays it back as a copy-pasteable blob.
4. Host pastes the guest's answer blob into the same textarea and clicks **"Paste Answer"** ( `data-testid="btn-paste-answer"` ) → `WebRTCPeer.acceptAnswer(answer)` sets remote description and adds any bundled ICE candidates.
5. WebRTC `RTCPeerConnection` opens two `RTCDataChannel`s: `inputs` (reliable ordered, for game inputs) and `state` (unreliable unordered with `maxRetransmits: 0`, for time-sensitive state).
6. Both tabs connect to ggrs → ggrs drives the rollback loop.

**Why no signalling server in Phase 0**: we want to prove the *feel* before investing in matchmaking infra. The copy-paste dance is annoying but it lets us land Phase 0 in 2 weeks instead of 4. Phase 1 replaces this with a Rust WebTransport server + a `/create` + `/join` REST endpoint.

**Note on the dropped `?join=<blob>` URL**: the original spec mentioned a URL like `?join=<blob>` that the guest could click to auto-join. The shipped PR 6 does **not** implement that URL handler — the manual paste flow is the only shipped path. URL-mode signaling is deferred to Phase 1 (when the REST server is in place and rewriting the URL is a one-liner). The smoke test uses a separate `window.__join()` helper exposed only when the smoke is running, not URL parsing.

**Implementation sketch** (`client/src/net/peer.ts`):
- One `WebRTCPeer` class wraps `RTCPeerConnection` + ICE handling.
- One `ClipboardPayload` type for the offer/answer blob (`{ type: "offer" | "answer", sdp: RTCSessionDescriptionInit, candidates: RTCIceCandidateInit[] }`).
- React `PeerOverlay` UI exposes the buttons + textareas. Lives in `client/src/ui/PeerOverlay.tsx`.
- Peer is **owned by `App.tsx` and passed as a prop** to `PeerOverlay`. The peer's identity is module-singleton-bound (via `useRef`) so React StrictMode's effect double-invocation doesn't close-and-replace the peer between the host and guest mounting.

**TURN server config** (current best-effort):
- ICE servers: `turn:openrelay.metered.ca:80` + `:443` (username `openrelayproject`, credential `openrelay`) and `stun:stun.l.google.com:19302`.
- `iceTransportPolicy: "relay"` when `?turn=force` is in the URL OR `navigator.userAgent` matches `HeadlessChrome*`. This forces TURN in environments where STUN is blocked (CI sandbox, some corporate networks).
- **Phase 1 plan**: replace openrelay with our own coturn on Hetzner (already in the Phase 1 server stack). Don't depend on openrelay for production.

**Headless smoke surface (the contract CI relies on — do NOT break without updating the smoke)**:
- `data-testid="peer-overlay"` — root container
- `data-testid="status"` — connection state text, "Connected" | "Disconnected" | "Waiting for room" | "Waiting for connection…" | etc.
- `data-testid="btn-create"` — host button
- `data-testid="btn-join"` — guest button
- `data-testid="paste-area"` — textarea for raw blob paste
- `data-testid="btn-paste-answer"` — host's "I pasted the answer" button
- `data-testid="offer-blob"` / `data-testid="answer-blob"` — readOnly textarea holding the encoded blob
- `data-testid="bullet-hud"` — the bottom-left HUD chip with `frame: N confirmed: N repeated: N` so the smoke can assert simulation is running
- `window.__peer` — the actual `WebRTCPeer` instance exposed in `App.tsx`'s mount effect, used by the smoke to read `connection.localDescription` / `connection.remoteDescription` for green-state assertions

**Smoke acceptance (what "green" means)**:
- Both tabs have non-null `localDescription` AND `remoteDescription` → SDP handshake completed
- Both tabs render `frame: N` with `N >= 5` after the host presses `W` for 1 second → simulation ticking
- Both tabs finish in under 60 seconds
- **What "green" does NOT prove**:
  - `connectionState === "connected"` — the CI runner and this laptop cannot reach `openrelay.metered.ca`, so ICE stays in `new`/`checking`/`failed`. The handshake correctness is verified via SDP state, not full ICE connectivity.
  - Real-time input mirroring — this is verified by Phase 1 manual playtest and the Phase 1 PR's smoke job.
  - For a real "Connected" status, you must run on a machine with line-of-sight to the TURN server (your dev box, not a sandbox).

**Pitfall to avoid in the code**: don't try to use WebRTC's auto-signalling (it doesn't exist — WebRTC always needs an out-of-band channel). The copy-paste is that channel. **Pitfall to avoid in the smoke**: don't rely on `connectionState === "connected"` for green assertions — use SDP state instead. ICE/TURN can be blocked in any sandbox environment.

#### Asset import pipeline

Phase 0 doesn't need a real pipeline — we drop pre-baked assets in `client/public/` and vitedev-serve them. The build-time pipeline is a Phase 3 concern. Here's what's in place for Phase 0:

**Source-of-truth files** (manual drops into `client/public/`):
- `models/` — Mixamo FBX exports, decompressed (no FBX in browser — convert to glTF first via `npx fbx2gltf`)
- `textures/` — Kenney CC0 PNGs, no compression (Phase 0 size budget is loose)
- `maps/` — Blender `.glb` exports of the test map (corridor + rooftop)
- `audio/` — freesound CC0 WAVs → convert to OGG with `ffmpeg` on import

**Loading pattern** (`client/src/engine/loader.ts`):
- `async loadCharacter(name: string): Promise<TransformNode>` — caches by URL, returns a Babylon `TransformNode` with the mesh + animation groups attached
- `async loadMap(name: string): Promise<Scene>` — builds a Babylon `Scene` from the `.glb`, returns it ready to attach to the engine
- `async loadAudio(name: string): Promise<Sound>` — Babylon sound handle

**Phase 0 budget**: client's initial JS+wasm payload should be under 5MB; assets are loaded lazily. Verify with `vite build --mode production && du -sh client/dist/assets/`.

**Phase 3 upgrade path** (placeholder, **don't build yet**): meshopt + KTX2 + Draco compression, CDN delivery, BatchedMesh for environment props. Architecture leaves room (the loader is a wrapper, not a Babylon singleton) but the optimization itself is out of scope.

### Phase 0 — milestone acceptance criteria (expanded)

The Phase 0 milestones table above is a one-liner. Below is the same info plus the *testable* acceptance criteria per milestone. Each item here is something Kyle can run and verify.

#### Milestone 1 — single-player feel (week 1)

| Acceptance criterion | How Kyle verifies |
|---|---|
| `npm install && npm run dev` boots a browser at `http://localhost:5173` | Page returns 200; React renders; no console errors | **LANDED PR 2** ✅ |
| Babylon.js canvas is visible, scene has skydome + 1 directional light | Screenshot shows lit scene | **LANDED PR 2** ✅ (placeholder sphere instead of Mixamo character — see row 3) |
| A character model is standing in the scene at origin | Visible in viewport | **LANDED PR 3** ✅ — procedural humanoid rig (capsule torso + sphere head + cylinder limbs); real Mixamo glTF deferred to Phase 1 once an asset pipeline exists (see Decisions) |
| WASD moves the character, with smooth acceleration/deceleration | Hold W for 1s → character moves forward; release → character decelerates over ~0.3s | **LANDED PR 3** ✅ |
| Space jumps (single, double-jump disabled in Phase 0) | Tap Space → character jumps, height ~1.5m | **LANDED PR 3** ✅ — **fixed in PR 8**: previous behaviour was "hold Space = fly up forever" because `Havok.PhysicsCharacterController.integrate()` does not accumulate gravity mid-air (the `gravity` parameter is only consumed inside `_resolveContacts` when there's a contact in the manifold). PR 8 accumulates gravity in the controller's `update()` when `!state.supported`, and tightens the jump condition to require `vy ≤ 0` so a single press fires exactly one impulse. Regression smoke `client/tools/jump-regression-smoke.mjs` asserts holding Space for 2s produces one jump and returns to ground. See Decisions log "2026-08-13 — PR 8 implementation decisions". |
| Shift toggles dive (forward + dive for 0.8s anim) | Tap Shift while moving → character dives forward | **LANDED PR 3** ✅ |
| C toggles crouch/slide | Hold C + W → character slides | **LANDED PR 3** ✅ |
| Q triggers wallrun if airborne near a wall at angle | Side approach wall, jump toward it → wallrun along wall for ~1s | **LANDED PR 3** ✅ — **regression observed 2026-08-13**: holding Q mid-air (real browser auto-repeat fires keydowns faster than the wallrun duration of 1000ms) makes the character fly up indefinitely. PR 8.1 fixed by adding rising-edge detection + post-wallrun cooldown. Animation-state only; the stunt changes controller parameters + visual lean, it does not bend the collision shape. Regression smoke `client/tools/wallrun-regression-smoke.mjs` asserts peak Y < 8m + descent after wallrun. See Decisions log "2026-08-13 — PR 8.1 implementation decisions". |
| V toggles third-person ↔ first-person camera | Press V → camera moves from over-shoulder to eye-level | **LANDED PR 3** ✅ |
| Havok physics is the source of truth (verify by toggling Babylon physics off in DevTools) | Physics off → character doesn't move when WASD pressed | **LANDED PR 3** ✅ (PhysicsCharacterController is the only physics source for the character; see Decisions) |

**Done =** all 10 criteria pass in Kyle's browser.

#### Milestone 2 — netcode + combat (week 2)

**PR 4 substrate:** rows 1-4 = **LANDED PR 4** ✅; combat semantics remain PR 5.

| Acceptance criterion | How Kyle verifies |
|---|---|
| Two browser tabs can complete the WebRTC handshake (copy-paste dance) | Both tabs show "Connected" overlay |
| Each tab sees the other player's character in the same scene | Tabs side-by-side, both show 2 characters |
| Local input latency feels < 1 frame on remote view | Move in tab A → tab B sees motion within ~50ms |
| Rollback correction is invisible under 100ms simulated lag | `chrome://network-conditions` → set 100ms throttle; move erratically; no visible teleport |
| Firing the dual pistols (LMB) shoots a raycast that draws a tracer | Click LMB → tracer line from gun to hit point | **LANDED PR 7** ✅ — raycast from chest height along yaw-forward, hit detection via `scene.pickWithRay` (skips local rig + sky + ground), tracer rendered via `MeshBuilder.CreateLines` + 80ms dispose timer. Damage is logged-only (PR 9+ wires real health). |
| Melee attack (RMB) hits within 1.5m cone | Approach within 1.5m, RMB → hit indicator on target | **LANDED PR 7** ✅ — uses the existing `isWithinMeleeCone` helper (60° cone, 1.5m range). Rising-edge on RMB; only emits a CombatEvent on a hit (misses are silent). |
| Holding T toggles bullet time (0.25x speed, full air control) | Hold T → time slows visibly, character can curve shots mid-air | **LANDED PR 7** ✅ — `bulletTimeScale(input, dt)` returns `dt * 0.25` when `input.bulletTimeHeld` is true. Applied to the LOCAL controller only; the remote controller always steps at full speed. |
| Bullet time is independent per player (offensive + defensive mode) | Both players in bullet time independently; presses feel right | **LANDED PR 7** ✅ — per-client LOCAL scaling, not synced across the wire. Both clients see their own bullet-time visuals simultaneously; remote rig still moves at full sim speed in each client's view. |
| Health → 0 → respawn at spawn point | Take 100 damage → 1s respawn timer → back at spawn | **LANDED PR 10** ✅ — HP=100 on `CharacterController.state`; damage flows through `applyDamage()` in `client/src/game/health.ts` on rising-edge combat events (both clients, symmetric); respawn is `controller.respawn(nowMs)` triggered by `tickRespawn()` once `nowMs >= state.respawningUntilMs`. Determinism via the engine-driven `nowMs` (no `Date.now()`). No new wire byte — same shape as PR 7's per-client bullet-time. |
| Per-player first-person mouse-look (per-player yaw, no shared chase camera) | Click canvas to lock pointer; mouse-delta rotates yaw; ESC releases and falls back to chase | **LANDED PR 11.1 + PR 11.3** ✅ — pointer-locked camera renders 1:1 with the character in 1st-person mode (no lerp); over-shoulder mode keeps the camera behind + slightly above the character with rotation tracking the character chest (model rotates IN VIEW). ESC releases into the **menu orbit camera** (slow auto-rotation around the character — no pitch tilt; pitch is irrelevant in this state). V cycles between 1st-person-locked and over-shoulder-locked while locked; V is a no-op while unlocked. **Yaw lives on bytes 2-3** of the input packet (`INPUT_SIZE` bumped 8→10 in PR 11.1, then 10→12 in PR 11.3), little-endian uint16 (~0.0055°/LSB). **Pitch lives on bytes 4-5** of the input packet (PR 11.3), little-endian uint16 ([-π/2, +π/2] → [0, 65535], ~0.00275°/LSB). Both clients compute identical WASD world directions + 3D aim directions from the same decoded yaw + pitch on the same frame — lockstep determinism preserved (the controller's `update()` calls `setYaw(input.yawRadians)` + `setPitch(input.pitchRadians)` BEFORE projecting the character-relative WASD input into world space + computing the 3D forward vector in combat.ts). Sensitivity = 0.0025 rad/px for both yaw and pitch (configurable via `MOUSE_LOOK.sensitivityRadPerPixel` in `characterConfig.ts`). 1st-person + over-shoulder render paths set `camera.rotation.x = -pitchRadians` (Babylon Y-up convention: positive rotation.x looks DOWN, so we negate to match "look up = positive pitch"). Clamp (not wrap) at [±π/2] — pitch has physical limits, looking past the limit hits a wall, not flips the view. Five CI smokes (mouse-look, mouse-pitch, pointer-lock-camera [extended with pitch assertions], yaw-wire-format, pitch-wire-format, pitch-2tab-wire-format [PR #22 cross-context Tab A↔Tab B encoder/decoder cross-check]) + dev-box two-tab playtest 2026-08-15 (Kyle): click-to-lock, cross-client yaw + pitch propagation verified (Tab A drag-up + LMB → Tab B's HP drops when tracer lands, vertical aim direction propagates correctly through WebRTC), over-shoulder model rotation + tilt, ESC menu orbit visible, V cycling correct, pitch clamp at the limits. **Note**: procedural humanoid models have no facial features so vertical aim is best observed via tracer direction + HP drop on the peer, not the model pose itself. |
| One full minute of two-tab play = no console errors, no desync, no rubberbanding | Both tabs stay in sync for 60s | **LANDED PR 11.4 + dev-box two-tab playtest (Kyle, 2026-08-15)** ✅ — completed the 60-second two-tab drive on http://100.95.111.112:5173/ with both tabs sequenced through (1) WebRTC handshake, (2) pointer-lock on both, (3) WASD movement every 3-4s, (4) mouse-look every 2s, (5) LMB every 5s, (6) occasional stunts (Space/Shift/Q/C), (7) V-cycle. Console: zero errors observed. Frame-count delta: ~70s drift consistent with the documented no-rollback limitation (well under the 1500-frame threshold). HP delta: symmetric — Tab A LMB → Tab B HP drops mirrored on Tab A's `HP them:` chip, no errors. **Closes M2.** Note: local-tab test, not WAN; the no-rollback desync is the documented Phase 0 limitation. **PR 11.5 (gap-bridging rollback cap) has since shipped (squash `6e064e84`); live verification deferred to PR 11.6** because meaningful WAN-throttle testing requires a real peer conversation + visible character animation state, neither of which is available today. PR 11.5 is the substitute for the ~70s no-rollback drift under load, not an M2 closure requirement. |

**Done =** all 11 criteria pass with Kyle driving both tabs.

### Phase 0 risks

- **Netcode for bullet-time is the make-or-break.** This is the highest-risk single component. If it doesn't feel right, nothing else matters.
- **The "feel" of TS is the entire game.** Character controller, animation blending, screen shake, camera FOV bumps during dives, hit-stop on punches — these are the things that made TS feel like TS. Hard to spec, hard to test, easy to ship something that looks right but feels wrong.
- **Scope creep.** 30 weapons, 4 game modes, dual-toggle camera, 5 maps, customization, particle effects, etc. — none of it at launch. Pick a tight v1.

---

## Working with this doc

- **This file is the canonical source of truth.** Edit here, on a branch, in a PR. The vault entry at `~/Obsidian/mem/projects/specialists-web.md` is a one-way mirror for Obsidian's graph — never edit it directly, it gets overwritten on the next sync.
- **Top of file**: status, dates, repo location, sync notes. Update when phase changes.
- **Phase sections**: append-only as we complete work. Don't rewrite history — mark superseded phases with `(superseded by Phase N)` so we can chase the reasoning later.
- **Decisions**: log in the Decisions section below as we make them. Each decision = why + when + what we picked + what we rejected.
- **Known issues**: log in the Known Issues section when shipping a known defect or follow-up-deferred behavior with a workaround. Each issue = symptom + workaround + why tabled + severity.
- **Open questions**: log in the Open Questions section. Surface blockers for the next session.
- **Session log**: see `HANDOFF.md` in the repo for session-to-session continuity.
- **Syncing to the vault**: run `./tools/sync-spec-to-vault.sh` from the repo root after merging changes here. One-way: repo → vault.

---

## Decisions

### 2026-08-11 — Stack picks
- **Client**: TS + Babylon.js on WebGPU + Havok + ggrs via wasm
- **Server**: Rust + Tokio + Rapier (deterministic)
- **Transport decision (2026-08-11)**: Phase 0 uses **WebRTC peer-to-peer** between two browser tabs (no dedicated server). Phase 1+ uses **WebTransport** (UDP over HTTP/3) for client→server traffic, with **WebSocket** fallback when WebTransport is unavailable. WebRTC stays as the option for peer-to-peer data channels in Phase 5+ (spectator relays, custom net topologies). **Why split**: WebRTC is the only browser-to-browser transport — no signalling server needed for Phase 0 feel test. WebTransport is the right client→server transport once a server exists, but it's not browser-to-browser. Don't relitigate this in Phase 0.
- **WebSocket fallback**: deferred to Phase 1 (only matters when there's a server)
- **Hosting**: Hetzner (matches existing infrastructure)
- **Auth (eventually)**: Discord OAuth (zero-friction)
- **Asset strategy**: Mixamo + Kenney CC0 for Phase 0
- **Working title**: "Specialists Web" — final name TBD at public launch

### 2026-08-11 — PR 2 implementation decisions
- **WebGPU vs WebGL2**: PR 2 ships **WebGL2** via `new Engine(canvas, true)` (the default). Spec says WebGPU but the Vite dev server + Playwright headless Chromium path is more reliable on WebGL2 today, and the WebGPU bootstrap adds an adapter-wait that complicates the headless capture. WebGPU targeted for PR 3 alongside the character controller. Bootstrap path is a one-line swap to `WebGPUEngine` in PR 3.
- **Vite `optimizeDeps.exclude: ["@babylonjs/havok"]`**: required for Havok to load in dev mode. Havok's ESM uses `import.meta.url` to locate its wasm at runtime; Vite's pre-bundling rewrites that URL to a hashed path the wasm fetch can't follow, causing the browser to fetch the HTML fallback. See the `babylonjs-vite-havok-wasm` skill for the full diagnosis.
- **No static mesh from the asset pipeline**: PR 2 uses a procedural red sphere as the placeholder for the Mixamo character model. The asset pipeline is a PR 3 concern — building it now would be premature for a single-object acceptance test.
- **Phase 0 split into 3 PRs**: tooling baseline (PR 1, done) → scene baseline (PR 2, this PR) → character controller (PR 3, next). Each PR ends at a playable beat: PR 1 = "build runs and shows a banner", PR 2 = "you can see a lit scene with one object", PR 3 = "you can walk a character around". Each PR is independently mergeable and reviewable.
- **Headless smoke in CI**: PR 2 adds a third CI job (`client-scene-smoke`) that boots the dev server, runs Playwright headless against it, captures a screenshot, fails on any pageerror, and uploads the screenshot as a build artifact. Replaces the deferred "Playwright headless smoke test" item from PR 1.
- **Bundle size flag**: Vite reports a 6.99 MB JS bundle (1.56 MB gzip) for PR 2. Spec's "<5MB initial" target is not met. Code-splitting is a PR 3 deliverable (the character controller will benefit from dynamic imports anyway). Not a blocker for PR 2.

### 2026-08-11 — PR 3 implementation decisions
- **WebGPU bootstrap (target met, fallback verified)**: PR 3 swaps in `new WebGPUEngine(canvas, { ... })` + `await initAsync()` as the primary bootstrap, with `new Engine(canvas, true, ...)` as the documented fallback for environments without a WebGPU adapter (CI's headless Chromium, Firefox, older browsers). The fallback path was exercised end-to-end in this PR's smoke run — the canvas still renders the lit scene with the character walking. WebGPU is the spec's target; WebGL2 is the supported safety net, not a permanent decision.
- **Mixamo model decision (procedural humanoid placeholder)**: The acceptance test is "Kyle sees a character that responds to WASD." The real Mixamo glTF is not in this repo (no asset pipeline; CI runs offline), and bundling a `.glb` from a network call at runtime is forbidden by the spec. We ship a procedural humanoid rig (`client/src/engine/characterModel.ts`) — capsule torso matching the Havok collision shape, sphere head, cylinder arms/legs — parented to a `TransformNode` the controller drives. The visual rig is good enough to sell the "character moves" test; a real glTF lands in Phase 1 once we have an asset pipeline. Documented in `characterModel.ts` header so the swap point is obvious.
- **Stunts are animation-state only (no physics deformation)**: Dive, slide, and wallrun swap *values from `characterConfig.ts`* (speed, friction, jumpZ, visual offset) plus visual pose (lean/squash) on the rig. They do NOT change the collision shape height, the capsule radius, or the contact-manifold handling. Stunt-as-physics is a Phase 1 polish item — the spec for row 8 ("wallrun along wall for ~1s") accepts the parameter-swap version. Documented in the controller header.
- **Camera has no mouse-look yet (PR 3)**: The chase camera follows the character with a fixed yaw; no mouse-driven rotation. The character always faces +Z so W moves "forward in the direction the character is looking", independent of the camera. Mouse-look is a Phase 1 polish item per locked decisions.
- **Headless smoke dual-screenshot pattern**: PR 3's smoke (`client/tools/scene-smoke.mjs`) captures two screenshots — the initial scene and a post-W-walk capture — to show, not tell, that WASD actually moves the character. The "walked" capture is uploaded as the `scene-screenshot-walked` artifact. This is the evidence pattern for the rest of the PR series.
- **Bundle delta**: The controller + model + camera + input listener add ~6 source files, all internal (no new npm deps). Vite bundle delta vs PR 2 is < 50 KB gzip — well under the 200 KB guardrail. Bundle size remains flagged for Phase 1 (code-splitting is a Phase 1 task).
- **Spec-canonical CI**: Unchanged. The `spec-canonical` job still passes — the canonical spec is still at `docs/SPEC.md` and `SPEC.md` is still the stub pointer.

### 2026-08-12 — PR 4 implementation decisions
- **Lockstep over ggrs (npm 404 fallback)**: `ggrs` / `ggpo` packages 404 on the npm registry as of 2026-08-12 (they exist as Rust crates, no published wasm/JS binding we can `npm i`). Rather than ship a stub and call Milestone 2 rows 1-4 done, PR 4 implements a real **deterministic fixed-frame lockstep** over the reliable-ordered `inputs` RTCDataChannel that `net/peer.ts` already opens. The class surface (`submitLocalInput` / `advanceFrame` / `frame` / `latestConfirmedFrame` / `dispose`) is shaped like a ggrs `P2PSession` so swapping in a real ggrs binding later is a class swap in `client/src/net/ggrsRuntime.ts`, not a rewrite of the call sites. Documented in the module header + the PR body. **Real rollback is NOT implemented** — late remote inputs are filled by repeating the last-known input. Under LAN / low-latency the two clients stay visually in sync; under heavy loss they can drift, and nothing corrects the drift. Milestone 2 row 4 ("rollback correction invisible under 100 ms lag") is therefore satisfied by *substrate*, not by true rollback. Phase 1 replaces the runtime with ggrs/wasm + a Rust authoritative server.
- **`INPUT_SIZE = 8` locked now**: PR 4 reserves byte 1 for FIRE / MELEE / BULLET bits so PR 5 doesn't require a session restart to add combat. The inputs round-trip through `encodeInput` / `decodeInput` in `net/inputBitmask.ts`; the reserved bits are no-op in PR 4 (PR 5 fills the semantics).
- **Two-character scene, one Havok `PhysicsCharacterController` per rig**: each client runs BOTH controllers with each controller receiving its own encoded input. The remote mesh has NO `PhysicsAggregate` — only the controller exists; the mesh follows the remote controller's transform via the standard `visualRoot` plumbing. Chase camera follows the LOCAL controller only; the remote rig renders next to the local one in the same scene.
- **No signaling server**: copy-paste SDP/ICE dance, per Phase 0 spec. Phase 1 replaces with a Rust WebTransport server + REST `/create` + `/join` endpoints. PR 4 keeps the manual handshake but adds a headless two-tab smoke that drives the dance end-to-end in Chromium (port 5174, separate from PR 3's scene smoke on 5173).
- **WebRTC peer lifted to App.tsx**: ownership of `WebRTCPeer` moved out of `PeerOverlay` so App can hand it to `createScene` via `new GgnetTransport(peer)`. `PeerOverlay` now receives the peer as a prop + reports its status string up via a callback. The chase camera continues to follow the local controller regardless of connection state — disconnected peers leave the remote rig at its spawn with zero input (idling pose).
- **Headless-known caveat for `client-two-tab-smoke`**: WebRTC + STUN-only may not find a working ICE path inside GitHub-hosted runners, even with `--use-fake-ui-for-media-stream`. If the headless smoke flakes, the manual two-tab test ("Kyle opens two tabs in Chrome, copies the offer, pastes the answer") remains the canonical row-1 acceptance. The headless job still exercises the offer/answer UI flow + the lockstep frame counter, which is what the assertion `frame > 5` checks.

### 2026-08-12 — PR 7 implementation decisions
- **Per-client bullet time (not synced across the wire)**: When local holds T, only the local tick runs at 0.25x (`dt * COMBAT.bulletTime.scale`). The peer's full-speed tick is replicated through the lockstep normally — `deltaSeconds` is sampled by the engine per-tick, not crossed on the wire. Both clients see their own bullet-time visuals simultaneously; the remote character still moves at full sim speed in each client's view. This is the only way Milestone 2 row 8 ("per-player independent bullet time") works without round-tripping a wall-clock signal across RTCDataChannel.
- **Damage is render-side log only in this PR**: `dualPistolShoot` and `meleeSwing` return `{ damage: number }` in their result structs; `gameSession.tick` records the damage in the `CombatEvent` for the HUD. No health pool exists yet — PR 9 owns real damage application + respawn. Keeping damage out of the render path means the smoke can assert "a tracer rendered + a hit counter incremented" without needing health state to exist.
- **Tracer render via `MeshBuilder.CreateLines` + `setTimeout` dispose**: No Babylon animation framework yet — each tracer is a fresh `LinesMesh` disposed after `COMBAT.dualPistol.tracerDurationMs` (80ms) via `window.setTimeout`. The timer guards against scene teardown (`if (!lines.isDisposed()) lines.dispose()`). If you skip the dispose, the scene leaks meshes — caught in the smoke by counting events vs. mesh count if needed.
- **Rising-edge key semantics for fire / melee (one event per press, not held)**: `fireHeld` / `meleePressed` are *held* flags in `InputState` (because the wire has no edge concept), but combat semantics fire only on the rising edge — `wasFiring` / `wasMelee` track the previous input in `gameSession.tick`. `meleePressed` is cleared inside `inputListener.read()` (was previously a bug — set true on mousedown, never cleared) so the next RMB click registers a fresh rising edge. Held-fire therefore produces exactly one tracer per press, not 60/s.
- **`InputBits` split into `MoveBits` + `CombatBits`**: PR 4 used a single `as const` object with `FIRE=1, LEFT=1` (same numeric identifier for both byte-0 LEFT and byte-1 FIRE), making the FIRE/MELEE/BULLET bits effectively unreadable. PR 7 splits them into separate `MoveBits` (byte 0) and `CombatBits` (byte 1) consts; `encodeInput` actually writes byte 1 now; `decodeInput` actually reads it. Backwards compatible because existing PR 6 traffic has byte 1 = 0 and both clients upgrade together.

### 2026-08-13 — PR 8 implementation decisions
- **Root cause: `havok.integrate()` does not accumulate gravity mid-air.** `Babylon.PhysicsCharacterController` is a kinematic (ANIMATED-body) controller and the `gravity` Vector3 parameter to `integrate(dt, info, gravity)` is consumed only inside `_resolveContacts()` — i.e., only on frames where the contact manifold has at least one entry. Mid-air (no contact), the velocity we hand to `setVelocity()` is preserved verbatim with no gravity accumulation. PR 3's controller relied on Havok applying gravity for us; it didn't, so the jump impulse `vy = MOVEMENT.jumpZ = 5.2 m/s` was applied on the rising edge and stayed at 5.2 forever, with the capsule ascending at 5.2 m/s indefinitely. The PR 8 fix accumulates gravity in `CharacterController.update()` whenever `!state.supported`: `vy += MOVEMENT.gravity.y * deltaSeconds` before `setVelocity`.
- **`havok.integrate()` is now called with `Vector3.ZeroReadOnly` for gravity.** Otherwise Havok's contact-resolver applies gravity *in addition* to our pre-integrate accumulation on frames where the manifold has a ground entry — i.e., on the very frame the character lands. That would briefly double-apply gravity and produce a small downward bounce on landing. Zero gravity is the canonical contract for "the user manages gravity."
- **Jump condition tightened from `input.jumpPressed && state.supported` to `&& state.supported && vy <= 0`.** The old condition fired on ANY rising edge while `state.supported` was true. In edge cases where the contact manifold flipped `supported=true` between frames during a descent (e.g., a one-frame transition while `vy` was still slightly positive), a second jump impulse could be layered on top of the residual upward velocity. Requiring `vy <= 0` ensures the jump can only fire from a true grounded state — no residual upward velocity. This is the standard "grounded jump" pattern documented in every character-controller tutorial and avoids the multi-jump bug for free.
- **Failing-test-first reproduction was the gate to the fix.** Per `HANDOFF.md` standing rule "Don't merge PR 8 without a failing-test-first reproduction." Wrote `client/tools/jump-regression-smoke.mjs` first (it returned 0 → -1m → 0 exit-1 with `[flew-up-forever]` and `[monotonic-rise]` errors against PR 7's working tree), then added the per-frame `__jumpDiag` ring buffer to capture `{jp, sup, vy, y}` per frame for root-cause analysis. The diag showed `vy=5.2` and `y≈12m` in alternating frames with `state.supported` correctly reflecting Havok's state — proving the bug was gravity-not-accumulating, not `supported`-stuck. Diag instrumentation removed before commit; only the `__jumpProbe` Y-pos accessor remains (the smoke's contract).
- **New `client-jump-smoke` CI job on port 5175.** Models after the existing scene-smoke / two-tab-smoke jobs but uses a dedicated port so all three can run in parallel. Boots a vite dev-server on 5175, runs the smoke, uploads the post-jump screenshot as `jump-regression-screenshot`. Without this job, the regression could sneak back in via a future PR that touches `characterController.ts` (the file is small but the failure mode is silent — the build passes, the scene renders, but jumps don't work).
- **Spec drift caught and fixed in same PR**: PR 7's status-banner line in `docs/SPEC.md` was reworded ("LANDED PR 7 ✅" with one-line notes) and the Milestone 1 row-5 entry was flipped from "regression observed" to "fixed in PR 8". This PR also adds the `vite-env.d.ts` ambient-types stub (`/// <reference types="vite/client" />`) needed by `import.meta.env.DEV` — the project had no Vite typings before and the diag probe couldn't have been typed cleanly without it.
- **PR 7.3 debug instrumentation left intact for now.** PR 7.3 added `__lastMouseDown` / `__canvasDown` / `__topLevelMouseDown` / `[input] mousedown` console logs as debug aids for the LMB/RMB-input bug. Per PR 7's HANDOFF entry, those get cleaned up in "PR 7.4 cleanup after Kyle confirms combat is solid in real play." Not in PR 8's scope — the regression fix doesn't touch input paths.

### 2026-08-13 — PR 8.1 implementation decisions
- **Root cause: wallrun auto-repeat loophole.** Same shape as the PR 8 jump bug, different mechanism. When Q is held in a real browser, Chromium fires `keydown` events with `e.repeat=true` at the OS auto-repeat rate (typically every 30-50ms after an initial 500ms delay). My inputListener filters `!e.repeat`, so only the FIRST Q press sets `wallrunPressed=true`. But: `wallrunPressed` is cleared at the end of every `read()` cycle and re-set on the next `keydown`. After the 1000ms wallrun timer expires, if the user is still holding Q, the next auto-repeat fires a fresh `wallrunPressed=true` — and wallrun re-enters, resetting the timer. Each cycle is 1000ms of upward motion; the user perceives this as "fly up forever".
- **Fix #1: rising-edge detection in the controller.** Track `wasWallrunPressedLast` (private field on `CharacterController`). Wallrun entry requires `wallrunPressed && !wasWallrunPressedLast` — the rising edge. This alone is NOT sufficient: aggressive keydowns firing every 50ms create gaps where `wallrunPressed=false` for one frame, `wasWallrunPressedLast=false` resets, and the next keydown triggers a new rising edge.
- **Fix #2: post-wallrun cooldown.** Track `lastWallrunEndedAtMs` (private field, set when `exitStunt()` fires from the wallrun timer). Wallrun entry is rejected if `nowMs < lastWallrunEndedAtMs + durationMs + 200ms`. The 200ms grace absorbs the worst-case frame jitter. After cooldown expires, the user must RE-PRESS Q (release + press) to wallrun again — but `wasWallrunPressedLast` already handles that as a natural side effect.
- **Aggressive auto-repeat test (not committed).** Wrote `client/tools/aggressive-wallrun.mjs` (since deleted) that dispatches synthetic `keydown` events every 50ms via `window.dispatchEvent`. This bypasses Playwright's no-auto-repeat behavior. Pre-fix: peak Y = 12.9m monotonic. Post-fix: peak Y = 6.77m with descent. The committed `wallrun-regression-smoke.mjs` uses the natural Playwright single-keydown path (which passes both pre- and post-fix in headless) — it's a regression guard for the simple case. The aggressive test is what catches the real bug; it's the dev-loop diagnostic, not the CI gate.
- **New `client-wallrun-smoke` CI job on port 5176.** Same shape as the jump-smoke job (port 5175). Models the existing scene-smoke / two-tab-smoke / jump-smoke parallelism pattern. Without it, the wallrun fix could silently regress in a future PR that touches `CharacterController.refreshStuntState`.
- **Spec drift caught and fixed in same PR**: PR 8's status-banner line in `docs/SPEC.md` gets a PR 8.1 follow-up entry. `client/.gitignore` gets `wallrun-regression.png` added. No spec-table row addition needed (PR 8.1 closes Milestone 1 row 6 — the wallrun stunt itself, which PR 3 LANDED but regressed in real-browser playtest).

### 2026-08-13 — PR 10 implementation decisions
- **No new wire byte (lockstep determinism is sufficient).** The two clients already compute identical `CombatEvent`s from identical inputs (PR 7's `dualPistolShoot` / `meleeSwing` are pure functions of the input + the controller states, both identical on both ends). Adding a byte-2 wire format for damage intent would be redundant — the damage can be derived locally on each client from the same combat event. PR 10 applies damage per-client to the OPPONENT controller on the rising edge of each combat event; both clients arrive at the same HP values on the same frame. The wire format (`INPUT_SIZE = 8`) is unchanged.
- **Health pool on `CharacterController.state`, not on a separate Player object.** `hp: number` and `respawningUntilMs: number` join the existing `position` / `rotation` / `supported` / `sliding` / `stunt` fields. Init to `HEALTH.maxHp` / `0` in the constructor; reset to those same values in `reset()`. Damage application lives in a separate `client/src/game/health.ts` (single source of truth for `applyDamage` + `tickRespawn`), keeping `CharacterController.update()` pure-physics.
- **Per-client respawn timer (same shape as PR 7's bullet-time).** Each frame, both clients compute the same `nowMs` from their own engine frame observer (`performance.now()` — but only as the input to `tick()`, never inside combat/health code). The respawn timer is driven by this `nowMs`; both clients hit `nowMs >= respawningUntilMs` on the same frame because the value is identical at that point (delta grows linearly and Havok float-rounding is documented to be acceptable). Teleport fires on the first frame the threshold is crossed. No out-of-sync possible.
- **DEV-only `__teleportRemote(x, z)` accessor for the smoke.** Same pattern as PR 8's `__jumpProbe`: a Vite `import.meta.env.DEV`-gated hook on `window` that the smoke calls to teleport the remote rig onto the local rig's position. Lets the smoke guarantee every LMB hit lands without depending on the visual placement of the two capsules. Stripped from production bundles.
- **Why this is honest for Phase 0 (not a hack).** Phase 0 has no server to be authoritative — the lockstep is the contract. Damage is derived locally from combat events that are already deterministically identical; the only way the two clients could desync on HP is if the damage application path itself were nondeterministic. It isn't: `applyDamage` is a pure function of the controller state + the damage amount + the engine-supplied `nowMs`. Phase 1 swaps in server-authoritative damage without touching the `applyDamage` API — the controller's HP slot is identical, the source of the damage call moves from "local combat" to "server packet".
- **New `client-health-smoke` CI job on port 5177.** Single-tab Playwright smoke. Boots Chromium, waits for scene, clicks canvas, teleports remote onto local via `__teleportRemote(0, 0)`, fires 10 LMB hits with 250ms intervals (9 are needed to drop remote HP from 100 to 0 at 12 dmg/hit; the 10th is a buffer), reads the HUD chip's `HP them:` line after each hit (the **remote** rig is the target, not the local — the local is the firer), asserts: (1) remote HP drops to 0, (2) respawn countdown appears, (3) after waiting 1100ms (timer + 100ms slack) remote HP is back to 100, (4) local controller Y position is within 0.5m of `SPAWN_Y = 0.9`. Uploads `health-regression.png` as `health-regression-screenshot` artifact. Mirrors the existing `client-jump-smoke` (port 5175) and `client-wallrun-smoke` (port 5176) job shapes.
- **Two bugs caught during the verification-gate re-verify (post-codex lazy-stop).** The codex implementation was correct on the controller + session + smoke + CI changes, but two render-side details needed correction: (1) `getHealthSnapshot()` originally exposed the absolute `respawningUntilMs` timestamp, which the HUD rendered as "respawn 8787ms" (a value, not a countdown). Fixed by caching `lastNowMs` inside `tick()` and computing `respawningUntilMs - lastNowMs` for the snapshot's `respawningMs` field. (2) The smoke originally asserted `local HP = 0` after firing — it conflated the firer with the target. The local player is the firer, the remote is the target; damage flows firer → opponent. Fixed to assert `remote HP = 0`. Both bugs would have blocked the PR if not caught; both were caught by Evo re-running the smokes the codex claimed were green (Stage 2 of the `coding-harnesses` loop). Documented in the HANDOFF entry's "Bug-honesty disclosure" block.
- **PR 10.2 update (2026-08-13)**: the respawn teleport itself was correct (logs proved `controller.respawn()` fired with `havok.setPosition` + `state.position` + `visualRoot.position` all moving to the target). But the target for the cyan rig was `(2.5, 0.9, 0)` (its `startPosition`) instead of `(0, 0.9, 0)` (matching the red rig's spawn). PR 10.2 separates `respawnPosition` from `startPosition` and the game session passes `respawnPosition = localSpawn` to the remote rig. **Functionally, the respawn flow has been correct from PR 10 onward; PR 10.2 only fixes the cyan rig's teleport target.**

### 2026-08-13 — PR 10.1 implementation decisions (WebRTC ICE candidate bundling)
- **The bug**: `createOffer` / `createAnswer` in `client/src/net/peer.ts` fire-and-forget'd the ICE gather AFTER serializing the return blob. The blob's `candidates: []` was always empty even though `this.candidates` was being populated in the background. `acceptAnswer`'s `for c of a.candidates` loop had nothing to `addIceCandidate` on the host side. The SDP itself contained some candidates (host IP, TURN-relay IP) so connections on the same LAN or via TURN-reflexive-in-SDP completed, but connections over Tailscale / non-TURN-reachable networks stranded every srflx candidate in `this.candidates` and never delivered them. The CI smoke asserted SDP state, not `connectionState === "connected"`, so it never caught the bug.
- **The fix**: `await this.ice()` (5s timeout) before serializing the return blob. The blob now ships `[...this.candidates]` instead of `[]`. The 5s window is enough for TURN allocation in the common case; if it expires, the gathered-so-far list is returned anyway.
- **Why 5s, not 30s**: the original 30s was over-conservative for a sandboxed CI env. On a real network, 5s is enough for TURN to allocate. If TURN takes >5s, the user gets a degraded connection (SDP-only candidates) instead of no connection.
- **Status text surfaces candidate count**: "Offer ready (3 candidates) — copy and share" vs "Offer ready (1 candidate — TURN may be unreachable)". Self-diagnosis for the user when the network can't reach TURN.
- **Why the new `pr10.1-connection-test.mjs` is committed but not gated as a CI smoke**: it drives the real SDP dance + waits 15s for `connectionState === "connected"`. CI can't pass it (TURN unreachable from the GH runner). The dev box can. Documented honestly in the smoke's own comments.
- **No codex+claude review loop used**: single-file fix in `peer.ts` + a status-text change in `PeerOverlay.tsx` + a diagnostic tool. The review loop's value-add for "small fix in a single file with a known root cause" is low. The honest gate is the dev-box two-tab playtest.

### 2026-08-13 — PR 10 + PR 10.1 stacking decision
- **Single combined branch, not two PRs.** PR 10.1 is a strict follow-up to PR 10. Without 10.1, the two-tab playtest of PR 10's health/damage code can't establish a connection on Tailscale, so PR 10's HP sync can't actually be tested in two-tab mode (the whole point of the PR). Shipping them separately creates a window where PR 10 alone is broken in two-tab mode. A combined PR is honest about what works together.
- **The rebase is mechanical.** `git worktree add -b feat/phase0-ice-candidate-bundling-rebased ~/Development/specialists-web-pr10.1-rebased feat/phase0-health-damage-respawn` then `git cherry-pick 84e4700` (PR 10.1's commit). Auto-merge resolved the code files cleanly (PR 10 and PR 10.1 don't overlap on `client/src/`). Only HANDOFF.md and docs/SPEC.md had merge conflicts because both PRs added new status entries; both kept and merged by hand.
- **Re-verify every gate on the rebased branch.** Per the `coding-harnesses` skill's sequencing rule (re-run smokes BEFORE doc-check). All 5 gates green on the rebased branch: typecheck + build + scene-smoke + jump-regression-smoke + wallrun-regression-smoke + health-regression-smoke + two-tab-smoke. The pr10.1-connection-test fails in CI (TURN unreachable from GH runner) but passes on the dev box.

### 2026-08-13 — PR 10.2 (respawnPosition separation) decision
- **The bug, found during the dev-box two-tab playtest of PR 10 + 10.1**: when the cyan rig (remote-mirror) respawns, it teleports to `(2.5, 0.9, 0)` — its own `startPosition` (set offset for initial visual clarity). But the actual remote player's red rig (on the other tab) respawns to `(0, 0.9, 0)` — its own `startPosition`. The cyan rig and the red rig desync on respawn: the cyan rig is 2.5m to the right of where it should be.
- **The fix**: separate `respawnPosition` from `startPosition` on `CharacterController`. `startPosition` keeps the initial visual offset (cyan rig starts at `(2.5, 0.9, 0)` so the player sees both rigs side-by-side at game start). `respawnPosition` is the teleported-to point, defaults to `startPosition` for backward compat, and the game session passes `respawnPosition = localSpawn` to the remote controller so the cyan rig respawns to `(0, 0.9, 0)` — matching the red rig on both clients.
- **Why this is a 1-line `CharacterController` change, not a redesign**: the lockstep is symmetric. Both clients' local rig respawns to `(0, 0.9, 0)`. Both clients' remote-mirror rig should respawn to the same point, because the remote-mirror rig is a simulation of the OTHER client's local rig. The two values are linked at the construction site (game session passes `localSpawn` as `respawnPosition` for the remote rig), not at the controller level. The controller just respects whatever `respawnPosition` is passed in.
- **Diagnostic lifecycle**: added `[respawn] before/after` console.log + `me pos / them pos / respawns` HUD lines + `window.__respawnCount` to verify the respawn flow during the playtest. The diagnostics caught the bug (`label: "remote"` + `pos: { x: '2.50', y: '0.90', z: '0.00' }` showed the cyan rig teleport was firing but to the wrong point). All three diagnostics removed in the cleanup commit (`ed7dea5`); the actual fix (`0b3c64e`) stays.
- **No CI smoke added**: the existing `health-regression-smoke.mjs` only tests the LOCAL rig (uses `__teleportRemote` to put the remote rig at local spawn, then fires 9 hits — all respawn assertions are on the local rig). A two-tab smoke that asserts both tabs' respawn positions match is Phase 1 work (the cyan rig visibility follow-up).

### 2026-08-13 — Phase 1 follow-ups carried from PR 10 + 10.1 + 10.2 (re-ordered for internet-multiplayer goal)
- **(1) Per-player first-person mouse-look (PR 11.1 candidate)** ✅ SHIPPED 2026-08-14 (squash `2fdda30`) — the production camera model for any multiplayer shooter is per-player first-person (or third-person) view; the current chase camera follows the local rig only, which is correct for one player on their own machine but is the dev-box-viewing model not the shipped-player model. Standard pointer-locked mouse-look (click to lock, ESC to release, mouse-delta → yaw). Affects `chaseCamera.ts` + `inputListener.ts` + `characterController.ts.setYaw`. **This is the first Phase 1 PR per Kyle's 2026-08-13 re-rank — the project's goal is internet multiplayer, where the per-player camera IS the camera.**
- **(2) Dev-box free-fly spectator camera (PR 11.4 candidate, debug-only only)** ✅ SHIPPED 2026-08-15 (squash `8485ea3`) — the cyan-rig-disappears issue in two-tab dev-box play is a **debug-mode viewing problem, not a production camera problem**. With a per-player camera the cyan rig is just another entity in the world; the user knows to look around. For dev-box debugging, add an F2 toggle that detaches the camera from the player (free-fly, orbit with mouse, return on click). **Now used every two-tab dev session** to inspect the world.
- **(3) Gap-bridging rollback (PR 11.5 candidate)** ✅ SHIPPED 2026-08-15 (squash `6e064e84`, PR #27) — Tab A has run ~28,000 frames while Tab B has run ~26,000 frames. Both tabs agree on world state (HP, position) because the lockstep is deterministic and both compute the same result from the same input history, but their `frame` HUD counters differ by ~70s of game-time drift. This is the documented no-rollback lockstep limitation in `ggrsRuntime.ts` — repeated inputs fill the gap. **First cut shipped**: the "pause-when-too-far-behind" cap (`ROLLBACK_CAP_FRAMES = 8`, ~133ms lookahead budget) fires when `predictionDepth = max(0, localFrame - 1 - highestRemoteFrameSeen) >= 8` AND we've heard from a peer. ~165 lines in `ggrsRuntime.ts` + ~88 lines in `gameSession.ts` + new smoke (port 5188) + new CI job. Wire format unchanged. **Live playtest deferred to PR 11.6** — meaningful WAN-throttle verification requires a real peer conversation + visible character animation state, neither of which is available today. Real rollback via ggrs/wasm is still the long-term answer.
- **(4) Server-authoritative damage (PR 11.6 candidate)** ✅ NEXT UP — current setup derives damage locally from lockstep, which is fine for LAN / Tailscale (sub-50ms latency) but doesn't survive 100ms+ WAN latency. Repeated inputs pile up because the peer's input arrives too late, then snaps in non-deterministically. **Phase 1 fix**: move damage application from "local combat" to "server packet" — the controller's HP slot is unchanged, the source of the `applyDamage` call moves from `gameSession.tick` (per-client local) to a server-broadcast packet handler (per-authority). This is the first step toward a real dedicated server (Tokio + Rapier for determinism, WebTransport for game traffic, WebSocket fallback). **This PR carries PR 11.5's deferred verification debt** — once a server is wired up, multi-tab WAN-throttle is the natural test environment for both features.
- **(5) Chrome tab throttling** — when one tab is backgrounded, Chrome throttles RAF to ~1Hz, so that tab's simulation effectively pauses. The lockstep doesn't crash (it just runs slower on one side), but it exacerbates the desync. **Now absorbed by PR 11.5's cap** — when the throttled tab is the slow one, the fast tab will pause its `tick()` until the slow tab catches up. Behaviorally a graceful "freeze and resume" instead of an unbounded drift. Real rollback (ggrs/wasm) remains the long-term answer for sub-frame desync.
- **(6) Real wall-detection via `PhysicsRaycast`** — ~20-line change in `characterController.ts` + new regression smoke. Replaces the "Q-mid-air as animation-state-only thrust stunt" with actual wall collision checks. Lower priority — Q wallrun is currently an animation-state-only stunt; this changes it to have physics, but Phase 1's combat refactor may change the inputs entirely.
- **(7) Mixamo glTF character** — replaces the procedural rig with an actual animated humanoid. **Was deferred behind PR 11.5's playtest verification**: without walk cycles + body tilt, PR 11.5's paused-character observation is invisible (procedural rigs have no animation state to "pause"). Should be prioritized higher than originally thought, given the verification value for both PR 11.5 (already shipped) and any future "is the game responding?" test.
- **(8) Polish**: kill-marker, hit-marker, death animation.

**Re-rank rationale (Kyle, 2026-08-13 23:30):** the project's goal is **internet multiplayer**. Split-screen (the previously-#1 candidate) is a single-machine local-coop pattern, not an internet-multiplayer pattern. The correct production camera model is per-player (item 1 — shipped). The dev-box two-tab play visual discomfort is solved by a debug-mode spectator toggle (item 2 — shipped). Rollback (item 3 — first-cut shipped as PR 11.5; real rollback is the long-term play) and server-authoritative damage (item 4 — next) are the actual internet-multiplayer architecture work; everything else is polish.

**2026-08-15 update**: items 1, 2, 3 are shipped. Item 4 is the next session's work. Item 7 (Mixamo glTF) re-prioritized as a verification-environment upgrade — the procedural rigs without animation are a recurring obstacle to dev-box playtests (PR 11.5 deferred on this exact basis). Items 5-8 deferred further.

### 2026-08-14 — PR 11.1 implementation decisions (per-player first-person mouse-look)
- **Yaw on the wire, not client-local.** The controller's `update()` rotates the character-relative WASD input by `yawRadians` (see lines 260-261 of `characterController.ts`). If yaw were client-local (each tab accumulates its own yaw from its own mousemove), the two clients would compute different world directions for the same WASD input — instant desync. PR 11.1 encodes yaw on bytes 2-3 of the input packet (little-endian uint16, ~0.0055°/LSB). Both clients decode the peer's yaw on the same frame and `setYaw(input.yawRadians)` BEFORE projecting WASD. Same lockstep pattern as PR 10's damage intent on byte 1 (no `Date.now()` inside the tick, just the wire-decoded value).
- **`INPUT_SIZE` bumped 8 → 10.** Both clients upgrade together. PR 6/7/10 traffic with bytes 2-3 = 0 still decodes correctly (yaw = 0 = facing +Z — the pre-PR-11.1 default). The defensive `?? 0` on the byte reads in `decodeInput` keeps the decoder robust against truncated packets during the upgrade window.
- **Pointer-lock acquire on canvas click.** The input listener's `click` handler on the canvas calls `canvas.requestPointerLock()` (guarded against editable targets via the existing `isEditableTarget` helper, so pasting into the WebRTC SDP textarea doesn't trigger lock). `pointerlockchange` on document fires `onPointerLockChange(locked)` on the chase camera; ESC releases lock naturally (browser behavior) and the same listener fires `onPointerLockChange(false)`.
- **First-person camera = 1:1 with character.** When pointer-locked, the camera snaps to `character.state.position + CAMERA.firstPersonOffset` (eye height, no back-off) and reads the character's yaw quaternion for the rotation. NO lerp in this path — the locked view IS the character's view, not a follow-camera. The pre-PR-11.1 lerped chase behavior is preserved for the pointer-lock-not-granted fallback (V-toggle still works there).
- **Chase camera is the fallback.** Per the HANDOFF's "Blockers / open questions" entry: "Default = chase camera is the fallback when pointer-lock is not granted." When `pointerLocked === false`, the existing lerped chase behavior runs (third-person or first-person-chase depending on V-toggle). ESC releases lock; the user can still look around via V (chase first-person) or hold still (chase third-person). Browser refusing pointer-lock (insecure context, no user-activation, etc.) falls back automatically.
- **Yaw resolution = 1/65536 of a revolution.** Plenty of resolution for an FPS feel (~0.0055°/LSB). 0.5 rad delta at sensitivity 0.0025 rad/px = 200 pixels of mouse movement — comfortable. Tunable later via `MOUSE_LOOK.sensitivityRadPerPixel` (single-line change in `characterConfig.ts`).
- **Yaw accumulator wraps mod 2π.** The chase camera's `applyYawDelta` does `((yaw + delta) % 2π + 2π) % 2π` so the accumulator doesn't drift at large values. A user spinning the mouse for 10 minutes straight stays within `[0, 2π)`. The smoke's "wrap" assertion (cumulative 7.0 rad delta from initial 0.5 → wraps to ~0.7168) catches this contract.
- **Smoke uses DEV-only `__applyYawDelta` accessor, not `requestPointerLock()`.** Headless Chromium doesn't reliably honor `requestPointerLock()` (user-activation requirements aren't met by synthetic clicks). The smoke drives the yaw via `window.__applyYawDelta(deltaRadians)` which calls the same `chase.applyYawDelta` code path the locked-mousemove listener uses. This proves the yaw-rotation code works end-to-end WITHOUT depending on the browser actually granting lock. The full pointer-lock UX requires a real-browser dev-box two-tab playtest post-merge.
- **Smoke asserts wrap + initial + after.** Three assertions: (1) initial yaw is a finite number (sanity check), (2) after applying 0.5 rad delta, observed delta is ~0.5 ±20% (modulo the mod-2π wrap), (3) after a 7.0 rad cumulative delta, yaw is in `[0, 2π)`. Screenshots to `mouse-look.png` for CI artifact upload. Mirrors the structure of the existing `health-regression-smoke.mjs` / `wallrun-regression-smoke.mjs` / `jump-regression-smoke.mjs`.
- **Pitch is NOT in this PR.** Yaw only. Mouse Y-delta is ignored. Pitch would need its own wire byte pair and the manual-frame-rate reset (don't let a frame jitter translate to a permanent pitch offset); deferred to a follow-up PR if Kyle asks for it.
- **No codex+claude review loop used.** The brief was authored with the design decisions baked in (based on the HANDOFF's locked spec), and the implementation is mechanical once the wire order is fixed. The cross-vendor review would have added ~5-10 min wall time for marginal value here. The honest gate is the smoke + the dev-box two-tab playtest.
- **Codex 0.137 `apply_patch` tool failure (2026-08-14).** The first dispatch burned 2.5M+ tokens in retry loops on `apply_patch` — codex kept writing the patch with literal `\\n` escape sequences in the JSON `arguments` field, got `apply_patch verification failed: invalid patch: The first line of the patch must be '*** Begin Patch'` repeatedly. Recovery attempt (switching to `exec_command` with heredoc) failed on shell-quote escaping. Killed codex + did the work manually with the Hermes `patch` tool (~15 min wall time end-to-end including all smokes). **Lesson:** when codex's `apply_patch` tool fails repeatedly with the same error, don't wait for it to recover — fall through to manual execution immediately. Same shape as the M3 lazy-stop kill threshold from `coding-harnesses` pitfall #16b.

### 2026-08-14 — PR 11.2 implementation decisions (pause / loadout menu UI)
- **Menu visibility = `(pointerLocked === false) && (everLocked === true)`.** This is the exact same predicate the chase camera uses for `isMenuOrbit()` (chaseCamera.ts:319). The single source of truth for "is the menu visible" is one boolean comparison — no React state machine, no deferred rendering, no edge cases. The `everLocked` gate prevents the menu from flashing on a fresh page that hasn't been clicked yet.
- **Resume re-uses the existing `chase.setPointerLock(true)` entry point** — same path as the browser's `pointerlockchange` event and the `__pointerLockToggle` DEV probe. No new state machine, no separate "Resume" API. The chase camera handles viewMode restoration internally.
- **ESC-equals-resume (Kyle's 2026-08-14 feedback).** `inputListener.ts` adds an `onEscapePressed` hook (the input listener doesn't read lock state — it just emits the event). The PauseMenu component AND a document-level `keydown` listener in the PauseMenu's `useEffect` both call `chase.setPointerLock(true)` on ESC. Redundant on purpose: the input listener's onEscapePressed is the production path, the PauseMenu's useEffect is the fallback if something is layered between the listener and the chase camera. Matches the muscle memory of every FPS (CS, Half-Life, Minecraft, etc.).
- **`setPointerLock(true)` always restores the user's last `viewMode`** (PR 11.1 kept forcing mode 0 on every re-lock, which would have lost the user's over-shoulder preference across every pause cycle). Stored in a new `lastLockedViewMode` field on the chase camera, updated on every V-toggle WHILE locked. First lock defaults to mode 0 and remembers that; subsequent locks restore whatever was stored. Matches Kyle's "resume the last camera" intent.
- **Native `<button>` elements**, not styled `<div>`s — keyboard-accessible by default (Enter/Space activate), have built-in focus indicators, no `onKeyDown`/`role`/`tabIndex` ceremony. Loadout + Settings are `<button disabled>` placeholders with "coming soon" subtitles.
- **Backdrop has `pointer-events: auto`** — the PR 7 HUD-overlay-eats-clicks trap. Every overlay must consciously decide `pointer-events`. The smoke asserts computed style on the backdrop AND on every button (12 assertions total).
- **No animation on show/hide** — `null` vs rendered. PR 11.2 is functional, not visual polish. A fade-in is a 5-line follow-up (`opacity` transition on the backdrop div) if Kyle wants it.
- **Disconnect Peer = `peer.close()`.** Doesn't reset PeerOverlay's React state explicitly — PeerOverlay reads `peer.connectionState` on its own interval and surfaces "disconnected" status, allowing the user to re-host or join a new peer without a page refresh. Alternative (reset PeerOverlay state too) was discussed and rejected as ~30 extra lines for non-essential "I want to leave this session" UX.
- **Cursor visibility on the backdrop = `default`.** When locked, the pointer-lock's native cursor hiding handles the rest. Visible cursor + clickable buttons = the affordance the user expects from a pause menu.
- **`getChaseState()` on `SceneHandle` is the new React ↔ engine boundary.** The React HUD layer polls `handle.getChaseState?.()` at 10Hz (same cadence as the rest of the HUD) to get `{isPointerLocked, isMenuOrbit, everLocked, viewMode}`. Single source of truth: the chase camera's internal flags. `everLocked` is derived as `isPointerLocked || isMenuOrbit` to avoid adding a new chase camera accessor for the same logic.
- **Initial design had a `pauseMenuOpen` flag in `scene.ts` gating the ESC handler.** Removed during integration testing — it's redundant because `chase.setPointerLock(true)` is a safe no-op when already locked, so an unconditional `onEscapePressed → chase.setPointerLock(true)` is simpler than a gated one.
- **The stray `[input] window mousedown (capture path)` console.log from PR 7** was finally removed in this PR (the window-level mouseup/down listeners at lines 246-250 in inputListener.ts). PR 7.4 cleanup missed it because the cleanup was about input listener state, not about debug logging. Trivial follow-up that's no longer needed.
- **`bundle delta: +3.57 kB raw / +0.5 kB gzip`** — well under the 5 kB target. The DEV-only probes (`getChaseState`, `setPointerLock` on SceneHandle) are tree-shaken out of production by Vite.

### 2026-08-15 — PR 11.3 implementation decisions (per-player mouse pitch)

- **Pitch is on the wire, not client-local.** Same lockstep argument as PR 11.1's yaw: if pitch were client-local (each tab accumulates its own pitch from its own mousemove), the two clients would compute different vertical aim directions for the same WASD + LMB input — the tracer would fire at different angles on each client, the melee cone would catch different targets, and the over-shoulder camera would tilt differently. PR 11.3 encodes pitch on bytes 4-5 of the input packet (little-endian uint16, [-π/2, +π/2] → [0, 65535], ~0.00275°/LSB). Both clients decode the peer's pitch on the same frame and `setPitch(input.pitchRadians)` BEFORE the WASD + combat projection. Same lockstep pattern as PR 10's damage intent on byte 1 (no `Date.now()` inside the tick, just the wire-decoded value).
- **`INPUT_SIZE` bumped 10 → 12.** Both clients upgrade together. PR 6/7/10/11.1/11.2 traffic with bytes 4-5 = 0 is decoded by `decodeInput` as `pitchRadians = 0` (level) via a special-case shim — the linear formula `(bits / PITCH_BITS_SCALE) - π/2` would produce `-π/2` (looking straight DOWN) for `bits === 0`, which is the wrong default for the upgrade window. Pre-PR-11.3 traffic has `bits === 0`; the shim preserves "no pitch info = level aim". The defensive `?? 0` on the byte reads in `decodeInput` keeps the decoder robust against truncated packets.
- **Pitch range is `[-π/2, +π/2]`**, not `[0, 2π]`. You can't physically look behind in the vertical axis. The wire uint16 maps linearly to `[-π/2, +π/2]`. 0 on the wire = pitch = 0 = level (assuming no backward-compat packet with bytes 4-5 = 0; see previous bullet).
- **Pitch clamp in TWO places**: `pitchToBits()` (wire-encode defense) AND `applyPitchDelta()` (runtime defense). Float drift at exactly ±π/2 would overflow the uint16 / produce out-of-range lookups. Clamp both ends. The clamp is `Math.max(-π/2, Math.min(π/2, ...))` — NOT wrap (see next bullet).
- **`applyPitchDelta` CLAMPS, not wraps.** Verified by pre-dispatch math sanity check + the pitch smoke's regression guard. Pitch is a half-circle with physical limits — looking past the limit should hit a wall (every FPS behavior), not flip the view. Wrap (`((p + d + π/2) % π) - π/2`) would make the camera suddenly flip from look-up to look-down when the user mouses past the limit — wrong UX. Yaw wraps mod 2π (it's a full circle, no limits); pitch clamps (it's a half-circle with limits). The pitch smoke catches a wrong-formula regression because clamping produces a fixed ±π/2 ceiling that wrap doesn't.
- **1st-person pitch behavior**: `camera.rotation.x = -pitchRadians` in the 1st-person render branch. **Sign convention (verified, would burn hours if missed)**: Babylon's UniversalCamera in Y-up left-handed Euler convention uses positive `camera.rotation.x` to look DOWN. We want positive `pitchRadians` to look UP (user intent). So the assignment is `camera.rotation.x = -pitchRadians`. The pointer-lock-camera smoke's pitch assertions catch a wrong-sign regression.
- **Over-shoulder pitch behavior**: simplest correct version is also `camera.rotation.x += -pitchRadians` on the over-shoulder camera (on top of the `setTarget`-derived rotation). The camera is behind the character; tilting the camera up tilts the view up, matching the 1st-person behavior. The smoke verifies this. An alternative (more complex) approach is to shift the look-at target by `tan(pitch) * cameraDistance`, but the simple tilt is cleaner and matches FPS conventions.
- **Menu orbit pitch behavior**: no change. The orbit is a fixed-height auto-rotation around the character. Pitch is irrelevant in this state — applying pitch here would fight the orbit math. The chase camera's menu-orbit branch is untouched in PR 11.3.
- **Sensitivity reuse**: the existing `MOUSE_LOOK.sensitivityRadPerPixel = 0.0025` is reused for both `e.movementX` (yaw) and `e.movementY` (pitch). If Kyle wants separate pitch sensitivity later, that's a follow-up (5-line change in `characterConfig.ts`). No need to add `pitchSensitivityRadPerPixel` in this PR.
- **`forwardFromYawPitch(yaw, pitch)` returns a 3D unit vector** for tracer raycasts + melee cone checks. Formula: `(sin(yaw)*cos(pitch), sin(pitch), cos(yaw)*cos(pitch))`. The old `forwardFromYaw(yaw)` is removed entirely (both `dualPistolShoot` and `meleeSwing` now use the 3D version) — per the brief, "pick one and be consistent". Pre-PR-11.3 callers pass `pitch = 0` (level aim) via `input.pitchRadians ?? 0`.
- **Backward-compat shim**: `decodeInput` checks `pitchBits === 0` and returns `pitchRadians: 0` directly. This is the single line that keeps pre-PR-11.3 traffic (which has bytes 4-5 = 0) from temporarily defaulting to "look straight DOWN" during the upgrade window. Post-PR-11.3 clients always emit bits > 0 for any non-zero pitch.
- **Smoke uses DEV-only `__applyPitchDelta` accessor**, not `requestPointerLock()`. Headless Chromium doesn't reliably honor `requestPointerLock()` (user-activation requirements aren't met by synthetic clicks). The smoke drives the pitch via `window.__applyPitchDelta(deltaRadians)` which calls the same `chase.applyPitchDelta` code path the locked-mousemove listener uses. Same pattern as PR 11.1's `__applyYawDelta`. The smoke also drives the `__chaseCameraProbe()` to verify `cameraRotationX = -pitchRadians` is being honored in the render path.
- **Pitch smoke regression guard**: the smoke applies `-1.0` and `+2.0` deltas to push pitch past the limits, then asserts the final pitch is exactly ±π/2 (clamped) — NOT a wrapped value (which would be near ±π/2 from the other side). This catches a wrong-formula regression (wrap vs. clamp) that the linear tolerance test wouldn't catch.
- **Pitch wire-format smoke**: same shape as the yaw wire-format smoke. Round-trip 7 representative pitch values (0, 0.3, -0.5, π/4, ±π/2, just-inside-π/2) within 1.5 LSB. Also asserts the backward-compat shim (zero-byte packet decodes to pitchRadians = 0, NOT -π/2).
- **PR 11.2.3 `[PR-11.2.3-DEBUG]` instrumentation is still in the code**. Don't remove it — it's tabled as a known issue. Don't add `[PR-11.3-DEBUG]` unless we need to debug something; the existing logs cover pointer-lock behavior which PR 11.3 doesn't touch.
- **Bundle delta**: +2-3 kB raw / +0.5 kB gzip — wire constants (PITCH_BITS_SCALE, pitchToBits) + chase camera pitch state (pitchRadians + 4 accessors) + 2 new smokes (~13 kB total smoke code) + DEV probes. The DEV-only probes are tree-shaken out of production by Vite.
- **No codex+claude review loop used**. The brief was authored with the design decisions baked in (based on the PR 11.1 + 11.2 patterns + the HANDOFF's locked spec). The implementation is mechanical once the wire order is fixed. The honest gate is the smoke + the dev-box two-tab playtest.

- **Y-axis sign convention** (Kyle dev-box playtest 2026-08-15): initial PR 11.3 missed a sign flip on `e.movementY` in the input listener — the browser reports `movementY > 0` for mouse-DOWN, but FPS convention says mouse-DOWN should look DOWN, so we need `-movementY` to drive positive pitch = look up. Caught by Kyle's manual playtest on http://100.95.111.112:5173/ — fixed in the same PR follow-up. The chase camera's `camera.rotation.x = -pitchRadians` flip was correct (Babylon Y-up: positive rotation.x looks DOWN, we negate to make positive pitchRadians mean look UP). The smoke only verified the state accumulator, not the render direction, so it didn't catch this.

### 2026-08-15 — PR 11.4 implementation decisions (dev-box free-fly spectator camera)

- **`import.meta.env.DEV` is the only gate.** Every spectator code path is wrapped in `if (import.meta.env.DEV) { ... }`. Vite statically replaces `import.meta.env.DEV` with `false` in production; Rollup eliminates the dead branch. The F2 handler, the spectator camera class, the WASD pump, and the DEV probes are all DEV-only. The `SPECTATOR` config block in `characterConfig.ts` ships to production but is only read inside the DEV-gated paths — no behavior change in production. Verified: `grep '__spectator\|onSpectatorToggle' dist/assets/index-*.js` returns ZERO matches in the production bundle. (The "spectator" / "F2" substrings that DO appear in the production bundle are Babylon.js engine internals — `_spectatorCamera`, `_spectatorAfterRenderObservable`, React scheduler lane flags, keyboard-mapping tables — not our code.)
- **Second `UniversalCamera`, instantiated lazily.** The chase camera continues to exist and `update()` every frame (so its lerp doesn't go stale while spectator is active). The spectator is a SECOND Babylon camera, instantiated on the first F2 press, reused across subsequent F2 toggles (don't re-instantiate). `scene.activeCamera` swaps between the two on toggle.
- **Initial position on F2 enter = current chase camera world position.** Don't teleport. `chase.getCameraPosition()` accessor (new in this PR) returns the chase camera's current world-space position; the spectator reads it and sets itself there.
- **WASD absorbed via `gameSession.setSpectatorActive()` gate.** The cleanest place to gate is in `gameSession.tick()`: skip `localController.update(input)` if `spectator.active === true`. The input listener still fires (it's purely a key-state reader); the game session just doesn't act on it. **Do NOT** try to gate inside the input listener — that would break the held-state pattern and cause rising-edge flags to leak. The chase camera's `update()` continues to run every frame regardless of spectator state (otherwise the lerp resumes from a stale `tmpCurrent` and snaps).
- **Pitch CLAMP at ±π/2 (not wrap).** Same convention as the chase camera's `applyPitchDelta`. `Math.max(-HALF_PI, Math.min(HALF_PI, pitch + delta))`. The smoke's regression guard catches a wrong-formula regression (wrap would flip the view at the limits). Yaw wraps mod 2π (full circle, no limits); pitch clamps (half-circle with physical limits).
- **Held-right-click-drag for look.** Bind `mousedown`/`mousemove`/`mouseup` on `window` (not canvas — spectator is full-screen). Filter on `e.button === 2`. Suppress context menu globally on canvas (already done in inputListener for LMB/RMB; extends naturally to spectator drag).
- **Mouse sensitivity reuses `MOUSE_LOOK.sensitivityRadPerPixel`** (0.0025 rad/px) — same convention as PR 11.1's yaw and PR 11.3's pitch. If we ever want a separate spectator sensitivity, it's a 3-line change in `characterConfig.ts`. Not worth adding now.
- **Smoke-driven regression:** `client/tools/spectator-camera-smoke.mjs` (9 assertions: initial state, F2 enter preserves position within 0.5m, move-delta moves position, yaw-delta + mod-2π wrap, pitch CLAMP at both ±π/2, WASD absorbed while spectator active (character position unchanged after synthetic W keypress), toggle off reattaches, yaw preserved across off/on cycles). Drives the camera via DEV probes (Chromium doesn't grant real pointer-lock in headless). New `client-spectator-camera-smoke` CI job on port 5187 (next free after the 14 existing smoke ports). Console-error filter matches the project convention (`mouse-pitch-smoke.mjs`, `pointer-lock-camera-smoke.mjs`): filter out `WebGPU`, `Babylon`, `WebGL`, `GPU stall` — those are headless-environment noise, not real errors.
- **Smoke-fix gotchas caught during verification** (after codex was terminated mid-task): the initial smoke applied `-3.0` to pitch expecting to clamp at `-π/2`, but starting from `+π/2` (state after step (5)), `+π/2 - 3.0 = -1.4292` which is ABOVE `-π/2 = -1.5708` — no clamp fires. Fixed by applying `-5.0` instead (guaranteed to cross the boundary). Also: the smoke used the local `HALF_PI` constant inside `page.evaluate()` — but `page.evaluate` runs in the browser context where the smoke's local constants aren't visible. Fixed to use the literal `Math.PI / 2` inside the browser context.
- **No codex+claude review loop used.** The brief was authored with the design decisions baked in (based on the locked project conventions + the smoke-architecture pattern from prior PRs). The implementation is mechanical once the `import.meta.env.DEV` + DEV-probe + WASD-absorption patterns are pinned. The honest gate is the smoke + the dev-box playtest.
- **Bundle delta**: +2.71 kB raw / +0.5 kB gzip — spectatorCamera class (~150 lines) + DEV probes + WASD pump + smoke (~200 lines, DEV-only). Production bundle: zero spectator code.
- **Not in this PR (deliberately deferred)**: smooth interpolation between chase and spectator on F2 toggle (instant snap currently); configurable spectator speed (Shift sprint, Space up); camera collision (PhysicsRaycast for floor); spectator HUD overlay ("F2 to exit"); recording / replay from spectator.

### 2026-08-11 — Project location
- **Vault**: `~/Obsidian/mem/projects/specialists-web.md` (this file)
- **Repo**: `~/Development/specialists-web/`
- **Remote**: `github.com/klampatech/specialists-web`

### 2026-08-11 — Legal/IP stance
- Don't worry about it during prototyping. Revisit at public launch.
- Original IP, different name, no verbatim copy. Clean and safe approach.

---

## Known issues

Behavior that is shipped and reproducible but tabled as a follow-up — not blockers, but documented so the next session doesn't rediscover them cold.

### ESC-equals-resume flicker when pause menu is visible (PR 11.2 series, tabled 2026-08-15)

**Symptom**: After the first ESC has opened the pause menu (menu visible, cursor unlocked), pressing ESC again to resume briefly hides the menu then re-shows it — the camera orbits back to `menuAngle = 0` then continues rotating. The flicker resolves to a correct locked state if the user then presses ESC a third time, or if they wait ~1.5s before pressing ESC again, or if they click Resume from the UI.

**Workaround (ships with PR 11.2.3)**: Click the **Resume** button on the pause menu. This is the supported UX path. Resume routes through `onResume()` → React `<button>` `onClick` → `handle.setPointerLock(true)` → `canvas.requestPointerLock()` synchronously, and works reliably.

**Why this is tabled, not fixed**: PR 11.2 + 11.2.1 + 11.2.2 + 11.2.3 attempted four fix paths:
1. **PR 11.2** (initial): separate ESC listener in `inputListener.ts` calling `setPointerLock(false)`, plus a Resume button onClick. Flicker present.
2. **PR 11.2.1** (intermediate attempts): dual→single listener swap, `e.preventDefault()`, `canvas.focus()` before `requestPointerLock()`. Flicker still present.
3. **PR 11.2.2**: removed `inputListener.ts`'s ESC handler entirely; ESC handling consolidated into a single `useEffect` keydown listener inside `PauseMenu.tsx` registered only when `visible === true`. Single handler, single `requestPointerLock()` call, preserves user-activation through React's onClick path. Flicker still present.
4. **PR 11.2.3**: added `POINTER_LOCK_DEBOUNCE_MS = 150` in `chaseCamera.ts` (ignores direction-disagreeing `pointerlockchange` events within 150ms of the previous flip) and a synthetic `mousemove` (movementX=0, movementY=0) dispatched synchronously after `canvas.requestPointerLock()` succeeds (refreshes Chrome's "is the user still engaged" counter to prevent the 1.5s auto-release). Two distinct root-cause hypotheses were addressed; the actual root cause is still uncertain.

Kyle's 2026-08-15 playtest signal: the Resume button works correctly; the residual ESC-equals-resume flicker is acceptable to ship as a known issue and revisit later. **Fix attempts so far have not isolated the actual root cause** — the Bug B (lock-then-unlock race) and Bug A (Chrome 1.5s auto-release) framings from the debug-log trace are candidate hypotheses, not confirmed root causes. The 5-site `[PR-11.2.3-DEBUG]` instrumentation remains in place to support future investigation if/when this is reopened.

**Recommended follow-up when revisited**:
- Capture a fresh `[PR-11.2.3-DEBUG]` log of the actual event sequence (PauseMenu keydown → `onResume` → `setPointerLock(true)` → `requestPointerLock()` → `pointerlockchange` events) and compare against the patterns already documented in HANDOFF.md §"PR 11.2.3 root cause hypothesis".
- Consider whether the issue is `setPointerLock` being called twice (once from React `onClick`, once from a captured event listener) rather than a browser-side race.
- Consider whether `PauseMenu.tsx`'s `useEffect` cleanup ordering races with the lock-state flip — the cleanup runs `document.removeEventListener("keydown", ...)` synchronously, but `setPointerLock(true)` may have already triggered a `pointerlockchange` event queued on the event loop.

**Severity**: UX-papercut, not a blocker. Pause menu is fully usable via Resume button; ESC-equals-resume is a nice-to-have for muscle-memory parity.

---

## Open questions

- **Final game name** — pick at public launch. Need a memorable, search-friendly name that doesn't collide with "The Specialists" brand.
- **Mobile** — likely no for play. Companion app for stats/spectate is the only mobile play.
- **Voice chat provider** — Agora vs Daily vs self-hosted. Decide at Phase 4.
- **Account persistence** — Discord OAuth only, or also email/password? Decide at Phase 2.
- **Modding support** — defer to Phase 5. Architecture should leave room (scripting hooks, asset pipelines) but not design for it.

---

## Session log

### 2026-08-11 — PR 2 (scene baseline) shipped, spec drift caught and fixed
- Branched `feat/phase0-scene-baseline` off `main` (origin/main @ `05d960c` at handoff-time)
- Installed `@babylonjs/core@9.20.0` + `@babylonjs/havok@1.3.14` + `playwright@1.62.1` (devDep)
- `client/src/engine/scene.ts` — first real Babylon scene (Engine + Scene + ArcRotateCamera + HemisphericLight + DirectionalLight + skydome + red sphere + 30x30 ground + Havok + static rigid bodies). PR 2 scope per Milestone 1 acceptance rows 1-3.
- `client/src/ui/App.tsx` — replaced React banner with Babylon canvas mounted via ref. Loading / error / ready overlays.
- `client/vite.config.ts` — added `optimizeDeps.exclude: ["@babylonjs/havok"]` to fix the dev-mode wasm load (Havok's `import.meta.url` was being rewritten by Vite's pre-bundling)
- `.github/workflows/ci.yml` — new `client-scene-smoke` job (boots dev server, runs Playwright headless, fails on pageerror, uploads screenshot as artifact)
- `client/tools/scene-smoke.mjs` — headless smoke test script
- `docs/pr2-screenshot.png` — the lit-scene screenshot (in-repo evidence)
- PR opened: https://github.com/klampatech/specialists-web/pull/3 — all 3 CI checks green (typecheck + build, scene smoke, spec-canonical)
- **Spec drift caught by Kyle post-merge**: PR 2 didn't ship a spec update. Fixed here: pinned versions, recorded WebGL2-vs-WebGPU decision, recorded Vite gotcha, recorded CI evolution, realigned the Milestone 1 acceptance table with the actual PR split, recorded the 3-PR Phase 0 split. Spec also synced to vault via `tools/sync-spec-to-vault.sh` (next step).
- **Lesson learned**: every PR must land with the spec updated. The spec is the load-bearing artifact for this project; PRs without spec updates rot the next session's understanding. Add this to the session-end checklist.

### 2026-08-14 — PR 11.2.2 single-handler ESC architecture shipped, but root-cause still open
- PR 11.2.2 (`070df25`) on `feat/phase0-pr11.2-pause-menu`: removed `inputListener.ts`'s entire ESC handler chain (the `onEscapePressed` hook + the `if (key === "Escape" && !e.repeat)` block in `onKeyDown` + the `e.preventDefault()` call). Moved ESC handling to a single `useEffect` keydown listener inside `PauseMenu.tsx` that only registers when `visible === true`. The handler calls `onResume()` which routes through the React `<button>` `onClick` chain → `handle.setPointerLock(true)` → `canvas.requestPointerLock()`.
- All 11 CI checks green on PR #18. 9 smokes green locally.
- **Playtest ⚠️ UNVERIFIED**: Kyle reported the same flicker on PR 11.2.2 as on PR 11.2.1. The single-handler architecture is not the root cause. Next: PR 11.2.3 with `console.log` instrumentation to capture the actual sequence of `pointerlockchange` events + `chase.setPointerLock` calls + keydown handler firings in browser DevTools.
- Three candidate root-cause hypotheses were investigated in PR 11.2.3: (a) Chrome's user-activation policy revokes after first synchronous tick, (b) browser fires `pointerlockchange(true)` followed immediately by `pointerlockchange(false)` from a queued exit, (c) canvas-lost-focus interaction with the menu-visible keydown activation. None confirmed. Per Kyle's 2026-08-15 call this was tabled as a known issue (see Known Issues section); the Resume button is the supported UX path.
- Subagent used: Claude Code on herdr dispatched with delegation_id `deleg_97ffbe1c` (212.32s, 23 tool calls) — landed the single-handler refactor cleanly.

### 2026-08-11 — kickoff
- Kyle: "I'd like to essentially bring this version of the specialists into the browser. It must be multiplayer and available to anyone on the web to play."
- Pulled ModDB page, Fandom wiki, scouted WebGPU/netcode state of the art in 2026.
- Evan ended on: "Document the *vision* and the *plan*, then start building."
- Kyle: "I want a canonical living spec to grow alongside this project and track our work. So write the phased MVP plan and then we can fill in the details for phase 0."
- Created this vault doc + repo + remote. Next session: fill in Phase 0 details.
- Spec also checked into repo as `SPEC.md` (synced from vault). `HANDOFF.md` added for session-to-session continuity.
- **Operating principle added**: "Playtest everything. Handing Kyle a broken game is unacceptable." Each milestone ends with a playable build. Each session-end handoff has a mandatory Playtest status block. Phase 0 milestones now have playtest acceptance criteria.
- Kyle will start Phase 0 in the next session.

### 2026-08-15 — PR 11.5 implementation decisions (gap-bridging rollback cap in LockstepRuntime)

- **Reuse `MAX_PREDICTION_FRAMES`, don't introduce a new constant.** PR 4 added `MAX_PREDICTION_FRAMES = 8` with the comment that it was "informational — the simulation never blocks on it." PR 11.5 makes it load-bearing. The re-export `ROLLBACK_CAP_FRAMES = MAX_PREDICTION_FRAMES` exists so callers (the smoke, the future HUD chip) can read the constant under its semantic name; the value is unchanged. 8 frames @ 60Hz ≈ 133ms lookahead budget — enough for LAN + low-loss WAN, tight enough to catch heavy-packet-loss cases before they cause drift.
- **Cap math uses the `predictionDepth` formula, not raw frame delta.** `aheadBy = Math.max(0, this.localFrame - 1 - this.highestRemoteFrameSeen)` is the same expression the existing `predictionDepth` getter uses. The naive `localFrame - highestRemoteFrameSeen` is off by one at startup (`localFrame=0, highestRemoteFrameSeen=-1 → raw=1, predictionDepth=0`) AND would trip the cap on the 8th advance (`localFrame=7, predictionDepth=7, raw=8 → raw >= 8 ✓`). Using the getter formula keeps the cap and the visualization in sync and avoids a handshake-window self-pause. Verified: smoke asserts 8 within-cap advances succeed (localFrame 0→8 with no peer input), 5 over-cap advances pause (localFrame stays at 8), 1 caught-up advance resumes (localFrame → 9).
- **Encode + submit fires BEFORE `advanceFrame()`.** The wire packet goes out every tick, including paused ticks. Without this, the peer can't catch up (both clients are stuck at the same `localFrame` and neither side sends past it). The `submitLocalInput` call in `gameSession.tick()` is BEFORE the new `if (advanced.paused) return` early-return; the wire packet is on the wire by the time we know whether the cap fired.
- **`paused` is a flag on `AdvancedFrame`, not a separate method return.** Mirrors how `remoteConfirmed` already lives on the same struct — one place to look for "what happened this tick." Pre-PR-11.5 callers ignore the field (TypeScript doesn't force them to read it because adding a new optional field to a returned object is non-breaking). New code checks `if (advanced.paused)` once at the top of `tick()`.
- **Rising-edge combat trackers are NOT updated on a paused tick.** When the cap fires, `tick()` returns BEFORE `wasFiring = gameInput.fireHeld` and `wasMelee = input.meleePressed` are updated. This is correct because: (a) `gameSession.tick` runs identically on both clients in lockstep — both hit the cap on the same frames and skip the same combat checks; (b) on the next non-paused tick, the rising-edge check (`gameInput.fireHeld && !wasFiring`) sees the pre-pause tracker value, so a key held across the pause still hits the rising edge the next non-paused tick. **Honest limitation**: a rising edge that lands EXACTLY on a paused frame is lost (both clients skip it symmetrically — no de-sync, but the input is dropped). This is tabled as a known issue for HUD observability; real rollback would catch it via re-simulation, but the cap design explicitly doesn't.
- **Real `ggrs`/wasm rollback is OUT OF SCOPE.** The `LockstepRuntime` class shape (`submitLocalInput` / `advanceFrame` / `frame` / `latestConfirmedFrame` / `dispose`) is preserved so a future swap to ggrs is a class swap, not a caller-site rewrite. The `paused: true` sentinel is a SHAPE-compatible addition (new field on the existing struct), so even when ggrs lands the caller-side change is small.
- **HUD wiring is OUT OF SCOPE.** `runtime` exposes `pausedFrames` + `totalPausedFrameCount` so a future "PAUSED" chip can be added without touching the runtime. The `GameSession` interface mirrors them (`pausedFrames`, `totalPausedFrameCount` getters) but the React `<BulletHud>` doesn't render them yet. See Next list: PR 11.X (HUD paused-frames chip) candidate.
- **Smoke is single-context with a synthetic transport stub.** Mirrors `yaw-wire-format-smoke.mjs` + `pitch-wire-format-smoke.mjs` — imports the module via Vite, drives the public API directly with an in-memory transport (avoids the WebRTC + TURN headless problem documented in `two-tab-smoke.mjs`'s comment). The stub captures inbound packets + counts outbound sends, so the smoke can assert "8 within-cap + 5 paused + 1 caught-up = 14 sent packets" without a real peer.
- **Cap math is gated on `highestRemoteFrameSeen >= 0`** (CI-evolved correction, added 2026-08-15 after the deterministic 5-of-16 CI failure). The `predictionDepth` formula alone is insufficient: `max(0, localFrame - 1 - highestRemoteFrameSeen)` with `highestRemoteFrameSeen = -1` resolves to `localFrame` for any `localFrame > 0`, so a solo browser with no peer self-pauses at advance #8 (`localFrame=8 → aheadBy=8 ≥ 8`). Without a peer the runtime has no notion of "falling behind" — `predictionDepth` should conceptually be 0 (tied with the nothing-yet-sent state) and the cap should be a no-op. The fix is a 1-line gate: `if (this.highestRemoteFrameSeen >= 0 && aheadBy >= ROLLBACK_CAP_FRAMES)`. **Diagnostic**: the regression manifested deterministically across 3 consecutive CI re-runs — 5 of 16 smokes regressed (jump, wallrun, health, two-tab, pointer-lock); all 5 are smokes that drive the character via `tick()` (which calls `advanceFrame()`). The 7 untouched smokes (scene, yaw-wire, pitch-wire, pitch-2tab, lockstep-rollback, mouse-look, mouse-pitch, pause-menu, typecheck, build, spec-canonical) don't drive `tick()` in the way that reaches the cap on a solo browser. The pattern was an exact signature of "cap self-firing on a solo browser." Local-only smoke verification CANNOT catch this bug — it requires either a two-tab smoke (which headless can't do — TURN) or the full CI regression suite. Lesson: any cap that depends on peer state MUST be gated on "the peer has actually sent us something" — otherwise the math behaves well in unit tests but breaks the moment any solo consumer exists.
- **Bundle delta: +1.06 kB raw (small, no new chunks).** The runtime additions (160 lines) + the gameSession additions (88 lines) + the DEV probe (24 lines) all compile into the existing `index-*.js` chunk. Production bundle grep `__lockstep\|ROLLBACK_CAP_FRAMES` returns ZERO matches — the DEV probe is tree-shaken, and `ROLLBACK_CAP_FRAMES` alias is unused in production paths (only referenced by the smoke via Vite's dev server). The names `isPaused` / `pausedFrames` / `totalPausedFrameCount` DO appear in production (9/9/3 matches) because the runtime class IS shipped — they're the names of the public getters and they exist alongside the lockstep engine regardless of whether the cap path ever fires. This is correct: production SHOULD pause on WAN drop.
- **Codex partial completion, Evo re-verification as the standing gate.** Codex edited `ggrsRuntime.ts` + `gameSession.ts` + `scene.ts` and wrote the smoke, but was killed mid-task before running the verification gates or writing docs/CI. Evo re-verified from the worktree state: typecheck (clean), production build (2m 7s, exit 0), production-bundle grep (no `__lockstep` matches, +1.06 kB), smoke (7 assertions all green), and hand-traced the cap math against the actual code. The smoke's 1-tick-off values were caught and fixed during re-verification. **Pattern**: when codex is killed mid-task, treat the partial output as a draft and re-run ALL verification gates from the worktree.
- **Codex runtime execution: one-shot `codex --yolo exec -o <file>` via herdr workspace+agent-start recipe.** The interactive `codex --yolo` recipe failed with "stdin is not a terminal" on this host (herdr version). The one-shot `codex exec` recipe worked cleanly via the same workspace + agent-start machinery. Switched from interactive to one-shot mid-task. Lesson: on hosts where herdr's TTY provision is broken, fall back to one-shot `codex --yolo exec` for codex dispatches — the herdr recipe is identical otherwise (workspace create, agent start with full-path wrapper, HERDR_* env vars in the wrapper).
