# PR 11.6 — Server-authoritative damage — Plan

> **Status: PLAN-DRAFT. Not approved for implementation.** This is the
> architecture decision record for Phase 1 / PR 11.6. It is the deliverable
> for this session — opening as a docs PR for review before any Rust code,
> any Vite churn, or any codex dispatch.
>
> **Owner**: kyle + Evo
> **Branch**: `docs/pr11.6-plan` (this PR)
> **Target merge**: review-only — no merge until the open questions below
> have a call.
> **Roll-out sequence**: PR 11.6.A (this plan PR) → 11.6.B (server
> scaffold) → 11.6.C (WebTransport transport) → 11.6.D (server-auth
> damage broadcast) → 11.6.E (verification + dev-box two-tab WAN-throttle
> smoke) — each independently mergeable, with progressively bigger
> orchestration budget.

---

## 1. Why this PR is the first thing post-M2

`docs/SPEC.md` §"Next" names PR 11.6 as the first entry under "Phase 1
work — server-authoritative networking starts here." Phase 0 / M2 is closed
at `5bc43f3` (PR #27 / PR 11.5 — gap-bridging rollback cap — MERGED).
PR 11.5 ships local-side pause-when-too-far-behind. PR 11.6 is what makes
that useful: **damage application moves off the lockstep local path and
onto a server** so it survives WAN latency instead of diverging per-client.

This is the **first Phase 1 work** in this codebase. `server/` and
`protocol/` are empty `.gitkeep` placeholders. The web-cookie of Phase 0
(lockstep + WebRTC + per-client bullet-time) doesn't break — PR 11.6
adds the server-side authority for *damage specifically* and leaves
movement + bullet-time + visuals on the existing P2P substrate. Movement
authoritization is a later PR (probably `PR 11.7` or later, after we've
proven damage can survive WAN).

---

## 2. What "server-authoritative damage" actually means

### 2.1 Current damage flow (pre-PR-11.6, in `client/src/game/gameSession.ts` lines 374–425)

Both clients run the **same** simulation with the **same** inputs (per
`client/src/net/ggrsRuntime.ts`'s deterministic-lockstep contract).
When local rises to fire:

1. `dualPistolShoot(input, local, remote, scene)` runs on Tab A — raycasts
   from A's chest forward, finds B's capsule, returns `{hit: true,
   damage: 12}`.
2. `applyDamage(remoteController, {source:"fire", amount:12}, nowMs)` runs
   on Tab A. A's view of B's HP drops by 12.
3. `applyDamage(localController, {source:"fire", amount:12}, nowMs)`
   runs **symmetrically** on Tab B (the peer's rising edge of fire). B's
   view of B's own HP drops by 12.
4. Both clients arrive at identical HP values because the inputs were
   identical and the damage math is a deterministic function of inputs.

This works under LAN / low-loss. **It breaks under WAN loss / jitter**
because the rising-edge can land on slightly different frames on each
client (Phase 0's `~70s no-rollback` documented limitation). Tab A's
"damage landed" frame and Tab B's "damage landed" frame can disagree →
HP diverges per-client.

### 2.2 PR 11.6's new flow

```
┌──────────────┐  DamageRequest        ┌──────────────┐  DamageBroadcast  ┌──────────────┐
│  Tab A (host)│ ────{frame,src,tgt,  │              │ ──{frame,src,tgt, │  Tab B (peer)│
│  client-side  │     amount,nonce}─▶ │   Game       │     amount,nonce,  │  client-side │
│  GameSession │                       │   Server     │     authority}──▶ │  GameSession │
│  (local node) │ ◀──{frame,confirmed}─ │ (Tokio +     │                    │  (peer node) │
└──────────────┘                       │  WebTransport│                    └──────────────┘
                                       │   relay)     │
                                       │              │   ...also broadcasts back to A so A's
                                       │              │   tab applies it via the same path.
                                       └──────────────┘
```

Sequence per damage event:

1. Tab A's `GameSession.tick` calls `dualPistolShoot` (or `meleeSwing`)
   — same code as today. It now returns a `DamageEvent` (already a
   partial struct today: `{hit, damage, source, ...}`) plus a NEW
   `eventId: u32` (counter).
2. Tab A sends `DamageRequest {frame, source, targetPlayerId, amount,
   eventId}` to the server. Tab A does **not** apply damage locally.
3. The server validates: player exists, source is `fire|melee`, amount
   is in `[0, MAX_DAMAGE]`, target ≠ source, frame-monotonic.
4. The server assigns the **canonical frame number** (its own
   server-frame clock, not the tab's local frame), tags it with a
   server-issued monotonic `serverSeq`, and broadcasts `DamageBroadcast`
   to all players in the match (both A and B).
5. Both tabs apply the damage on their local controllers on
   receive — driven by `frame`-aligned entry, not by their own tick
   counter. Identical results ⇒ HP stays symmetric.

### 2.3 Why this works without breaking lockstep

- **Damage is no longer a function of either client's input** — it's a
  function of the server's broadcast order, which is the same for both
  tabs. Both tabs apply the same broadcasts in the same order.
- **Movement, bullet-time, visual stay in the lockstep substrate.**
  PR 11.6 doesn't touch `gameSession.tick`'s physics path; it inserts a
  `serverDamageQueue` that overrides `applyDamage(...)` on receive.
- **Wire-format addition only.** Two new packet types
  (`DamageRequest` + `DamageBroadcast`) on a separate channel from the
  `inputs` lockstep channel. Bytes-on-wire budget: `DamageRequest` ≈
  9 bytes (frame u32 + source u8 + playerId u16 + amount u8 + eventId
  u32). Trivial.

### 2.4 Determinism contract (preserved + extended)

**Pre-PR-11.6 rule (still applies for movement/combat visuals):**
*Every physics-affecting input is a frame-aligned integer. Never read
`Date.now()` inside the tick.*

**PR 11.6 addition (damage-specific):**
*The HP pool's value, on each client, equals the sum of
`DamageBroadcast.amount` values applied, in receive order, with the
starter HP of 100. Both clients receive identical broadcasts in
identical order ⇒ HP converges without any per-tick timestamping
needed.*

Edge case (called out explicitly): if a `DamageBroadcast` arrives
after the local client's respawn cycle resets HP to 100, the damage
applies to the new HP pool — no special-case code, the math just
falls out because `applyDamage` clamps at 0 and re-arms the respawn
timer when HP crosses 0 (same code as pre-PR-11.6).

---

## 3. Architecture

### 3.1 Repository structure (after PR 11.6 lands)

```
~/Development/specialists-web/
├── client/                  (existing — TS + Vite + Babylon + Havok)
│   └── src/net/
│       ├── ggrsRuntime.ts   (existing — lockstep, unchanged)
│       ├── peer.ts          (existing — WebRTC, unchanged for now)
│       ├── ggnet.ts         (existing — 2-method transport interface)
│       ├── serverTransport.ts   (NEW — wraps WebTransport-to-server client)
│       └── damageBus.ts     (NEW — server-auth damage broadcast consumer)
├── server/                  (NEW — first real Rust code in this repo)
│   ├── Cargo.toml
│   ├── src/
│   │   ├── main.rs          (Tokio + WebTransport bootstrap)
│   │   ├── session.rs       (per-room state: players, HP, damage log)
│   │   ├── transport.rs     (WebTransport server + WebSocket fallback)
│   │   └── damage_relay.rs  (validate + re-broadcast incoming DamageRequest)
│   └── tests/
│       └── damage_relay.rs  (unit + integration tests)
├── protocol/                (NEW — shared types)
│   ├── damage.proto         (or .ts + .rs — wire format spec)
│   └── constants.ts/.rs     (PLAYER_MAX_HP, MAX_DAMAGE, FRAME_RATE)
├── tools/                   (existing — sync-spec-to-vault)
└── docs/                    (existing — SPEC.md, HANDOFF.md, this PR file)
```

### 3.2 Why `Cargo.toml` not a workspace

`client/` is a separate package universe (npm, React, TS). `server/`
doesn't share any code with it — they're connected only by the wire
protocol in `protocol/`. A workspace adds ceremony (root
`Cargo.toml`, `[workspace.dependencies]`) for a single-crate server.
Single-crate `Cargo.toml` is the right grain until Phase 1 adds a
matchmaker (which would be the second crate).

### 3.3 WebTransport vs WebSocket for the client → server transport

Two implementation paths for the PR 11.6 rollout:

| Concern | WebTransport | WebSocket |
|---|---|---|
| Transport semantics | UDP-over-HTTP/3 | TCP |
| Head-of-line blocking | None (QUIC streams) | Yes |
| Browser support (Aug 2026) | Chrome, Edge, FF nightly | Universal |
| Rust crate | `wtransport` (0.x, mature) | `tokio-tungstenite` (mature) |
| Spec match to docs/SPEC §"Stack" | Yes | Degraded |
| Initial implementation cost | Higher (cert handling) | Lower |

**Spec-aligned recommendation: WebTransport for production,
WebSocket fallback for restricted networks (matches `docs/SPEC.md` §"The
stack" verbatim).** However — for **PR 11.6 specifically**, the
WebSocket path is the recommended *first* implementation, behind a
feature flag:

- WebSocket first because it has universal browser support (no cert
  chain rigamarole for local dev), mature `tokio-tungstenite`, and the
  damage-broadcast flow is a control-channel pattern that doesn't need
  UDP's no-head-of-line guarantee.
- WebTransport layered on top in a follow-up PR once the damage
  pipeline is proven. Likely `PR 11.6.B.2` or `PR 11.7`.

**Rationale for the deferral**: PR 11.6's acceptance test is "damage
applied identically on both tabs after a 200ms throttle." WebSocket
gets us to that acceptance test 1-2 sessions faster than WebTransport.
The spec's WebTransport preference is preserved as a follow-up.

**This is open question #1.** Calling it WebSocket-first isn't
contradicting the spec — it's implementing the spec's "WebSocket
fallback for restricted networks" path first and treating WebTransport
as the production-hardening layer that comes later.

### 3.4 Server architecture (single Tokio binary, room-per-connection)

```rust
// server/src/main.rs (sketch — not for merge, for architecture only)

#[tokio::main]
async fn main() -> anyhow::Result<()> {
    // One TCP listener for WebSocket (Phase 1 §3.3 fallback path).
    let ws_listener = TcpListener::bind("0.0.0.0:4433").await?;
    tokio::spawn(async move { accept_websocket_loop(ws_listener).await });

    // One HTTP/3 listener for WebTransport (deferred to §3.3 follow-up).
    // TODO(PR 11.6.B.2): wire when WebTransport path is built.

    // Shared room registry — currently in-process; Redis-backed rooms
    // are a Phase 1 matchmaker concern, deferred.
    let rooms = Arc::new(RwLock::new(HashMap::<RoomId, Room>::new()));
    loop { /* … */ }
}
```

Rooms are mapped by URL fragment (`ws://server/rooms/<roomId>`). For
PR 11.6, roomId is a 4-byte u32 random per connection (no signup, no
matchmaker). The client's `PeerOverlay` URL gets `?server=ws://localhost:4433`
appended and the WebSocket opens a connection that becomes a room of one
(the joining player's matchmaking is the entire PR 11.6's "reach server
→ same room as peer" issue; for the dev-box smoke both tabs share the
same hard-coded `roomId = "DEVBX"` until matchmaker exists).

### 3.5 Wire protocol (`protocol/damage.proto` — TypeScript + Rust hand-translated, no codegen)

```typescript
// protocol/damage.ts

/** Tab → Server. "I think this damage happened." */
export interface DamageRequest {
  /** Server-side frame the tab believes this aligns with (informational;
   *  server uses its own frame clock for the broadcast tag). */
  frame: number;
  /** What tab ID is making the claim. */
  sourcePlayerId: u16;
  /** What tab ID is being hit. Must differ from source. */
  targetPlayerId: u16;
  /** Source type — only "fire" and "melee" for PR 11.6. */
  source: 0 | 1; // 0 = fire, 1 = melee
  /** Damage amount. Server clamps to [0, MAX_DAMAGE = 100]. */
  amount: u8;
  /** Tab-issued monotonic counter so the server can dedupe retransmits. */
  eventId: u32;
}

/** Server → all tabs in the room. "This damage is canonical." */
export interface DamageBroadcast {
  /** Server-frame number (server clock). */
  serverFrame: number;
  /** Monotonically-increasing server sequence; tabs ignore out-of-order
   *  broadcasts with this seq. */
  serverSeq: number;
  sourcePlayerId: u16;
  targetPlayerId: u16;
  source: 0 | 1;
  amount: u8;
  /** Original DamageRequest's eventId (for tab-side deduplication if it
   *  ever receives its own request echoed back). */
  originEventId: u32;
}
```

Wire-encoding shape (matches the existing 1-byte alignment used by
`client/src/net/inputBitmask.ts`):

```
DamageRequest on the wire (8 bytes):
  byte 0..3   frame (u32 BE)
  byte 4..5   sourcePlayerId (u16 BE)
  byte 6..7   targetPlayerId (u16 BE)
  byte 8      source (u8 — 0 fire / 1 melee)
  byte 9      amount (u8)
  byte 10..13 eventId (u32 BE)

DamageBroadcast on the wire (13 bytes):
  byte 0..3   serverFrame (u32 BE)
  byte 4..7   serverSeq (u32 BE)
  byte 8..9   sourcePlayerId (u16 BE)
  byte 10..11 targetPlayerId (u16 BE)
  byte 12     source (u8)
  byte 13     amount (u8)
  byte 14..17 originEventId (u32 BE)
```

**Bytes are aligned to BIG-ENDIAN to match `ggrsRuntime.ts`'s existing
`big-endian uint32` for the frame header.** No codegen; the same byte
layout is hand-translated in TypeScript (`protocol/damage.ts`) and Rust
(`protocol/damage.rs`). Three assertions in `server/tests/damage_wire.rs`
cover the symmetric encode/decode path.

### 3.6 The `GgnetTransport` interface stays — new `ServerTransport` is a sibling

`client/src/net/ggnet.ts` is currently:

```typescript
export class GgnetTransport {
  constructor(private peer: WebRTCPeer) {}
  send(p: Uint8Array) { this.peer.send("inputs", p) }
  onPacket(f: (p: Uint8Array) => void) { this.peer.on("packet", f) }
}
```

PR 11.6 changes `ggnet.ts` to:

```typescript
/** 4-channel abstraction: inputs (existing P2P), damageRequests (NEW), damageBroadcasts (NEW), state (existing P2P, unchanged). */
export interface GameTransport {
  // Existing P2P shape — unchanged.
  sendInputs(p: Uint8Array): void;
  onInputs(f: (p: Uint8Array) => void): void;
  // NEW — server-auth damage round-trip.
  sendDamageRequest(p: Uint8Array): void;
  onDamageBroadcast(f: (p: Uint8Array) => void): void;
  /** Fired when the underlying transport errors out — UI uses this to
   *  show "disconnected" in the HUD. */
  onDisconnect(f: () => void): void;
}
```

Two implementations:

1. **`P2PGgnetTransport`** (existing — wraps `WebRTCPeer`, ignores the
   new methods → throw "not implemented in Phase 0"). Used for the
   legacy signal-only demo path during the migration window.
2. **`ServerGgnetTransport`** (NEW — wraps a WebSocket connection +
   multiplexes `inputs` and `damage*` onto separate binary message
   types on the same socket using the first byte as a discriminator).

The discriminator format on the wire is **opaque to the server**:
bytes 0 == 0x00 → inputs (forwarded to lockstep unchanged), byte 0
== 0x01 → damageRequest, byte 0 == 0x02 → damageBroadcast. Lengths
are unchanged from the existing INPUT_SIZE/PACKET_SIZE conventions.

**This swap is the load-bearing architectural decision of PR 11.6.** The
existing 14 smokes must continue to pass against `P2PGgnetTransport`
(default-untouched path), AND a new smoke must pass against
`ServerGgnetTransport` + the actual server in `server/`. Both
implementations live side-by-side in `ggnet.ts`.

### 3.7 What does NOT change in PR 11.6

- `client/src/net/ggrsRuntime.ts` — unchanged. The lockstep substrate
  for movement / bullet-time / combat visuals continues to work.
- `client/src/game/combat.ts` — `dualPistolShoot` and `meleeSwing`
  return shapes unchanged (they already return `{hit, damage, source}`).
- `client/src/game/health.ts` — `applyDamage` and `tickRespawn`
  unchanged. The caller (`gameSession.tick`) instead applies a
  `serverDamageQueue.pop()` value instead of the local-computed
  damage.
- `client/src/engine/*` — everything physics-engine-related unchanged.
- `server/protocol/*` — the Rust code is the first Rust in the repo.
  Same wire protocol = server must encode/decode the same byte shape.
- `client/vite.config.ts`, the Vite + Vite-plugin stack — unchanged.
  PR 11.6 doesn't add new dev-server wiring.

### 3.8 What does change

| File | Change | Scope |
|---|---|---|
| `client/src/net/ggnet.ts` | Add `GameTransport` interface + `ServerGgnetTransport` impl. Keep `GgnetTransport` (P2P) backward-compatible. | ~150 lines |
| `client/src/net/serverTransport.ts` (NEW) | `ServerTransport` class: opens WebSocket to `ws://server:4433/rooms/{roomId}` + multiplexes inputs/damage. | ~80 lines |
| `client/src/net/damageBus.ts` (NEW) | Re-export typed `encodeDamageRequest`, `decodeDamageBroadcast`, the queue, and the "apply-on-receive" caller-side. | ~100 lines |
| `client/src/game/gameSession.ts` | Replace the two `applyDamage` call sites (lines 377, 393, 414, 424) with `damageBus.queue(event)`. Local `applyDamage` body moves into a `damageBus.applyBroadcast(bc, localNowMs)` method called on receive. | ~30 lines delta |
| `client/src/game/health.ts` | Unchanged (respawn timer + applyDamage body carry over). | 0 lines |
| `client/src/ui/PeerOverlay.tsx` | New `?server=ws://server:4433` URL param routes to `ServerTransport` instead of WebRTC. | ~30 lines delta |
| `client/tools/*-smoke.mjs` | New `client-damage-server-smoke.mjs` (port 5190) that spawns the Rust binary, opens two tabs against it, fires a tracer round, asserts both tabs' HP converge to the same value. | ~200 lines |
| `.github/workflows/ci.yml` | New `server-build` job (`cargo build --manifest-path server/Cargo.toml --locked` + `cargo test`) + new `client-damage-server-smoke` job (port 5190). | ~100 lines delta |
| `server/Cargo.toml` (NEW) | Single-crate Cargo.toml with `tokio`, `tokio-tungstenite`, `bytes`, `serde`, `anyhow`, `tracing`. Locked-versions in `Cargo.lock`. | ~30 lines |
| `server/src/main.rs` (NEW) | Tokio bootstrap + accept loop + room registry. | ~80 lines |
| `server/src/session.rs` (NEW) | `Room` struct: `players: Vec<Player>`, `damage_log: Vec<DamageBroadcast>`, `next_server_seq: u64`. | ~120 lines |
| `server/src/damage_relay.rs` (NEW) | `validate_and_relay(req) -> Option<DamageBroadcast>` — checks source≠target, amount∈[0,100], eventId monotonic, source type valid. | ~80 lines |
| `server/tests/damage_relay.rs` (NEW) | Unit tests for validation; integration test for full round-trip (mock WebSocket server fixture). | ~150 lines |
| `protocol/damage.ts` + `protocol/damage.rs` (NEW) | Hand-translated wire types + encode/decode fns. | ~80 lines each |
| `tools/canary-server.sh` (NEW) | Spawns the Rust binary in the background, captures stdout+stderr, exposes for smoke. | ~40 lines |
| `docs/SPEC.md` | Updated §"Next" → "Phase 1: PR 11.6 IN PROGRESS", add PR 11.6 sub-PR table (see §5 below). Status banner updates per merge. | ~50 lines |
| `HANDOFF.md` | Per-PR entries with the standard Phase 0 format (file-by-file shipped surface, verification gates, lessons learned). | ~150 lines/PR |

That's ~1,500 lines net new code spread over 5 sub-PRs. **No PR
introduces more than ~500 lines.**

---

## 4. Verification

### 4.1 Test layers (carrying forward from Phase 0's three-tier gate)

1. **Server unit tests** (`server/tests/damage_relay.rs`) — pure
   deterministic, no I/O. Round-trip wire encode/decode + validation
   edge cases (source == target, amount > MAX, etc.).
2. **Client smoke** (`client/tools/damage-server-smoke.mjs`, port 5190)
   — boots `canary-server.sh`, boots a vite dev server on port 5190,
   opens two headless tabs, drives the WebRTC handshake so
   `ServerTransport` opens a WebSocket to `localhost:4433` with the
   shared `roomId = "DEVBX"`, fires tracer + melee damage events,
   asserts both tabs' HP converge to identical values + both tabs'
   `applyDamage` was called from a `DamageBroadcast` (not from
   local).
3. **Dev-box two-tab WAN-throttle** — Kyle's real-browser test on
   http://100.95.111.112:5173/. **Carries PR 11.5's debt** (the 6-step
   WAN-throttle checklist in `HANDOFF.md` §2026-08-15 — PR 11.5 must be
   verified at the same time the server is introduced).

### 4.2 What "green" means

- Server unit tests: 100% pass
- Typecheck: server + client clean
- Build: `cargo build --release` clean, `npm run build` clean
- All 14 existing client smokes still pass against `P2PGgnetTransport`
  (no regressions)
- New `client-damage-server-smoke` smoke: passes against `ServerTransport` + the canary server (verifies HP convergence + the "damage applied via broadcast, not local" property)
- Dev-box two-tab WAN-throttle 200ms: damage applied identically on
  both tabs; PR 11.5 cap behavior verified at the same time

### 4.3 What "green" does NOT prove (carry-over honesty)

- Anti-cheat. PR 11.6's server-authority boundary is the foundation;
  *actual* cheat detection (impossible movement rate, statistical
  anomalies) is Phase 4.
- Reconnect handling. The simplest "graceful" disconnect: drop the
  player, fire `playerLeft` to remaining tabs. Reconnection = a
  separate PR.
- >2 players. Rooms are designed for >2 in the data model, but
  verification is 2-player until PR 11.6.D.
- WebTransport — that's the WebSocket-follow-up PR (§3.3).

---

## 5. Sub-PR roll-out sequence

This is **what each subsequent codex dispatch will target**. Each
sub-PR is independently reviewable and mergeable. No sub-PR depends
on the user extending state with another sub-PR's content (the
"stacked-PPR" anti-pattern from PR 10).

| Sub-PR | Title | Files | Sessions est. | Acceptance |
|---|---|---|---|---|
| 11.6.A | **This plan** | `docs/PR-11.6-plan.md` + `SPEC.md` status update | 0.5 (this session) | Plan reviewed and merged |
| 11.6.B | **Server scaffold** (Tokio + WebSocket + room registry + `canary-server.sh`) | `server/Cargo.toml`, `server/src/main.rs`, `server/src/session.rs`, `tools/canary-server.sh`, `.github/workflows/ci.yml` (`server-build` job) | 1 | `cargo build --release` + `cargo test` (unit-only) + `canary-server.sh` boots, accepts a connection, echoes back the bytes |
| 11.6.C | **Wire protocol + transport mux** (`GameTransport` interface + `ServerGgnetTransport` + `P2PGgnetTransport` backward-compat + `protocol/damage.{ts,rs}` encode/decode) | `client/src/net/ggnet.ts`, `client/src/net/serverTransport.ts`, `client/src/net/damageBus.ts`, `protocol/damage.ts` + `.rs`, server-side wire codecs, new server unit tests | 1 | All 14 existing smokes still pass; `ServerTransport.connect()` opens + receives a malformed-but-recognised packet without crashing |
| 11.6.D | **Server-auth damage end-to-end** (caller-side `damageBus.queue(event)` replace + UI `?server=` URL parameter routing + `damage-relay` server + smoke) | `client/src/game/gameSession.ts`, `client/src/ui/PeerOverlay.tsx`, `server/src/damage_relay.rs`, `client/tools/damage-server-smoke.mjs`, `.github/workflows/ci.yml` (`client-damage-server-smoke` job), `docs/SPEC.md` + `HANDOFF.md` per-PR entry | 1-2 | `client-damage-server-smoke` passes + dev-box two-tab convergence verified + PR 11.5 WAN-throttle playtest |
| 11.6.E | **WebTransport layer (deferred)** (replace WebSocket with WebTransport; add cert handling; production transport) | `server/src/transport.rs` rewrite + `client/src/net/serverTransport.ts` webtransport branch | 1 (separate session) | Same smoke on WebTransport transport + docs/SPEC.md §"Stack" reconciled |

**Total estimate: 4-6 sessions** end-to-end (matches spec's "~2-4
sessions" lower bound was optimistic; revised estimate after surveying
the codebase, ~4-6 is honest).

### 5.1 Roll-out discipline

- **Each sub-PR opens with a fresh codex dispatch**. No "extend the
  in-progress branch." Each PR is reviewed + merged before the next.
- **Each sub-PR lands SPEC.md + HANDOFF.md updates in the same commit.**
  Stale docs are the load-bearing bug class from Phase 0 (PR 7.4
  cleanup was an entire sub-PR spent on this; don't repeat).
- **Each sub-PR ends with all existing smokes still passing.** No
  regressions allowed into main. If a smoke must change, the change is
  in the same PR + the smoke's value-add is documented in `HANDOFF.md`.
- **`canary-server.sh` is the dev-box launch surface.** Same role as
  `vite-main-tailscale.sh` does for the client. Lives under `tools/` so
  sessions can reuse it.

### 5.2 Where I'm NOT pushing back

- The spec calls for Phase 1 to use WebTransport. We're getting to it
  in 11.6.E, not 11.6.B. This is a deliberate two-step to defer the
  cert puzzle until the wire-protocol question is settled.
- The spec has Hetzner hosting for Phase 1. PR 11.6 deploys to
  localhost; production Hetzner deploy is a separate CI/CD PR after
  11.6.E.

---

## 6. Open questions for review

These are the calls **only you can make**, in priority order:

### Q1. **WebSocket-first or WebTransport-first?**
PR 11.6.B's `tools/canary-server.sh` + smoke assumes WebSocket for
the dev path. WebTransport lands as PR 11.6.E. Alternative:
WebTransport first, which would push PR 11.6.E into 11.6.B and push
the canary smoke out by 1-2 sessions for cert chain work.

**My recommendation**: WebSocket-first (§3.3). The damage broadcast
flow doesn't benefit from UDP's no-head-of-line guarantee —
retransmits on the control channel are fine, the lockstep channel
is separate.

### Q2. **Matchmaker or hard-coded "DEVBX" room?**
PR 11.6.D's smoke needs both tabs in the same room. Options:
- (a) Both tabs share a hard-coded `roomId = "DEVBX"` (smoke-only).
  Spec-style matchmaking (`roomId = "DEVBX"` from URL or random on
  host) is a separate PR.
- (b) Add a minimal matchmaker endpoint (`POST /rooms` creates a
  room, `GET /rooms/<id>` polls existence). Two-tab smoke would
  hit the same `/rooms` endpoint.

**My recommendation**: (a). Smoke-friendliness wins; Phase 1's
matchmaker is a separate PR after 11.6.E.

### Q3. **Server-frame clock source?**
For PR 11.6.D, `DamageBroadcast.serverFrame` needs a clock. Options:
- (a) `tokio::time::Instant::now()` at server startup, monotonic across
  the process. Re-broadcast frames per damage event.
- (b) `server.nextDamageBroadcastNumber` counter, wraps at u32::MAX.

**My recommendation**: (a). Monotonic clock is universal and matches
the spec's "tick-driven" framing.

### Q4. **Whose eventId is canonical?**
The tab-issued `eventId` is the dedupe key on the tab side; the
server's `serverSeq` is the dedupe key on the server side. Q:
should we *also* send the server's `serverSeq` back to the tab? Q4
specifically: do we want bidirectional dedupe (tab's `eventId`
matched against server's echoed `originEventId`)?

**My recommendation**: yes — round-trips are cheap, dedupe is the
canonical Phase 4 anti-cheat concern.

### Q5. **Should PR 11.6 ship before the existing sim server gets a
`Disconnect` handling layer?**
Today, PR 11.5's pause-when-too-far-behind has a known honest
limitation: a `paused` tick drops the input that the cap fired on
(both clients drop symmetrically, no de-sync, but the input is lost).
PR 11.6 inherits this — if the server's damage broadcast doesn't fire
on a `paused` frame, the rising-edge is lost.

**My recommendation**: yes, ship PR 11.6 with this carry-over. The
fix is a follow-up that ties `paused: true` ticks to a "request
re-broadcast" wire packet from the server. Documented as
post-11.6.D follow-up.

---

## 7. What this PR (11.6.A) ships

Just the plan. Specifically:

- `docs/PR-11.6-plan.md` — this file.
- `docs/SPEC.md` — "Phase 1 work — server-authoritative networking"
  banner updated; "Next" list updated to read "(1) PR 11.6 in
  progress (this PR) → (2-4) Phase 1 follow-ups." No status flips
  because nothing's shipped yet.
- `HANDOFF.md` — new top entry: "2026-08-15 — PR 11.6.A opened (plan PR).
  PR 11.6 server-auth damage architecture documented in `docs/PR-11.6-plan.md`
  for review. No Rust code, no codex dispatch, no Vite churn. Net new
  pending review."

No `Cargo.toml`. No new dependencies. No CI jobs.

---

## 8. Review checklist (for kyle)

- [ ] WebSocket-first vs WebTransport-first (§3.3 / Q1) — make the call
- [ ] Hard-coded room vs matchmaker (§6 / Q2) — make the call
- [ ] Server-frame clock source (§6 / Q3) — confirm monotonic-`Instant` is fine
- [ ] Bidirectional eventId dedupe (§6 / Q4) — yes/no
- [ ] Ship PR 11.6 with the `paused`-tick-input-loss carry-over (§6 / Q5) — confirm OK
- [ ] Sub-PR count (5 sub-PRs, 4-6 sessions) — OK?
- [ ] SPEC.md + HANDOFF.md placement (in the docs/ subfolder, both
      go in same PRs as code changes) — confirm convention
- [ ] No `world-factory-ctf` Rust scaffold to crib from
      (`~/Development/world-factory-ctf` does not exist; closest Rust
      repos are `~/Development/world-factory` and `~/Development/orca-rust`)
      — confirm greenfield-from-scratch design is acceptable

---

## Appendix A: Decisions intended to land in `docs/SPEC.md` decisions log

(Drafted now so the prose is reviewable here, then copied into SPEC
when this PR merges.)

### 2026-08-15 — PR 11.6 architecture decisions (plan PR)

- **WebSocket-first dev path, WebTransport production.** PR 11.6.B's
  `canary-server.sh` ships WebSocket. WebTransport layered as PR 11.6.E.
  See `docs/PR-11.6-plan.md` §3.3 for rationale.
- **Damage auth only — movement stays in lockstep for PR 11.6.**
  Movement authorization is a later PR. PR 11.6 adds server-auth
  `DamageRequest`/`DamageBroadcast` only, leaving the lockstep +
  WebRTC substrate intact.
- **No codegen for the wire protocol.** Hand-translated
  `protocol/damage.ts` + `protocol/damage.rs`. Same byte layout in both.
  Three server-side round-trip tests assert the symmetry.
- **`GameTransport` 4-channel interface.** Existing `inputs` + new
  `damageRequests` + `damageBroadcasts` + `state` (unchanged). First
  byte as discriminator on the WebSocket message.
- **Hard-coded `DEVBX` room for the dev-box smoke.** Matchmaker is a
  separate PR after 11.6.E.
- **`paused`-tick input-loss carry-over** documented + tabled to a
  follow-up PR. PR 11.6 ships with this limitation inherited from
  PR 11.5.
