# Specialists Web — Living Spec

> **The canonical spec lives at [`docs/SPEC.md`](docs/SPEC.md).**
>
> This file is a stub pointer. All editing happens in `docs/SPEC.md` on a branch + PR. The vault copy at `~/Obsidian/mem/projects/specialists-web.md` is a one-way mirror generated by `tools/sync-spec-to-vault.sh`.

## Why this layout

- The repo is the single source of truth for the specialists-web spec.
- A thin pointer at `SPEC.md` keeps the file at the repo root where contributors and CI expect to find it.
- The Obsidian vault gets a mirrored copy so the project stays linked in the vault graph; the mirror is regenerated, never hand-edited.
- A CI guard (`.github/workflows/ci.yml` → `check-spec-canonical`) verifies the spec is the canonical file and that the vault mirror matches what the repo says.

## See also

- [`docs/SPEC.md`](docs/SPEC.md) — the canonical spec
- [`HANDOFF.md`](HANDOFF.md) — session-to-session continuity
- [`README.md`](README.md) — repo intro and quickstart
- [`tools/sync-spec-to-vault.sh`](tools/sync-spec-to-vault.sh) — repo → vault sync
